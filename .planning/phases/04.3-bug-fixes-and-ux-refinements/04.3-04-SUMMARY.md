---
phase: 04.3-bug-fixes-and-ux-refinements
plan: "04"
subsystem: frontend
tags: [react, typescript, posterwall, gamellobby, app, null-guard, styling]

# Dependency graph
requires:
  - phase: 04.3-02
    provides: Backend enrichment context
provides:
  - PosterWall null-safe poster rendering (BUG-A)
  - App.tsx flex column layout eliminating empty row (BUG-D)
  - Pastel green Continue and pastel red Archive buttons in GameLobby (BUG-D)
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "posterUrl returns string | null; callers guard before use (early-return if !url)"
    - "renderablePosters filter applied before layout logic — count check and distributeColumns use clean list"
    - "variant=outline + className override pattern for custom-colored shadcn Button"

key-files:
  created: []
  modified:
    - frontend/src/components/PosterWall.tsx
    - frontend/src/App.tsx
    - frontend/src/pages/GameLobby.tsx

key-decisions:
  - "posterUrl changed from string to string | null — callers guard with if (!url) return null inside map; no TypeScript errors"
  - "renderablePosters filter precedes length check so minimum-5 threshold counts only renderable items"
  - "App.tsx flex flex-col eliminates block-layout gap caused by RadarrNotificationBanner returning null but occupying flow as empty slot"
  - "variant=outline used as base for colored buttons — provides correct interactive states; className overrides colors only"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-03-18
---

# Phase 4.3 Plan 04: PosterWall Null-Guard + BUG-D Visual Fixes Summary

**Three pure-frontend fixes: PosterWall null-safe poster rendering (BUG-A), App.tsx flex layout fixing empty row (BUG-D), and pastel button colors on session cards (BUG-D)**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-18T19:29:00Z
- **Completed:** 2026-03-18T19:33:14Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `PosterWall` no longer constructs broken `https://image.tmdb.org/t/p/w185null` URLs — `posterUrl()` returns `string | null` and callers guard before rendering; pre-filter with `renderablePosters` ensures layout logic only sees valid items
- `onError` handler on `<img>` hides broken images as a final safety net
- `App.tsx` outer container changed from block to `flex flex-col` — eliminates narrow empty row below NavBar caused by `RadarrNotificationBanner` occupying a block-flow slot when returning null
- Continue and Archive buttons on session cards now have pastel green and red styling respectively (`variant="outline"` + className override pattern)

## Task Commits

Each task committed atomically:

1. **Task 1: BUG-A — PosterWall null-guard for missing poster paths** — `22cf25a` (fix)
2. **Task 2: BUG-D — empty row fix and pastel button colors on session cards** — `a245ea9` (fix)

## Files Created/Modified

- `frontend/src/components/PosterWall.tsx` — `posterUrl` returns `string | null`; `renderablePosters` filter; `if (!url) return null` guard in map; `onError` handler on img
- `frontend/src/App.tsx` — `flex flex-col` on outer container; `w-full flex-1` on content div
- `frontend/src/pages/GameLobby.tsx` — Archive button: `variant="outline" className="bg-red-100 text-red-800 border border-red-200 hover:bg-red-200"`; Continue button: `variant="outline" className="bg-green-100 text-green-800 border border-green-200 hover:bg-green-200"`

## Decisions Made

- `posterUrl` return type changed to `string | null` — TypeScript enforces callers guard; confirmed zero TS errors with `npx tsc --noEmit`
- `renderablePosters` filter runs before the `length < 5` check — minimum threshold counts only items that will actually render
- `flex flex-col` on App.tsx container is the minimal fix for the empty row — `RadarrNotificationBanner` participates in flex layout as a zero-height item when returning null, not as a block-flow gap
- NavBar.tsx has no bottom margin — no changes needed there; the empty row was from block-layout treatment of the null banner slot

## Deviations from Plan

None — plan executed exactly as written. All three fixes applied in the specified locations with the exact patterns from the plan.

## Self-Check: PASSED

All 3 modified files confirmed present. Both task commits (22cf25a, a245ea9) confirmed in git log.
