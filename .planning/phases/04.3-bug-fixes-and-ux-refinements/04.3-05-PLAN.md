---
phase: 04.3-bug-fixes-and-ux-refinements
plan: "05"
type: execute
wave: 3
depends_on: ["04.3-03", "04.3-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 7 Phase 4.3 items (BUG-A, BUG-B, UX-A, UX-B, UX-C, BUG-C, BUG-D) verified on live NAS"
    - "Backend tests pass in Docker"
    - "Frontend tests pass locally"
    - "Poster images visible behind blur on session pages"
    - "Images remain visible after Mark as Watched click"
    - "Column headers sort movies correctly with ↑/↓ indicators"
    - "Load More appends movies to the list"
    - "Session stats show only Watched, Runtime, Started"
    - "Search returns full filmography results from backend"
    - "No empty row below NavBar"
    - "Continue button is pastel green, Archive button is pastel red"
  artifacts:
    - path: ".planning/phases/04.3-bug-fixes-and-ux-refinements/04.3-05-SUMMARY.md"
      provides: "NAS verification results"
      contains: "PASS"
  key_links: []
---

<objective>
Rebuild Docker images, deploy to NAS, run full automated test suite in Docker, then perform human verification of all 7 Phase 4.3 items on the live NAS. This is the phase gate checkpoint.

Purpose: Confirm all bugs are fixed and UX improvements work in the production environment before Phase 5.
Output: Verified phase complete; SUMMARY with NAS test results.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-bug-fixes-and-ux-refinements/04.3-CONTEXT.md
@.planning/phases/04.3-bug-fixes-and-ux-refinements/04.3-03-SUMMARY.md
@.planning/phases/04.3-bug-fixes-and-ux-refinements/04.3-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + Docker rebuild + NAS deploy</name>
  <files></files>
  <read_first>
    - .planning/STATE.md (confirm NAS deploy command pattern used in prior phases)
  </read_first>
  <action>
Run the full test suites locally, then rebuild Docker images and deploy to NAS.

**Step 1 — Backend test suite:**
```bash
cd /Users/Oreo/Projects/backend && python -m pytest tests/ -q
```
All tests must pass or skip. Zero failures.

**Step 2 — Frontend test suite:**
```bash
cd /Users/Oreo/Projects/frontend && npx vitest run
```
All tests must pass. Zero failures (all SessionCounters tests now GREEN after plan 04.3-03).

**Step 3 — TypeScript compile check:**
```bash
cd /Users/Oreo/Projects/frontend && npx tsc --noEmit
```
Zero errors.

**Step 4 — Docker rebuild (no-cache to pick up all frontend/backend changes):**
```bash
cd /Users/Oreo/Projects && make rebuild
```
Or if no Makefile target: `docker compose build --no-cache backend frontend`

**Step 5 — NAS deploy:**
Push updated images to NAS and restart containers using the established deploy pattern from prior phases (docker push sambo7262/cinemachain-*:latest then pull+restart on NAS via SSH or deploy script). Use the same commands that worked in 04.2-06.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && python -m pytest tests/ -q 2>&1 | tail -5 && cd /Users/Oreo/Projects/frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0 (passes or skips, 0 failures)
    - `npx vitest run` exits 0 (0 failures)
    - `npx tsc --noEmit` exits 0 (0 type errors)
    - Docker build completes without error
    - NAS containers respond: `curl -s http://localhost:8000/health` returns `{"status": "ok"}`
  </acceptance_criteria>
  <done>All automated tests green. NAS deployment successful. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 7 Phase 4.3 items implemented and deployed:
- BUG-A: PosterWall null-guard + img error handler
- BUG-B: mark_current_watched enriches thumbnails + keepPreviousData on session query
- UX-A: Column-header sort with ↑/↓ indicators replacing sort dropdown
- UX-B: Load More appends movies + scrolls to first new result + hides when exhausted
- UX-C: SessionCounters simplified to Watched/Runtime/Started (Steps and Actors removed)
- BUG-C: Search passes to backend, returns full filmography without pagination
- BUG-D: Empty row eliminated + Continue=green / Archive=red button colors
  </what-built>
  <how-to-verify>
Open the NAS app and perform each check in order:

**BUG-A — Poster wall images:**
1. Navigate to any active game session page
2. Confirm: The session page background shows a blur of movie poster images (not a blank/dark background)
3. If no posters appear: navigate to a different session that has been played further; the poster wall shows when 5+ posters are cached

**BUG-B — Images persist after Mark as Watched:**
1. On an active session page (view = "home"), confirm the Now Playing movie poster is visible
2. Click "Mark as Watched"
3. Confirm: The Now Playing poster and chain history thumbnails remain visible (do not go blank) immediately after clicking

**UX-A — Column sort headers:**
1. Navigate to an active session, watch the current movie, go to Eligible Actors tab, pick an actor
2. Go to Eligible Movies tab — movies should be sorted by Rating descending by default (highest ★ first)
3. Click the "Rating" column header — observe ↑ or ↓ indicator appears, sort order changes
4. Click "Rating" again — direction reverses
5. Click "Year" header — movies sort by year with ↑ indicator
6. Click "Runtime" and "Rated" headers — each sorts correctly
7. Confirm: No sort dropdown control exists (it has been removed)

**UX-B — Load More appends:**
1. On the Eligible Movies tab with an actor selected (actor with 20+ movies), scroll to the bottom
2. Click "Load more"
3. Confirm: Movies from page 1 remain visible above the new results (list grows, not replaced)
4. Confirm: View scrolls to show the first newly added movie
5. Load all pages — confirm the "Load more" button disappears when no more pages exist
6. Type a search term — confirm "Load more" button disappears immediately

**UX-C — Session stats simplified:**
1. On any active session page (view = "home" or tab view)
2. Confirm: Session stats bar shows exactly 3 counters: Watched · Runtime · Started
3. Confirm: "Steps" and "Actors" counters are absent

**BUG-C — Backend search:**
1. On Eligible Movies tab with an actor that has a large filmography (e.g. Tom Hanks)
2. Type "cast" or another distinctive word in the search box
3. Confirm: Results update immediately (backend query), showing movies whose title contains the search term
4. Confirm: No "Load more" button while search is active
5. Clear the search — confirm movies return to normal paginated view with Load more

**BUG-D — Empty row + button colors:**
1. Navigate to a game session page (not the home page)
2. Confirm: No narrow empty strip visible directly below the NavBar
3. Navigate to the home page (/)
4. Confirm: Active session cards show "Continue →" button in pastel green
5. Confirm: Active session cards show "Archive" button in pastel red
  </how-to-verify>
  <resume-signal>
Type one of:
- "all pass" — all 7 items verified on NAS
- "partial: [items that passed]" — with list of items that failed so gap-closure plans can be created
- Describe specific failures if any items need re-investigation
  </resume-signal>
</task>

</tasks>

<verification>
Phase 4.3 complete when human verifier confirms all 7 items pass on NAS. Any failures get documented and gap-closure plans created via `/gsd:plan-phase --gaps`.
</verification>

<success_criteria>
- All 7 Phase 4.3 items verified PASS on live NAS
- Full automated test suite green
- Phase 5 (Production Deployment) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-bug-fixes-and-ux-refinements/04.3-05-SUMMARY.md` with NAS verification results.

Also update:
- `.planning/STATE.md` — set current phase to 4.3 complete; update progress to 97%+
- `.planning/ROADMAP.md` — mark Phase 4.3 plans complete
</output>
