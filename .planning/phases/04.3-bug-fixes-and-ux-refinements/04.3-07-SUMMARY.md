---
phase: 04.3-bug-fixes-and-ux-refinements
plan: "07"
subsystem: api
tags: [fastapi, sqlalchemy, poster-wall, movies]

# Dependency graph
requires:
  - phase: 04.3-04
    provides: PosterWall frontend component with TMDB CDN fallback via posterUrl()
provides:
  - Broadened poster-wall supplement query returning movies with poster_path regardless of vote_count
affects: [BUG-A gap closure, NAS poster wall rendering]

# Tech tracking
tech-stack:
  added: []
  patterns: [nullslast() ordering on nullable SQLAlchemy columns to prevent NULL-sort edge cases]

key-files:
  created: []
  modified:
    - backend/app/routers/movies.py

key-decisions:
  - "04.3-07: Removed vote_count.isnot(None) filter from supplement query — on fresh NAS the nightly cache has not run and most movie stubs have vote_count=NULL, causing supplement to return zero rows; removed filter allows any movie with poster_path to appear"
  - "04.3-07: vote_count.desc().nullslast() + Movie.tmdb_id secondary sort — popular cached movies appear first when available; uncounted movies appear as stable fallback without crash"

patterns-established:
  - "nullslast() pattern: use .nullslast() on SQLAlchemy 2.x ordering expressions when the sorted column is nullable to avoid unintended empty result sets or DB-level sort errors"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-03-18
---

# Phase 04.3 Plan 07: BUG-A Poster Wall Supplement Query Fix Summary

**Removed `vote_count IS NOT NULL` filter from poster-wall supplement query and added `nullslast()` ordering so fresh-NAS movie stubs with null vote_count are included in the poster wall response**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-18T20:35:00Z
- **Completed:** 2026-03-18T20:40:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Removed `Movie.vote_count.isnot(None)` from the Step 2 WHERE clause in `get_poster_wall`
- Changed ordering to `Movie.vote_count.desc().nullslast()` to prevent NULL-sort issues and keep popular movies ranked first
- Added `Movie.tmdb_id` as stable secondary sort key
- On a fresh NAS where the nightly cache job has not run, the endpoint now returns all movies with `poster_path IS NOT NULL` instead of returning zero supplement rows

## Task Commits

Each task was committed atomically:

1. **Task 1: Broaden poster-wall supplement query to include movies without vote_count** - `daec86b` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `backend/app/routers/movies.py` - Supplement query updated: filter removed, nullslast ordering added

## Decisions Made
- Removed `vote_count.isnot(None)` filter without replacement — vote_count being null is not an error condition, just means the nightly enrichment cache has not run yet
- Chose `nullslast()` over COALESCE or other approaches — native SQLAlchemy 2.x API, no magic values

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `python` binary not found locally (python3 required) — syntax-checked with `python3 -m py_compile` instead; confirmed OK
- asyncpg not installed locally — import verification skipped; pytest confirmed test skips rather than failures (matches project convention)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- BUG-A gap closure partially addressed on the backend side: supplement query now returns enough rows
- NAS re-verification required to confirm poster wall renders with 5+ items
- UX-A (rating sort order) gap closure still pending (plan 04.3-08)

---
*Phase: 04.3-bug-fixes-and-ux-refinements*
*Completed: 2026-03-18*
