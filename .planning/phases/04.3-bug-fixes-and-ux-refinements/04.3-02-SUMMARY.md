---
phase: 04.3-bug-fixes-and-ux-refinements
plan: "02"
subsystem: api
tags: [fastapi, game, eligible-movies, thumbnails, search, sort]

# Dependency graph
requires:
  - phase: 04.3-01
    provides: Wave 0 test stubs for BUG-B/BUG-C/UX-A already in test_game.py
provides:
  - mark_current_watched endpoint enriches response with poster_path/profile_path/runtime on steps
  - eligible-movies endpoint accepts search param (full filmography, no pagination)
  - eligible-movies accepts sort=year and sort=mpaa with sort_dir=asc|desc
  - eligible-movies applies sort_dir to all sort types (rating, runtime, genre, year, mpaa)
affects: [04.3-03, 04.3-04, 04.3-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "_enrich_steps_thumbnails + _enrich_steps_runtime called together before _build_session_response in all response-building endpoints"
    - "Search bypasses pagination: early-return with page=1 page_size=total has_more=False"
    - "sort_dir _desc flag applied as reverse= kwarg to all sort() calls"

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "sort_dir default is 'desc' — matches existing UI expectation of highest-rated-first default"
  - "search block placed after sort so sort order is preserved within search results"
  - "_ensure_actor_credits_in_db in search block only fires when actor_id is not None and tmdb_client is available — harmless no-op for combined-view (actor_id=None) which cannot easily call it"
  - "Existing sort cases (rating, runtime, genre) now respect sort_dir; previous behavior was always-descending for rating (negated key) and always-ascending for runtime/genre"

patterns-established:
  - "Enrichment pattern: all endpoints that call _build_session_response must call _enrich_steps_thumbnails and _enrich_steps_runtime first"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-18
---

# Phase 4.3 Plan 02: Backend Enrichment and Eligible-Movies Extension Summary

**BUG-B backend fix (mark_current_watched enriched response) and BUG-C + UX-A backend fix (eligible-movies search/sort_dir/year/mpaa params) in game.py**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-18T19:26:47Z
- **Completed:** 2026-03-18T19:29:01Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- `mark_current_watched` now returns fully enriched steps with `poster_path`, `profile_path`, and `runtime` — fixes root cause of BUG-B blank images written to React Query cache
- `get_eligible_movies` accepts `search` param that calls `_ensure_actor_credits_in_db` (no-op if cached) then returns all title-matching results without pagination
- `get_eligible_movies` accepts `sort_dir` (asc/desc) and new sort values `year` and `mpaa`, enabling UX-A column-header sort frontend work

## Task Commits

Each task was committed atomically:

1. **Task 1: BUG-B — Add thumbnail enrichment to mark_current_watched** - `7adf7ef` (fix)
2. **Task 2: BUG-C + UX-A backend — add search and sort_dir params to eligible-movies** - `20a7209` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified

- `backend/app/routers/game.py` — Two surgical additions: mark_current_watched enrichment (+9 lines) and eligible-movies search/sort extension (+52 lines)

## Decisions Made

- `sort_dir` defaults to `"desc"` to preserve existing UI behavior (highest-rated first by default)
- Search block runs after sort so search results maintain sort order
- `_ensure_actor_credits_in_db` in search block is gated on `actor_id is not None` — combined-view (no actor selected) skips the call since there's no single actor to ensure credits for
- Existing sort cases now respect `sort_dir` — previous `rating` sort negated the key to achieve descending; replaced with `reverse=_desc` for consistency

## Deviations from Plan

None — plan executed exactly as written. All three changes applied in the specified locations with the exact code from the plan.

## Issues Encountered

None. The Edit tool required additional context to uniquely identify the replacement target in `mark_current_watched` (multiple similar re-fetch blocks exist in the file), resolved by including the preceding `await db.commit()` comment block as anchor text.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- BUG-B backend complete — frontend fix (placeholderData: keepPreviousData on session query) ready to implement in 04.3-03
- BUG-C backend complete — frontend search input + no-pagination-in-search-mode ready to implement in 04.3-04
- UX-A backend complete — sort_dir and year/mpaa sort values available for column-header sort frontend in 04.3-03 or 04.3-04

---
*Phase: 04.3-bug-fixes-and-ux-refinements*
*Completed: 2026-03-18*
