---
phase: 04.3-bug-fixes-and-ux-refinements
plan: "06"
subsystem: api
tags: [python, fastapi, sorting, game]

# Dependency graph
requires:
  - phase: 04.3-02
    provides: eligible-movies endpoint with sort/sort_dir parameters for rating, year, runtime, mpaa
provides:
  - Null-stable sort comparators for all five sort columns in get_eligible_movies
affects: [phase-05-production-deployment]

# Tech tracking
tech-stack:
  added: []
  patterns: [two-pass null separation before sort — separate nulls, sort non-nulls, concat]

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "Two-pass split (partition nulls then sort each group) instead of null-guard boolean in tuple key — avoids reverse=True flipping the null-guard bit"
  - "Pattern applies uniformly to all five sort columns: rating, runtime, genre, year, mpaa"

patterns-established:
  - "Null-stable sort: rated = [non-null]; unrated = [null]; rated.sort(..., reverse=_desc); result = rated + unrated"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-03-18
---

# Phase 04.3 Plan 06: Sort Null-Stability Fix Summary

**Two-pass null-stable sort replaces buggy tuple comparator in get_eligible_movies — null/unrated movies now always appear last regardless of sort direction**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-18T20:35:00Z
- **Completed:** 2026-03-18T20:40:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Fixed null-stability bug in rating sort: null-rated movies (below 500 vote floor) now land last in both ascending and descending order
- Applied the same two-pass fix to runtime, genre, year, and mpaa sort columns
- Removed the buggy `(is_none_bool, value)` tuple-key pattern from all five sort branches

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix null-stable sort comparators for rating, year, runtime, mpaa** - `89237c5` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `/Users/Oreo/Projects/backend/app/routers/game.py` - Replaced five sort blocks with two-pass null-stable implementation

## Decisions Made

- Two-pass split pattern chosen over alternative approaches: partition into non-null and null lists, sort only the non-null list with `reverse=_desc`, then concatenate null list at the end. This keeps null placement independent of sort direction.
- The `_effective_rating` helper and `VOTE_FLOOR = 500` constant are unchanged — only the sort block is modified.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `python -c "from app.routers.game import ..."` fails locally due to missing asyncpg (expected behavior throughout the project — passes in Docker). Python syntax verified clean via `ast.parse`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- UX-A rating sort bug is now resolved. Combined with UX-A direction toggle (already working from 04.3-03), the full column sort feature is complete.
- BUG-A (poster wall images on NAS) remains the only open gap closure item before Phase 5.
- Phase 5 (Production Deployment) ready once BUG-A is resolved.

---
*Phase: 04.3-bug-fixes-and-ux-refinements*
*Completed: 2026-03-18*
