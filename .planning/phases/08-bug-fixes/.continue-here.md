---
phase: 08-bug-fixes
task: complete
total_tasks: complete
status: complete
last_updated: 2026-03-30T00:00:00Z
---

<current_state>
Phase 08 is fully executed and verified (11/11 must-haves passed). Several post-execution hotfixes were applied this session in response to live testing on NAS. All hotfixes are committed and pushed to GitHub. Docker CI/CD pipeline is now in place. Context exhausted — pausing here.
</current_state>

<completed_work>

**Phase 08 execution (all 4 plans complete):**
- 08-01: Missing data display — MPAA NR fallback, RT em dash, overview fallback, _backfill_overview_pass
- 08-02: Mobile UI overflows — actor name truncation, Now Playing break-words, GameLobby flex-wrap
- 08-03: Pagination — Load More replaced with prev/next, ChainHistory paginated
- 08-04: Session bugs — CSV export forward-scan, eligible movies self-heal for missing credits

**Post-execution hotfixes (all pushed to main):**
- `b005132` — Validate actor-movie credit link on movie pick (prevents invalid chain links from stale credits)
- `b5a4269` — GitHub Actions CI/CD: builds and pushes Docker images on every push to main
- `e4442ac` — Committed missing utils.ts (was untracked, broke CI build)
- `721a127` — Removed leftover accumulation references (useRef, setAccumulatedMovies, idx) that broke tsc -b in Docker
- `c4ff550` — Added missing `logger` to game.py (caused 500 on all eligible-movies requests)
- `6181f79` — Added `filmography_fetched` flag to Actor model (migration 0009) + reverted sticky table columns
- `65b7fa4` — Stable sort for eligible movies: sort low-rated bucket, add tmdb_id tiebreaker
- `b5437d3` — Per-movie exception handling in _backfill_mpaa_pass (was crashing after first failure)
</completed_work>

<remaining_work>

**Additional hotfixes committed 2026-03-30 (pushed as 2dc9eec):**
- Overview backfill now catches `overview=""` in addition to NULL — HP:DH2 and similar will be fixed on next nightly run
- VOTE_FLOOR lowered 500→10 — actor-scoped eligible movies now sort by rating correctly (obscure films with real ratings no longer buried)
- Eligible Movies tab scrolls to top on prev/next page navigation

**Pending NAS verification (to test tomorrow):**
- Overview appearing for HP:DH2 (requires nightly job to run first)
- Rating sort correctness in actor-scoped view
- Pagination scroll-to-top

**Next phase:**
- Phase 09: Navigation Redesign (`/gsd:discuss-phase 9` or `/gsd:plan-phase 9`)
</remaining_work>

<decisions_made>

- `filmography_fetched` flag on Actor table: correct fix for actors like Drew Barrymore showing 0 eligible movies (stale credit short-circuit was too aggressive)
- Sticky columns reverted: CSS sticky not working as expected on mobile — deferred
- Sort stability fix: split "unrated" bucket into low_rated (has vote_average but below floor) and no_rating (null), each sorted separately
- `_backfill_mpaa_pass` missing per-movie exception handler was causing the entire MPAA backfill to silently fail after the first movie — fixed with try/except per movie
- GitHub Actions CI: triggers on push to main, builds both images, tags with `latest` + git SHA. Requires DOCKERHUB_USERNAME + DOCKERHUB_TOKEN secrets in GitHub repo settings.
</decisions_made>

<blockers>

- Docker Hub secrets needed: DOCKERHUB_USERNAME and DOCKERHUB_TOKEN must be set in GitHub repo Settings → Secrets → Actions for CI to push images
- MPAA data for "The Others": will self-correct on next nightly run (no code action needed)
</blockers>

<context>
Phase 08 was a bug-fix phase. All 8 BUG requirements satisfied. Multiple post-execution issues found during live NAS testing — all fixed. The session was heavily reactive (fix → test on NAS → new bug reported → fix). Main systemic issues were: missing logger in game.py (caused 500s), filmography_fetched flag missing (actors with only 1 credit showed 0 movies), sort instability in unrated bucket, and MPAA backfill crashing silently. All resolved.
</context>

<next_action>
Start with: `/gsd:discuss-phase 9` to begin planning the Navigation Redesign phase. Run `/clear` first to get a fresh context window.
</next_action>
