---
phase: 03.1-ui-improvements-and-multi-session-support
plan: "07"
type: execute
wave: 6
depends_on: ["03.1-06"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08]
must_haves:
  truths:
    - "Docker images rebuilt and pushed with Phase 03.1 changes"
    - "NAS is running the new images"
    - "All manual verification scenarios pass on live NAS"
  artifacts:
    - path: "docker-compose.yaml"
      provides: "Confirmed running on NAS with new images"
      contains: "cinemachain"
  key_links:
    - from: "Makefile rebuild target"
      to: "NAS Docker daemon"
      via: "docker buildx build + compose pull + compose up"
      pattern: "sambo7262/cinemachain.*latest"
---

<objective>
Rebuild Docker images with all Phase 03.1 changes, deploy to NAS, run Alembic migration 0004, and perform human verification of all new features.

Purpose: Phase 03.1 cannot be declared complete without live NAS verification — the NAS is the only environment where the full stack runs.
Output: Phase 03.1 declared complete after all manual scenarios pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-ui-improvements-and-multi-session-support/03.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend test suite and Docker rebuild</name>
  <files></files>
  <read_first>
    - Makefile (rebuild target pattern — confirmed from Phase 3: `docker buildx build --platform linux/amd64 --no-cache -t sambo7262/cinemachain-backend:latest --push backend/` and same for frontend)
  </read_first>
  <action>
Run the full backend test suite first to confirm no regressions before rebuilding:

```bash
cd /Users/Oreo/Projects/backend && python -m pytest tests/ -x -q
```

All tests must pass (including the 8 new Phase 03.1 stubs). If any fail, stop and fix before proceeding.

Then rebuild and push both images using the Makefile rebuild target (matches the pattern from Phase 3 deploys):

```bash
cd /Users/Oreo/Projects && make rebuild
```

If there is no `make rebuild` target, run:
```bash
docker buildx build --platform linux/amd64 --no-cache \
  -t sambo7262/cinemachain-backend:latest --push backend/

docker buildx build --platform linux/amd64 --no-cache \
  -t sambo7262/cinemachain-frontend:latest --push frontend/
```
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -x -q` exits 0 (all tests pass, including 8 Phase 03.1 stubs)
    - Docker push command exits 0 for both backend and frontend images
    - `docker images | grep cinemachain` shows recently-built images (check timestamp)
  </acceptance_criteria>
  <done>Backend test suite passes. Both Docker images rebuilt and pushed to Docker Hub.</done>
</task>

<task type="auto">
  <name>Task 2: NAS deploy and run migration 0004</name>
  <files></files>
  <read_first>
    - docker-compose.yaml (confirm NAS deployment pattern — image names, service names, alembic command)
    - .planning/STATE.md (NAS SSH address and deploy method used in prior phases)
  </read_first>
  <action>
On the NAS (via SSH), pull and redeploy:

```bash
# Pull new images
docker compose pull

# Run migration 0004 before restarting (adds name + archived_at columns + partial unique index)
docker compose run --rm backend alembic upgrade head

# Restart services
docker compose up -d
```

Verify services are healthy:
```bash
docker compose ps
```
All services should show status "running" or "healthy".

Verify migration ran:
```bash
docker compose exec backend alembic current
```
Should show `0004 (head)`.
  </action>
  <verify>
    <automated>MISSING — NAS SSH access required. Human deploys via SSH in Task 2. Automated check is `docker compose exec backend alembic current` showing `0004`.</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows backend, frontend, db all running
    - `docker compose exec backend alembic current` shows `0004 (head)`
    - `curl http://localhost:8000/health` (or NAS equivalent) returns 200
  </acceptance_criteria>
  <done>NAS running new images. Migration 0004 applied. Services healthy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 03.1 complete implementation:
1. Multi-session support (multiple sessions active simultaneously)
2. Session naming (required at creation, unique among active)
3. Home page session grid (cards with name, current movie, chain length)
4. Archive/Archived Sessions page
5. Chain history vertical table with timestamps
6. CSV export button (downloads chain as CSV)
7. CSV import validation UI (sequence validation, row deletion, disabled when invalid)
8. TMDB ID "Movie 12345" fully eliminated
  </what-built>
  <how-to-verify>
Navigate to the app on the NAS (Tailscale IP or local).

**Scenario 1 — Multi-session and naming:**
1. Go to home page ("/")
2. Confirm the page shows a session grid (not a single "Session in progress" banner)
3. Click "+ Start a new session"
4. Leave name blank → confirm the movie tabs are disabled or creation is blocked
5. Enter "Game Night 1" and select any watched movie → session created, navigated to session page
6. Go back to "/" → "Game Night 1" card appears with current movie and step count
7. Click "+ Start a new session" again
8. Enter "Game Night 1" (same name) → confirm an error appears ("Session name already in use" or similar)
9. Enter "Game Night 2" → successfully creates a second session
10. Confirm BOTH sessions appear as cards on "/"

**Scenario 2 — Archive flow:**
11. Click Archive on "Game Night 2"
12. Confirm "Game Night 2" disappears from home page grid
13. Click "Archived" in NavBar → navigate to /archived
14. Confirm "Game Night 2" appears as a card in Archived Sessions
15. Click "View" → navigates to the session page for "Game Night 2"
16. Confirm you CANNOT start game actions (pick actor, mark watched) on an archived session — all such actions should fail gracefully (the UI may not prevent navigation, but the server will return 422 if any game actions are attempted)

**Scenario 3 — Chain history table:**
17. Navigate to an active session with at least 2 steps in the chain
18. Confirm the chain history is a vertical table (NOT a horizontal scroll)
19. Confirm rows are numbered (#1, #2...)
20. Confirm Movie rows show movie title (NOT "Movie 12345" or any numeric ID)
21. Confirm Actor rows show actor name below the corresponding movie row
22. Confirm the "When" column shows a date for watched movies, "—" for unwatched

**Scenario 4 — CSV export:**
23. Navigate to any active session
24. Click "Export CSV" button in the session header
25. Confirm a file downloads
26. Open the file and confirm: first row is `order,movie_name,actor_name`; subsequent rows have movie names (not IDs) in movie_name column

**Scenario 5 — CSV import validation:**
27. Go to home page, click "+ Start a new session"
28. Click "Import Chain" tab
29. Upload the exported CSV from Scenario 4 (roundtrip test)
30. Confirm parsed rows appear in a preview list
31. Confirm the Import Chain button is ENABLED (valid sequence from export)
32. Delete a movie row to create an invalid sequence (actor with no preceding movie)
33. Confirm the invalid row is highlighted (red border)
34. Confirm the Import Chain button is DISABLED when invalid rows exist

**Scenario 6 — NavBar:**
35. Confirm NavBar has both "Sessions" link (→ "/") and "Archived" link (→ "/archived")
36. Confirm "Sessions" link is active/highlighted when on "/" or "/game/:id"
37. Confirm "Archived" link is active/highlighted when on "/archived"
  </how-to-verify>
  <resume-signal>Type "PASS" if all scenarios pass. Or describe which scenarios failed with specifics (e.g. "Scenario 3 step 20 — TMDB ID still shows in chain history").</resume-signal>
</task>

</tasks>

<verification>
Human verification is the gate. All 6 scenarios must pass before Phase 03.1 is declared complete.
</verification>

<success_criteria>
- Backend test suite passes (python -m pytest tests/ -x -q)
- Docker images rebuilt and deployed to NAS
- Migration 0004 running on NAS (alembic current shows 0004)
- All 6 manual verification scenarios pass on live NAS
- No "Movie 12345" TMDB IDs visible anywhere in the UI
- Multiple sessions can coexist without issues
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-improvements-and-multi-session-support/03.1-07-SUMMARY.md`
</output>
