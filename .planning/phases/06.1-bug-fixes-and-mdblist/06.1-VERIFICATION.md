---
phase: 06.1-bug-fixes-and-mdblist
verified: 2026-03-22T00:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 6.1: Bug Fixes & MDBList RT Integration Verification Report

**Phase Goal:** Fix post-Phase-6 bugs and integrate MDBList RT scores throughout the app
**Verified:** 2026-03-22
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Clicking the settings gear icon in the NavBar navigates to /settings | VERIFIED | NavBar.tsx L48: `to="/settings"`; App.tsx `/settings` route at L44 |
| 2  | Home page session cards show correct watched count and total runtime (not zeros) | VERIFIED | game.py L953-954: `_enrich_steps_runtime` called in `list_sessions`; L970-971: same in `list_archived_sessions` |
| 3  | Movie model has rt_score and rt_audience_score nullable integer columns | VERIFIED | models/__init__.py L30-31: both columns present as `Mapped[Optional[int]]` |
| 4  | Alembic migration 0008 adds rt_score and rt_audience_score to movies table | VERIFIED | 20260322_0008_rt_scores.py: revision="0008", down_revision="0007", correct upgrade/downgrade ops |
| 5  | MDBList service fetches RT scores from mdblist.com API using TMDB ID | VERIFIED | mdblist.py: MDBLIST_API_URL="https://mdblist.com/api/", params `apikey` + `tm` |
| 6  | get_eligible_movies enriches movies with RT scores on-demand when mdblist_api_key is configured | VERIFIED | game.py L1592: `await fetch_rt_scores(...)`, L1596-1602: DB refresh with 0-sentinel conversion |
| 7  | EligibleMovieResponse includes rt_score field | VERIFIED | game.py L143: `rt_score: int \| None = None`; api.ts L75: `rt_score: number \| null` |
| 8  | Settings API accepts and returns mdblist_api_key | VERIFIED | settings.py L25, L40: field in both SettingsResponse and SettingsUpdateRequest |
| 9  | MDBList API key field exists on Settings page; RT scores display in table, Now Playing tile, and splash dialog | VERIFIED | Settings.tsx: MDBList Card section at L238-255; GameSession.tsx: table L951/L1015, Now Playing L599-601, splash L1085-1088 |

**Score:** 9/9 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/App.tsx` | Settings route reachable without flash of null | VERIFIED | Loading guard renders NavBar+empty div (L20-27), never returns bare null |
| `frontend/src/components/NavBar.tsx` | Settings icon link navigates correctly | VERIFIED | L48: `to="/settings"` inside React Router Link |
| `backend/app/routers/game.py` | list_sessions enriches runtime_map | VERIFIED | L953-954 and L970-971: both list endpoints call `_enrich_steps_runtime` and pass `runtime_map=runtime_map` |
| `backend/app/models/__init__.py` | Movie.rt_score and Movie.rt_audience_score columns | VERIFIED | L30-31: both nullable Integer columns present |
| `backend/alembic/versions/20260322_0008_rt_scores.py` | Migration adding rt_score columns | VERIFIED | Correct revision chain 0007→0008, proper upgrade/downgrade |
| `backend/app/services/mdblist.py` | fetch_rt_scores function | VERIFIED | Function present and substantive (not a stub) — full httpx async implementation |
| `backend/app/routers/settings.py` | mdblist_api_key in SettingsResponse and SettingsUpdateRequest | VERIFIED | L25 and L40: field present in both models |
| `frontend/src/lib/api.ts` | rt_score in EligibleMovieDTO; mdblist_api_key in SettingsDTO | VERIFIED | L75: rt_score; L146: mdblist_api_key |
| `frontend/src/pages/Settings.tsx` | MDBList API key input in settings form | VERIFIED | L238-255: MDBList Card with masked Input, emptyForm includes mdblist_api_key at L24 |
| `frontend/src/pages/GameSession.tsx` | RT score in table, Now Playing, and splash | VERIFIED | 5 occurrences of rt_score: table header L951, table cell L1015, Now Playing L599-601, splash L1085-1088 |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| NavBar.tsx | /settings | React Router Link | VERIFIED | `to="/settings"` at L48 |
| game.py list_sessions | _build_session_response | runtime_map parameter | VERIFIED | `runtime_map=runtime_map` at L954 and L971 |
| game.py get_eligible_movies | mdblist.py | import + call fetch_rt_scores | VERIFIED | L18: `from app.services.mdblist import fetch_rt_scores`; L1592: `await fetch_rt_scores(...)` |
| mdblist.py | https://mdblist.com/api/ | httpx GET request | VERIFIED | L46-48: `client.get(MDBLIST_API_URL, params={"apikey": ..., "tm": ...})` |
| settings.py | settings_service | mdblist_api_key in response/request models | VERIFIED | L25, L40: field in both Pydantic models |
| Settings.tsx | api.saveSettings | mdblist_api_key in formData | VERIFIED | L249-250: bound to `formData.mdblist_api_key` via `handleChange("mdblist_api_key")` |
| GameSession.tsx eligible movies table | EligibleMovieDTO.rt_score | movie.rt_score rendering | VERIFIED | L1015: `movie.rt_score ? \`🍅 ${movie.rt_score}%\` : ""` |
| GameSession.tsx splash dialog | splashMovie.rt_score | Badge component | VERIFIED | L1085-1088: `splashMovie?.rt_score && <Badge>🍅 {splashMovie.rt_score}%</Badge>` |

---

## Requirements Coverage

No requirement IDs were declared in the plan frontmatter (`requirements: []` across all three plans). Phase 6.1 is gap-closure only, not tied to formal requirements.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/src/pages/GameSession.tsx` | 951 | `// TODO: backend sort by rt_score` | Info | Misleading — backend sort IS implemented at game.py L1647. Comment is stale, not a real gap. |

No blocker or warning anti-patterns found. The stale TODO comment does not affect functionality.

---

## Human Verification Required

### 1. Settings Navigation End-to-End

**Test:** Load the app cold (no cache). Before the settings status API call resolves, click the gear icon in the NavBar.
**Expected:** Browser navigates to /settings without waiting for the API call.
**Why human:** Can't programmatically verify that the loading skeleton renders fast enough for the click to land.

### 2. Session Card Stats Non-Zero

**Test:** On the home page (GameLobby), open a session card that has at least one watched movie.
**Expected:** Watched count and total runtime show correct non-zero values.
**Why human:** Requires a live DB with session data to confirm the fix actually returns correct values.

### 3. RT Score Display with Live MDBList Key

**Test:** Configure a real MDBList API key in Settings. Load a game session and open the eligible movies panel.
**Expected:** Movies with RT scores show "🍅 N%" in the RT column, Now Playing tile, and splash dialog.
**Why human:** Requires a live MDBList API key and actual DB-cached scores to verify rendering.

### 4. MDBList API Key Masking

**Test:** Navigate to Settings page. Observe the MDBList API key field appearance.
**Expected:** Field is masked (password dots) by default.
**Why human:** Visual behavior requires browser rendering.

---

## Gaps Summary

No gaps. All 9 must-have truths are fully verified — artifacts exist, are substantive (no stubs), and are wired correctly. All 6 feature commits (026561e, febc644, c4ca649, f72b062, f143166, 3321b5e) are confirmed present in git history. The one stale TODO comment (L951 of GameSession.tsx) is informational only and does not indicate a missing implementation.

---

_Verified: 2026-03-22_
_Verifier: Claude (gsd-verifier)_
