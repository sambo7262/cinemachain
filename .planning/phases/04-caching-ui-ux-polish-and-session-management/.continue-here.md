---
phase: 04-caching-ui-ux-polish-and-session-management
task: bug-fixes
status: blocked
last_updated: 2026-03-18T03:37:43.375Z
---

<current_state>
Actively fixing bugs found during phase 04 testing. Persistent 500 error on `request_movie` endpoint specifically when requesting the 2nd+ chain movie (not the starting movie). The starting movie Radarr call works fine; subsequent chain picks 500. Three rounds of fixes deployed, still reproducing.
</current_state>

<completed_work>

- Fix 1: `_request_radarr` — wrapped internals in try/except, replaced 502 raise with graceful `{"status": "not_found_in_radarr"}` return
- Fix 2: `apiFetch` — added 204 No Content early return (fixed delete archived session)
- Fix 3: Eligible movies table — added Runtime and Rated (MPAA) columns at xl breakpoint; bumped max-w-3xl → max-w-5xl
- Fix 4: `_ensure_actor_credits_in_db` — on_conflict_do_update now backfills title, year, poster_path (fixes blank movie cards)
- Fix 5: NotificationContext — relaxed showRadarr type to string; added banner cases for not_found_in_radarr and error statuses
- Fix 6: Added outer try/except around `_request_radarr` call in `request_movie` to mirror `create_session` pattern
- All fixes committed and rebuilt to Docker Hub (latest commit: 6561bfb)
- Deferred items filed: local poster caching, nightly sync backfill for movie stubs

</completed_work>

<remaining_work>

- **BLOCKER**: 500 still occurring on 2nd+ chain movie pick via `request_movie`. Root cause unknown — inner+outer try/except should be catching Radarr errors but isn't preventing the 500.
- After bug fix resolved: cache optimization and load time audit (user's next stated priority)

</remaining_work>

<decisions_made>

- `create_session` has always had an outer try/except around `_request_radarr` — that's why starting movie never 500'd. `request_movie` didn't have it.
- `GameSessionStep.movie_tmdb_id` has NO FK to Movie table — ruled out as cause.
- No DB constraint issues identified in models.
- The 500 is specific to the chain pick flow (pick_actor → request_movie sequence), NOT the create_session flow.

</decisions_made>

<blockers>

- **500 on request_movie (2nd+ chain pick)**: Inner try/except in `_request_radarr` + outer try/except added to `request_movie` both deployed, but 500 persists. Actual error traceback never seen — NAS docker logs needed to identify true exception. Theories exhausted without log access:
  - Not a FK constraint (no FK on movie_tmdb_id)
  - Not a DB commit issue (no constraints violated)
  - Could be BaseException (CancelledError) slipping past `except Exception`
  - Could be error occurring BEFORE or AFTER the Radarr call (in enrich_steps or build_session_response)
  - `create_session` works because it has the outer guard AND runs Radarr before pick_actor

</blockers>

<context>
The key diagnostic gap: we've never seen the actual stack trace. The user reports "500 for Radarr" but the true exception source is unknown. Next session should start by pulling NAS docker logs (`docker logs cinemachain-backend --tail 100`) immediately after reproducing the 500 to get the actual traceback. The fix may be simple once we know the real error.

Flow for 2nd chain pick:
1. handleMovieConfirm → pickActor API call → pick_actor endpoint (creates actor step)
2. handleMovieConfirm → requestMovie API call → request_movie endpoint
   - DB commit (step + session update)
   - Background task (credits prefetch)
   - Re-fetch session
   - Outer try/except → _request_radarr (inner try/except)
   - _enrich_steps_watched_at
   - _build_session_response
   - return dict

Any of the non-Radarr steps could be throwing. Log first, fix second.
</context>

<next_action>
Start with: Pull NAS docker logs immediately after reproducing the 500 to get actual traceback:
`docker logs cinemachain-backend --tail 100 2>&1 | grep -A 20 "ERROR\|Traceback\|500"`
Then read the actual exception and fix the real cause.
</next_action>
