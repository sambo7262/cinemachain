---
phase: 04-caching-ui-ux-polish-and-session-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/tests/test_cache.py
  - backend/tests/test_session_mgmt.py
  - frontend/package.json
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/__tests__/RadarrBanner.test.tsx
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - SESSION-01
  - SESSION-02
  - UX-06

must_haves:
  truths:
    - "APScheduler is listed in requirements.txt at >=3.10.4,<4.0"
    - "@radix-ui/react-dialog and @radix-ui/react-dropdown-menu are in package.json"
    - "dialog.tsx exports Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogClose"
    - "dropdown-menu.tsx exports DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem"
    - "test_cache.py contains failing stubs for CACHE-01 and CACHE-02"
    - "test_session_mgmt.py contains failing stubs for SESSION-01 and SESSION-02"
  artifacts:
    - path: "backend/tests/test_cache.py"
      provides: "Failing test stubs for nightly cache job"
    - path: "backend/tests/test_session_mgmt.py"
      provides: "Failing test stubs for delete endpoints"
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive for confirmation dialogs"
    - path: "frontend/src/components/ui/dropdown-menu.tsx"
      provides: "shadcn DropdownMenu primitive for session actions menu"
  key_links:
    - from: "frontend/src/components/ui/dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "import * as DialogPrimitive"
      pattern: "from \"@radix-ui/react-dialog\""
    - from: "frontend/src/components/ui/dropdown-menu.tsx"
      to: "@radix-ui/react-dropdown-menu"
      via: "import * as DropdownMenuPrimitive"
      pattern: "from \"@radix-ui/react-dropdown-menu\""
---

<objective>
Install new dependencies and create the test scaffolding and shadcn UI primitives that all subsequent Phase 4 plans depend on.

Purpose: Wave 0 foundation — APScheduler (backend), @radix-ui/react-dialog and @radix-ui/react-dropdown-menu (frontend) must be installed before any implementation begins. The shadcn Dialog and DropdownMenu primitives must exist before Phase 4 destructive-action plans write components that import them. Test stubs give the Nyquist feedback loop its hooks.

Output: requirements.txt updated, package.json updated with two Radix packages, dialog.tsx and dropdown-menu.tsx written, test_cache.py and test_session_mgmt.py stubs created.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-caching-ui-ux-polish-and-session-management/04-CONTEXT.md
@.planning/phases/04-caching-ui-ux-polish-and-session-management/04-RESEARCH.md
@.planning/phases/04-caching-ui-ux-polish-and-session-management/04-UI-SPEC.md
@.planning/phases/04-caching-ui-ux-polish-and-session-management/04-VALIDATION.md

<interfaces>
<!-- Existing patterns the executor must replicate -->

From frontend/src/components/ui/tabs.tsx (established shadcn manual-write pattern):
```typescript
import * as TabsPrimitive from "@radix-ui/react-tabs"
import { cn } from "@/lib/utils"
const Tabs = TabsPrimitive.Root
// ... manual exports with cn() class merging
export { Tabs, TabsList, TabsTrigger, TabsContent }
```

From backend/tests/test_game.py (existing test stub pattern):
```python
@pytest.mark.asyncio
async def test_something(async_client: AsyncClient):
    # RED — not implemented yet
    pytest.skip("not implemented")
```

From frontend/src/components/ui/ (all shadcn components follow this pattern):
- Import Radix primitive as `* as XxxPrimitive`
- Re-export roots directly
- Wrap compound pieces with `cn()` for className merging
- Export named components only (no default export)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install APScheduler and Radix packages</name>
  <files>backend/requirements.txt, frontend/package.json</files>
  <read_first>
    - backend/requirements.txt (see current packages — apscheduler absent)
    - frontend/package.json (see current @radix-ui/* packages already installed)
  </read_first>
  <action>
    1. Add `apscheduler>=3.10.4,<4.0` to backend/requirements.txt after the `httpx==0.27.2` line.

    2. Run `cd /Users/Oreo/Projects/frontend && npm install @radix-ui/react-dialog @radix-ui/react-dropdown-menu` to install the packages. This updates package.json automatically.

    Do NOT use pip install — requirements.txt is the source of truth for the Docker build. The pip install will happen inside Docker.
  </action>
  <verify>
    <automated>grep "apscheduler" /Users/Oreo/Projects/backend/requirements.txt && grep "react-dialog" /Users/Oreo/Projects/frontend/package.json && grep "react-dropdown-menu" /Users/Oreo/Projects/frontend/package.json</automated>
  </verify>
  <done>requirements.txt contains `apscheduler>=3.10.4,<4.0`; package.json contains `@radix-ui/react-dialog` and `@radix-ui/react-dropdown-menu` in dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Write shadcn Dialog and DropdownMenu primitives</name>
  <files>frontend/src/components/ui/dialog.tsx, frontend/src/components/ui/dropdown-menu.tsx</files>
  <read_first>
    - frontend/src/components/ui/tabs.tsx (shadcn manual-write pattern to replicate)
    - frontend/src/components/ui/select.tsx (second reference for cn() usage)
    - frontend/src/lib/utils.ts (cn() import path)
    - frontend/src/components/ui/dialog.tsx (check if exists — it does NOT per research)
    - frontend/src/components/ui/dropdown-menu.tsx (check if exists — it does NOT per research)
  </read_first>
  <action>
    Write `frontend/src/components/ui/dialog.tsx` with the exact content from the research document (RESEARCH.md Pattern 4). The file must export:
    `Dialog, DialogTrigger, DialogPortal, DialogOverlay, DialogClose, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription`

    Full implementation:
    ```typescript
    import * as DialogPrimitive from "@radix-ui/react-dialog"
    import { X } from "lucide-react"
    import { cn } from "@/lib/utils"

    const Dialog = DialogPrimitive.Root
    const DialogTrigger = DialogPrimitive.Trigger
    const DialogPortal = DialogPrimitive.Portal
    const DialogClose = DialogPrimitive.Close

    const DialogOverlay = React.forwardRef<
      React.ElementRef<typeof DialogPrimitive.Overlay>,
      React.ComponentPropsWithoutRef<typeof DialogPrimitive.Overlay>
    >(({ className, ...props }, ref) => (
      <DialogPrimitive.Overlay
        ref={ref}
        className={cn(
          "fixed inset-0 z-50 bg-black/80 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
          className
        )}
        {...props}
      />
    ))
    DialogOverlay.displayName = DialogPrimitive.Overlay.displayName

    const DialogContent = React.forwardRef<
      React.ElementRef<typeof DialogPrimitive.Content>,
      React.ComponentPropsWithoutRef<typeof DialogPrimitive.Content>
    >(({ className, children, ...props }, ref) => (
      <DialogPortal>
        <DialogOverlay />
        <DialogPrimitive.Content
          ref={ref}
          className={cn(
            "fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border border-border bg-background p-6 shadow-lg duration-200 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=open]:slide-in-from-left-1/2 rounded-lg",
            className
          )}
          {...props}
        >
          {children}
          <DialogClose className="absolute right-4 top-4 rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 disabled:pointer-events-none data-[state=open]:bg-accent data-[state=open]:text-muted-foreground">
            <X className="h-4 w-4" />
            <span className="sr-only">Close</span>
          </DialogClose>
        </DialogPrimitive.Content>
      </DialogPortal>
    ))
    DialogContent.displayName = DialogPrimitive.Content.displayName

    const DialogHeader = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
      <div className={cn("flex flex-col space-y-1.5 text-center sm:text-left", className)} {...props} />
    )
    DialogHeader.displayName = "DialogHeader"

    const DialogFooter = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
      <div className={cn("flex flex-col-reverse sm:flex-row sm:justify-end sm:space-x-2", className)} {...props} />
    )
    DialogFooter.displayName = "DialogFooter"

    const DialogTitle = React.forwardRef<
      React.ElementRef<typeof DialogPrimitive.Title>,
      React.ComponentPropsWithoutRef<typeof DialogPrimitive.Title>
    >(({ className, ...props }, ref) => (
      <DialogPrimitive.Title
        ref={ref}
        className={cn("text-lg font-semibold leading-none tracking-tight", className)}
        {...props}
      />
    ))
    DialogTitle.displayName = DialogPrimitive.Title.displayName

    const DialogDescription = React.forwardRef<
      React.ElementRef<typeof DialogPrimitive.Description>,
      React.ComponentPropsWithoutRef<typeof DialogPrimitive.Description>
    >(({ className, ...props }, ref) => (
      <DialogPrimitive.Description
        ref={ref}
        className={cn("text-sm text-muted-foreground", className)}
        {...props}
      />
    ))
    DialogDescription.displayName = DialogPrimitive.Description.displayName

    export {
      Dialog, DialogPortal, DialogOverlay, DialogClose, DialogTrigger,
      DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription,
    }
    ```
    Remember to add `import React from "react"` or use the React namespace correctly per the existing TSX pattern (check an existing component for whether React is explicitly imported).

    Write `frontend/src/components/ui/dropdown-menu.tsx` exporting:
    `DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel`

    ```typescript
    import * as DropdownMenuPrimitive from "@radix-ui/react-dropdown-menu"
    import { cn } from "@/lib/utils"

    const DropdownMenu = DropdownMenuPrimitive.Root
    const DropdownMenuTrigger = DropdownMenuPrimitive.Trigger
    const DropdownMenuGroup = DropdownMenuPrimitive.Group
    const DropdownMenuPortal = DropdownMenuPrimitive.Portal
    const DropdownMenuSub = DropdownMenuPrimitive.Sub
    const DropdownMenuRadioGroup = DropdownMenuPrimitive.RadioGroup

    const DropdownMenuContent = React.forwardRef<
      React.ElementRef<typeof DropdownMenuPrimitive.Content>,
      React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Content>
    >(({ className, sideOffset = 4, ...props }, ref) => (
      <DropdownMenuPrimitive.Portal>
        <DropdownMenuPrimitive.Content
          ref={ref}
          sideOffset={sideOffset}
          className={cn(
            "z-50 min-w-[8rem] overflow-hidden rounded-md border border-border bg-popover p-1 text-popover-foreground shadow-md data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2",
            className
          )}
          {...props}
        />
      </DropdownMenuPrimitive.Portal>
    ))
    DropdownMenuContent.displayName = DropdownMenuPrimitive.Content.displayName

    const DropdownMenuItem = React.forwardRef<
      React.ElementRef<typeof DropdownMenuPrimitive.Item>,
      React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Item> & { inset?: boolean }
    >(({ className, inset, ...props }, ref) => (
      <DropdownMenuPrimitive.Item
        ref={ref}
        className={cn(
          "relative flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-sm outline-none transition-colors focus:bg-accent focus:text-accent-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
          inset && "pl-8",
          className
        )}
        {...props}
      />
    ))
    DropdownMenuItem.displayName = DropdownMenuPrimitive.Item.displayName

    const DropdownMenuSeparator = React.forwardRef<
      React.ElementRef<typeof DropdownMenuPrimitive.Separator>,
      React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Separator>
    >(({ className, ...props }, ref) => (
      <DropdownMenuPrimitive.Separator
        ref={ref}
        className={cn("-mx-1 my-1 h-px bg-muted", className)}
        {...props}
      />
    ))
    DropdownMenuSeparator.displayName = DropdownMenuPrimitive.Separator.displayName

    const DropdownMenuLabel = React.forwardRef<
      React.ElementRef<typeof DropdownMenuPrimitive.Label>,
      React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Label> & { inset?: boolean }
    >(({ className, inset, ...props }, ref) => (
      <DropdownMenuPrimitive.Label
        ref={ref}
        className={cn("px-2 py-1.5 text-sm font-semibold", inset && "pl-8", className)}
        {...props}
      />
    ))
    DropdownMenuLabel.displayName = DropdownMenuPrimitive.Label.displayName

    export {
      DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem,
      DropdownMenuSeparator, DropdownMenuLabel, DropdownMenuGroup,
      DropdownMenuPortal, DropdownMenuSub, DropdownMenuRadioGroup,
    }
    ```
  </action>
  <verify>
    <automated>grep -c "DialogContent\|DialogHeader\|DialogTitle\|DialogDescription\|DialogClose" /Users/Oreo/Projects/frontend/src/components/ui/dialog.tsx && grep -c "DropdownMenuContent\|DropdownMenuItem\|DropdownMenuTrigger" /Users/Oreo/Projects/frontend/src/components/ui/dropdown-menu.tsx && cd /Users/Oreo/Projects/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "dialog\|dropdown" || true</automated>
  </verify>
  <done>Both files exist with correct exports; TypeScript compile produces no errors referencing dialog.tsx or dropdown-menu.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Write failing test stubs for CACHE-01, CACHE-02, SESSION-01, SESSION-02</name>
  <files>backend/tests/test_cache.py, backend/tests/test_session_mgmt.py</files>
  <read_first>
    - backend/tests/test_game.py (existing test patterns — fixtures, async_client, skip stubs)
    - backend/tests/conftest.py (verify fixture names: async_client, db_session, etc.)
    - backend/tests/test_cache.py (check if exists — it does NOT)
    - backend/tests/test_session_mgmt.py (check if exists — it does NOT)
  </read_first>
  <action>
    Write `backend/tests/test_cache.py` with these failing stubs:

    ```python
    """Tests for nightly TMDB cache job (CACHE-01, CACHE-02)."""
    import pytest
    from httpx import AsyncClient


    @pytest.mark.asyncio
    async def test_nightly_cache_job_fetches_top_n_movies(async_client: AsyncClient):
        """CACHE-01: nightly_cache_job() pages through TMDB discover endpoint and inserts movies into DB."""
        pytest.skip("not implemented — requires nightly_cache_job in services/cache.py")


    @pytest.mark.asyncio
    async def test_nightly_cache_job_skips_already_cached(async_client: AsyncClient):
        """CACHE-01: nightly_cache_job() skips movies where fetched_at IS NOT NULL AND genres IS NOT NULL."""
        pytest.skip("not implemented — requires nightly_cache_job in services/cache.py")


    @pytest.mark.asyncio
    async def test_lazy_enrich_populates_genres_and_runtime(async_client: AsyncClient):
        """CACHE-02: Movies with genres IS NULL get genres and runtime populated by _ensure_movie_details_in_db."""
        pytest.skip("not implemented — requires _ensure_movie_details_in_db importable from services/cache.py")
    ```

    Write `backend/tests/test_session_mgmt.py` with these failing stubs:

    ```python
    """Tests for session management endpoints (SESSION-01, SESSION-02)."""
    import pytest
    from httpx import AsyncClient


    @pytest.mark.asyncio
    async def test_delete_last_step_removes_step_and_reverts_movie(async_client: AsyncClient):
        """SESSION-01: DELETE /game/sessions/{id}/steps/last removes highest step_order step
        and reverts current_movie_tmdb_id to the previous step's movie_tmdb_id."""
        pytest.skip("not implemented — requires DELETE /game/sessions/{id}/steps/last endpoint")


    @pytest.mark.asyncio
    async def test_delete_last_step_blocked_on_single_step_session(async_client: AsyncClient):
        """SESSION-01: DELETE /game/sessions/{id}/steps/last returns 400 when only 1 step remains."""
        pytest.skip("not implemented — single-step guard not implemented")


    @pytest.mark.asyncio
    async def test_delete_archived_session_removes_session_and_steps(async_client: AsyncClient):
        """SESSION-02: DELETE /game/sessions/{id} permanently removes an archived session and all its steps."""
        pytest.skip("not implemented — requires DELETE /game/sessions/{id} endpoint")


    @pytest.mark.asyncio
    async def test_delete_archived_session_blocked_on_active_session(async_client: AsyncClient):
        """SESSION-02: DELETE /game/sessions/{id} returns 403 when session status is not 'archived'."""
        pytest.skip("not implemented — status guard not implemented")
    ```

    Run `cd /Users/Oreo/Projects/backend && python -m pytest tests/test_cache.py tests/test_session_mgmt.py -v 2>&1 | head -30` to confirm stubs are collected and skip (not error).
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && python -m pytest tests/test_cache.py tests/test_session_mgmt.py -v --co -q 2>&1 | grep -E "test_nightly|test_lazy|test_delete" | wc -l</automated>
  </verify>
  <done>7 test functions collected across the two stub files; running tests shows all skipped (not erroring); `wc -l` above returns 7 or more.</done>
</task>

</tasks>

<verification>
All foundations in place for Phase 4 execution:
- `grep "apscheduler" backend/requirements.txt` returns a match
- `grep "react-dialog" frontend/package.json` returns a match
- `grep "react-dropdown-menu" frontend/package.json` returns a match
- `ls frontend/src/components/ui/dialog.tsx frontend/src/components/ui/dropdown-menu.tsx` both exist
- `cd backend && python -m pytest tests/test_cache.py tests/test_session_mgmt.py -v` — all 7 tests collected and skipped
</verification>

<success_criteria>
- apscheduler>=3.10.4,<4.0 in requirements.txt
- @radix-ui/react-dialog and @radix-ui/react-dropdown-menu in frontend/package.json
- dialog.tsx exports all 9 named components; no TypeScript errors
- dropdown-menu.tsx exports DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel; no TypeScript errors
- test_cache.py: 3 skip stubs collected
- test_session_mgmt.py: 4 skip stubs collected
</success_criteria>

<output>
After completion, create `.planning/phases/04-caching-ui-ux-polish-and-session-management/04-01-SUMMARY.md`
</output>
