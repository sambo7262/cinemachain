---
phase: 05-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_game.py
autonomous: true
requirements:
  - BUG-1
  - BUG-3
  - BUG-4
  - ENH-1

must_haves:
  truths:
    - "Running pytest test_game.py exits non-zero (stubs are RED — asyncpg-skip applied, but stubs exist)"
    - "Six new test function names exist in test_game.py: test_bug1_auto_actor_single, test_bug1_disambiguation_multiple, test_bug3_eligibility_scoped_to_current_movie, test_bug4_csv_actor_name_canonical, test_bug4_csv_roundtrip, test_enh1_actor_precache_triggered"
  artifacts:
    - path: "backend/tests/test_game.py"
      provides: "Wave 0 test stubs for all backend bugs"
      contains: "test_bug1_auto_actor_single"
  key_links:
    - from: "backend/tests/test_game.py"
      to: "backend/app/routers/game.py"
      via: "FastAPI TestClient in conftest.py"
      pattern: "client\\.post.*pick-actor|request-movie"
---

<objective>
Append Wave 0 test stubs for BUG-1, BUG-3, BUG-4, and ENH-1 to the existing test_game.py.

Purpose: Establish the RED phase for all backend bugs before implementation. Stubs follow the existing asyncpg-skip pattern (skip locally, run green in Docker after Wave 1 fixes).
Output: Six new test functions in test_game.py that are SKIP locally and will be RED in Docker until Wave 1 implements the fixes.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bug-fixes/05-CONTEXT.md
@.planning/phases/05-bug-fixes/05-RESEARCH.md
@.planning/phases/05-bug-fixes/05-VALIDATION.md

<interfaces>
<!-- asyncpg-skip pattern from test_game.py (must replicate exactly): -->
```python
@pytest.mark.asyncio
async def test_<name>(client):
    """<description>"""
    try:
        import asyncpg  # noqa: F401
    except ImportError:
        pytest.skip("asyncpg not installed locally — runs in Docker")

    from app.models import GameSession, GameSessionStep, Movie, Actor, Credit, WatchEvent
    from sqlalchemy.ext.asyncio import AsyncSession
    from app.db import engine
    # ... DB setup ...
    async with AsyncSession(engine) as db:
        # seed data
        await db.commit()
    # ... assertions ...
```

<!-- pick_actor endpoint signature (for ENH-1 stub): -->
POST /game/sessions/{session_id}/pick-actor
Body: {"actor_tmdb_id": int, "actor_name": str}
Returns: GameSessionResponse

<!-- request_movie endpoint signature (for BUG-1 stub): -->
POST /game/sessions/{session_id}/request-movie
Body: {"movie_tmdb_id": int, "movie_title": str | None}
Returns: {"status": str, "session": GameSessionResponse}
Current status values: "already_in_radarr", "queued", "not_found_in_radarr", "error"
BUG-1 adds: "disambiguation_required" + "candidates" list

<!-- get_eligible_actors endpoint (for BUG-3 stub): -->
GET /game/sessions/{session_id}/eligible-actors?include_ineligible=false
Returns: list of {tmdb_id, name, profile_path, character}

<!-- import_csv_session endpoint (for BUG-4 stub): -->
POST /game/sessions/import-csv
Body: {"rows": [{"movieName": str, "actorName": str, "order": int}], "name": str}
Returns 201: {id, status, steps: [...]}
Actor steps have: {actor_tmdb_id, actor_name, ...}

<!-- export_session_csv endpoint (for BUG-4 round-trip stub): -->
GET /game/sessions/{session_id}/export-csv
Returns: text/csv with columns: movie_name, actor_name (rows interleaved)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append BUG-1 test stubs (auto-actor single + disambiguation multiple)</name>
  <files>backend/tests/test_game.py</files>
  <read_first>
    - backend/tests/test_game.py (full file — understand existing stub structure, asyncpg-skip pattern, conftest fixtures)
    - backend/app/routers/game.py (lines 1580-1665 — request_movie endpoint current implementation)
  </read_first>
  <behavior>
    - test_bug1_auto_actor_single: when a movie is picked without an explicit actor, and exactly one eligible actor connects previous_movie to selected_movie, request-movie must auto-create the actor step (response status != "disambiguation_required"; response session.steps includes an actor step with actor_tmdb_id set)
    - test_bug1_disambiguation_multiple: when a movie is picked without an explicit actor, and multiple eligible actors connect previous_movie to selected_movie, request-movie must return status="disambiguation_required" with a non-empty "candidates" list of {tmdb_id, name} dicts
  </behavior>
  <action>
Append two test functions to the END of backend/tests/test_game.py following the exact asyncpg-skip pattern.

test_bug1_auto_actor_single:
- Seed: one movie (tmdb_id=91001, "Movie A"), one movie (tmdb_id=91002, "Movie B"), one actor (tmdb_id=91001, "Actor Solo") who appears in BOTH movies via Credit rows, one GameSession with current_movie_tmdb_id=91001 current_movie_watched=True, WatchEvent for tmdb_id=91001, one GameSessionStep for movie 91001 (actor_tmdb_id=None, step_order=0)
- Call POST /game/sessions/{sid}/request-movie with {movie_tmdb_id: 91002, movie_title: "Movie B"} WITHOUT first calling pick-actor
- Assert: response.status_code == 200
- Assert: resp.json()["status"] != "disambiguation_required" (auto-resolved)
- Assert: any step in resp.json()["session"]["steps"] has actor_tmdb_id == 91001

test_bug1_disambiguation_multiple:
- Seed: Movie A (tmdb_id=92001), Movie B (tmdb_id=92002), Actor X (tmdb_id=92001, "Actor X") in both movies, Actor Y (tmdb_id=92002, "Actor Y") in both movies, GameSession current_movie_tmdb_id=92001 current_movie_watched=True, WatchEvent for 92001, step for movie 92001 (step_order=0)
- Call POST /game/sessions/{sid}/request-movie with {movie_tmdb_id: 92002, movie_title: "Movie B"} WITHOUT first calling pick-actor
- Assert: response.status_code == 200
- Assert: resp.json()["status"] == "disambiguation_required"
- Assert: len(resp.json()["candidates"]) >= 2

Both stubs use asyncpg-skip. Both are appended after the last function in the file.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && python -m pytest tests/test_game.py -x -q -k "test_bug1" 2>&1 | tail -5</automated>
  </verify>
  <done>Two test functions exist in test_game.py. Locally they either skip (asyncpg absent) or fail with NotImplementedError/AssertionError (asyncpg present but fix not yet implemented). No syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append BUG-3, BUG-4, ENH-1 test stubs</name>
  <files>backend/tests/test_game.py</files>
  <read_first>
    - backend/tests/test_game.py (end of file — see current last line number after Task 1)
    - backend/app/routers/game.py (lines 621-700 — _resolve_actor_tmdb_id and CSV import section)
    - backend/app/routers/game.py (lines 1527-1580 — pick_actor endpoint)
  </read_first>
  <behavior>
    - test_bug3_eligibility_scoped_to_current_movie: eligible actors for a session must be ONLY actors from the current (last watched) movie — actors from previous chain movies must NOT appear
    - test_bug4_csv_actor_name_canonical: after CSV import, actor steps must store the canonical TMDB name (from TMDB search result), not the raw CSV string when they differ
    - test_bug4_csv_roundtrip: export then re-import a session produces the same number of steps with no duplicates and no blank actor names
    - test_enh1_actor_precache_triggered: calling pick-actor triggers a background pre-fetch of the selected actor's filmography credits
  </behavior>
  <action>
Append four test functions to backend/tests/test_game.py. All use asyncpg-skip pattern.

test_bug3_eligibility_scoped_to_current_movie:
- Seed: Movie A (tmdb_id=93001), Movie B (tmdb_id=93002), Actor In A Only (tmdb_id=93001) with Credit to Movie A only, Actor In B Only (tmdb_id=93002) with Credit to Movie B only, GameSession with steps=[step for Movie A (step_order=0, actor_tmdb_id=None), actor step for actor 93001 (step_order=1, actor_tmdb_id=93001), step for Movie B (step_order=2, actor_tmdb_id=None)], current_movie_tmdb_id=93002, current_movie_watched=True, WatchEvent for 93002
- GET /game/sessions/{sid}/eligible-actors
- Assert: all returned actors have tmdb_id that appears in Credits for movie 93002 (not movie 93001)
- Assert: tmdb_id 93001 (Actor In A Only) is NOT in the returned actor list

test_bug4_csv_actor_name_canonical:
- Mock _resolve_actor_tmdb_id to return (99001, "Canonical Name From TMDB") for input "csv raw name"
- POST /game/sessions/import-csv with rows=[{movieName: "Inception", actorName: "csv raw name", order: 0}] and a pre-seeded Movie(tmdb_id=27205, title="Inception") already in DB plus a high-confidence mock for _resolve_movie_tmdb_id
- Assert: response.status_code == 201
- Find the actor step in response["steps"] where actor_tmdb_id == 99001
- Assert: that step's actor_name == "Canonical Name From TMDB" (NOT "csv raw name")

test_bug4_csv_roundtrip:
- Create a real session with 2 movie steps + 1 actor step, GET export-csv, parse the CSV, POST re-import
- Assert: no duplicate movie entries in the imported session's steps
- Assert: no blank actor_name in any actor step that has actor_tmdb_id set

test_enh1_actor_precache_triggered:
- With patch("app.routers.game.background_tasks.add_task") or inspect BackgroundTasks call: POST /game/sessions/{sid}/pick-actor should trigger a background task that calls _prefetch_actor_credits_background
- Alternative approach: verify the actor's Credits exist in DB after pick-actor completes (end-to-end)
- Use a simpler approach: patch _prefetch_actor_credits_background and assert it was called after pick-actor

Note for test_bug4_csv_actor_name_canonical: since TMDB calls are live in test, use unittest.mock.patch to mock _resolve_actor_tmdb_id at the game module level. The mock returns a tuple (99001, "Canonical Name From TMDB"). Import the mock inside the try/except asyncpg block.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && python -m pytest tests/test_game.py -x -q -k "test_bug3 or test_bug4 or test_enh1" 2>&1 | tail -5</automated>
  </verify>
  <done>Four additional test functions exist in test_game.py. All locally skip (asyncpg absent) or fail with expected assertion errors. Running the full test_game.py shows no syntax errors: `python -m pytest tests/test_game.py --collect-only` exits 0.</done>
</task>

</tasks>

<verification>
cd /Users/Oreo/Projects/backend && python -m pytest tests/test_game.py --collect-only -q 2>&1 | grep -E "test_bug|test_enh|error" | head -20

Expected: 6 new test names collected, no collection errors.
</verification>

<success_criteria>
- test_game.py contains exactly these 6 new function names: test_bug1_auto_actor_single, test_bug1_disambiguation_multiple, test_bug3_eligibility_scoped_to_current_movie, test_bug4_csv_actor_name_canonical, test_bug4_csv_roundtrip, test_enh1_actor_precache_triggered
- `python -m pytest tests/test_game.py --collect-only` exits 0 (no syntax/collection errors)
- Stubs follow asyncpg-skip pattern (skip locally)
- No existing passing tests are broken: `python -m pytest tests/test_game.py -x -q` exits with skip/pass for existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes/05-01-SUMMARY.md`
</output>
