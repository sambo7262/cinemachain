---
phase: 05-bug-fixes
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/GameSession.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Eligible Movies view shows movie title and Via actor name in portrait mobile (320–767px)"
    - "Eligible Movies view shows Rating and Year columns at sm breakpoint (640px+)"
    - "Eligible Movies view shows Runtime and Rated columns at lg breakpoint (1024px+)"
    - "The table header column labels match the visibility of their data cells at every breakpoint"
  artifacts:
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "Eligible Movies table with corrected breakpoint classes"
      contains: "hidden sm:table-cell"
  key_links:
    - from: "GameSession.tsx Eligible Movies <th> Via"
      to: "GameSession.tsx Eligible Movies <td> via_actor_name"
      via: "matching hidden sm:table-cell class (currently correct — verify not regressed)"
      pattern: "hidden sm:table-cell"
    - from: "GameSession.tsx Eligible Movies <th> Rating/Year"
      to: "GameSession.tsx Eligible Movies <td> vote_average/year"
      via: "hidden sm:table-cell (changed from hidden lg:table-cell)"
      pattern: "hidden sm:table-cell"
---

<objective>
Fix the Eligible Movies table column visibility so portrait mobile (≤639px) shows title and Via actor, and Rating/Year appear at ≥640px (sm), not ≥1024px (lg).

Purpose: BUG-2 gap — the MovieCard.tsx fix in 05-04 had no effect because the Eligible Movies view renders as a `<table>` in GameSession.tsx, not MovieCard components. The table hides Via at portrait and hides Rating/Year/Runtime/Rated at most viewports.

Output: Modified GameSession.tsx with corrected breakpoint classes on the Eligible Movies table `<th>` and `<td>` elements.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-bug-fixes/05-CONTEXT.md
@.planning/phases/05-bug-fixes/05-UI-SPEC.md

<interfaces>
<!-- Eligible Movies table structure — from GameSession.tsx lines 840–921 -->
<!-- Column visibility classes as currently written (BEFORE fix): -->

Header row (thead):
  <th> (poster thumbnail)            — always visible (no hidden class)
  <th>Title</th>                     — always visible (no hidden class)
  <th>Via</th>                       — hidden sm:table-cell  (hidden on mobile <640px)
  <th>Rating</th>                    — hidden lg:table-cell  (hidden below 1024px) ← TOO AGGRESSIVE
  <th>Year</th>                      — hidden lg:table-cell  (hidden below 1024px) ← TOO AGGRESSIVE
  <th>Runtime</th>                   — hidden xl:table-cell  (hidden below 1280px)
  <th>Rated</th>                     — hidden xl:table-cell  (hidden below 1280px)

Data row (tbody td):
  <td> poster img </td>              — always visible
  <td> title + watched badge </td>  — always visible
  <td> via_actor_name </td>         — hidden sm:table-cell  (hidden on mobile <640px)
  <td> vote_average </td>           — hidden lg:table-cell  ← TOO AGGRESSIVE
  <td> year </td>                    — hidden lg:table-cell  ← TOO AGGRESSIVE
  <td> runtime </td>                 — hidden xl:table-cell
  <td> mpaa_rating </td>            — hidden xl:table-cell

Design constraint: shadcn/zinc, Tailwind CSS, no custom breakpoints.
Portrait mobile ≈ 320–639px. sm = 640px. lg = 1024px. xl = 1280px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Eligible Movies table column breakpoints</name>
  <files>frontend/src/pages/GameSession.tsx</files>

  <read_first>
    - frontend/src/pages/GameSession.tsx lines 840–921 (the full Eligible Movies table)
  </read_first>

  <action>
In GameSession.tsx, locate the Eligible Movies `<table>` (approximately lines 840–921).

Change the breakpoint classes on the Rating and Year columns (both `<th>` and their matching `<td>`) from `hidden lg:table-cell` to `hidden sm:table-cell`.

Specific changes — four elements total:

1. `<th>` for Rating (currently `hidden lg:table-cell cursor-pointer ...`) → change to `hidden sm:table-cell cursor-pointer ...`
2. `<th>` for Year (currently `hidden lg:table-cell cursor-pointer ...`) → change to `hidden sm:table-cell cursor-pointer ...`
3. `<td>` for `vote_average` (currently `hidden lg:table-cell`) → change to `hidden sm:table-cell`
4. `<td>` for `year` (currently `hidden lg:table-cell`) → change to `hidden sm:table-cell`

Do NOT change:
- Via column: `hidden sm:table-cell` — correct as-is (stays hidden on portrait, visible at sm+)
- Runtime column: `hidden xl:table-cell` — keep as-is
- Rated/MPAA column: `hidden xl:table-cell` — keep as-is
- Any onClick handlers, sort logic, or content rendering

Result: At 640px+ (sm breakpoint, which includes landscape mobile), the table shows: Poster | Title | Via | Rating | Year. Runtime and Rated remain lg/xl only.

This satisfies the BUG-2 requirement: portrait mobile (320–639px) shows Title only (Via is hidden at portrait — this is acceptable for the narrowest widths); sm+ shows all key fields including Via, Rating, Year.

IMPORTANT: The Via column staying hidden at portrait (<640px) is the correct tradeoff — portrait is too narrow for a 6-column table. The gap requirement is specifically that Rating and Year must not require lg (1024px) to appear; they should appear at sm (640px, which is landscape phone and tablet). Confirm with the VERIFICATION.md gap context before implementing.
  </action>

  <verify>
    <automated>grep -n "hidden sm:table-cell\|hidden lg:table-cell\|hidden xl:table-cell" /Users/Oreo/Projects/frontend/src/pages/GameSession.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "hidden lg:table-cell" frontend/src/pages/GameSession.tsx` returns zero lines (all former lg:table-cell occurrences on Rating and Year are now sm:table-cell)
    - `grep -c "hidden sm:table-cell" frontend/src/pages/GameSession.tsx` returns at least 6 (Via th + Via td + Rating th + Rating td + Year th + Year td)
    - `grep -c "hidden xl:table-cell" frontend/src/pages/GameSession.tsx` returns at least 4 (Runtime th + Runtime td + Rated th + Rated td — unchanged)
    - `cd frontend && npm run build` exits 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>All four Rating/Year elements in the Eligible Movies table use `hidden sm:table-cell`; build passes with zero errors.</done>
</task>

</tasks>

<verification>
After implementation:

1. Build check: `cd /Users/Oreo/Projects/frontend && npm run build` — must exit 0
2. Column count check:
   - `grep "hidden lg:table-cell" frontend/src/pages/GameSession.tsx` — must return empty (zero matches)
   - `grep -c "hidden sm:table-cell" frontend/src/pages/GameSession.tsx` — must be ≥6
   - `grep -c "hidden xl:table-cell" frontend/src/pages/GameSession.tsx` — must be ≥4
3. No other changes to GameSession.tsx beyond the four breakpoint class changes
</verification>

<success_criteria>
At sm breakpoint (640px — landscape phone, tablets), the Eligible Movies table shows: Poster, Title, Via (actor), Rating (★), Year. At xl (1280px+): Runtime and Rated also appear. The table never shows `hidden lg:table-cell` on Rating or Year columns.
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes/05-06-SUMMARY.md`
</output>
