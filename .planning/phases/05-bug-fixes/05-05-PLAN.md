---
phase: 05-bug-fixes
plan: 05
type: execute
wave: 5
depends_on:
  - "05-04"
files_modified: []
autonomous: false
requirements:
  - BUG-1
  - BUG-2
  - BUG-3
  - BUG-4
  - ENH-1

must_haves:
  truths:
    - "Docker images rebuilt and deployed to NAS with Phase 5 changes"
    - "BUG-1: selecting a movie without prior actor pick triggers auto-record or disambiguation dialog"
    - "BUG-2: MovieCard shows all required fields in portrait mode; home page buttons align on mobile"
    - "BUG-3: eligible actors shown are only from the current (last watched) movie's cast"
    - "BUG-4: CSV import stores canonical TMDB names; round-trip produces no duplicate rows"
    - "ENH-1: actor selection loads eligible movies faster than before"
  artifacts:
    - path: "docker-compose.yml or compose.yaml"
      provides: "Running container stack on NAS"
      contains: "cinemachain"
  key_links:
    - from: "NAS Docker stack"
      to: "Phase 5 game.py"
      via: "Docker image build"
      pattern: "sambo7262/cinemachain-backend"
---

<objective>
Docker rebuild, NAS deploy, and human verification of all Phase 5 bug fixes.

Purpose: Confirm all 5 items (BUG-1, BUG-2, BUG-3, BUG-4, ENH-1) are fixed in production on the NAS.
Output: Verified fixes running on live NAS; Phase 5 declared complete.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bug-fixes/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker rebuild and NAS deploy</name>
  <files></files>
  <read_first>
    - Makefile (look for rebuild target — established in plan 03-11 as `make rebuild`)
    - compose.yaml or docker-compose.yml (confirm service names and image tags)
  </read_first>
  <action>
Run Docker rebuild using the established Makefile target:
  cd /Users/Oreo/Projects && make rebuild

This rebuilds and pushes images tagged sambo7262/cinemachain-backend:latest and sambo7262/cinemachain-frontend:latest.

Then deploy to NAS via SSH:
  ssh nas "cd /path/to/cinemachain && docker compose pull && docker compose up -d"

(Use the NAS SSH path established in prior phases — check STATE.md or previous SUMMARY.md for the exact NAS deploy path if uncertain.)

Verify the stack is running:
  ssh nas "docker compose ps"

All containers should show status: Up.
  </action>
  <verify>
    <automated>ssh nas "docker compose -f /volume1/docker/cinemachain/compose.yaml ps" 2>&1 | grep -E "Up|running" | wc -l</automated>
  </verify>
  <done>All containers show "Up" status. Backend responds: `curl -s http://nas-ip:8000/health` returns {"status":"ok"}.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify all Phase 5 fixes on NAS</name>
  <what-built>
All Phase 5 fixes: BUG-1 (actor auto-resolve/disambiguation), BUG-2 (mobile layout), BUG-3 (eligibility scoping), BUG-4 (CSV canonical names), ENH-1 (actor pre-cache). Docker images rebuilt and deployed to NAS.
  </what-built>
  <action>Verify all 5 Phase 5 items on live NAS per the how-to-verify steps below.</action>
  <how-to-verify>
Test each item on the live NAS:

**BUG-3: Eligible actors scoped to current movie (test this first — simpler)**
1. Start a session: The Matrix (tmdb_id=603) → Carrie-Anne Moss → Chocolat
2. On the Eligible Actors tab for Chocolat, verify Keanu Reeves (from The Matrix) does NOT appear
3. Only actors from Chocolat's cast should be listed

**BUG-1: Chain actor auto-record**
4. In the same session: select a movie from Eligible Movies WITHOUT first clicking on an actor
5. Expected (1 shared actor): Actor step is auto-recorded — chain history shows the actor name in the VIA column
6. Expected (multiple shared actors): A "Who are you following?" dialog appears listing the candidates

**BUG-2: Mobile layout**
7. Open the app on a phone (or browser DevTools at 375px width, portrait)
8. Navigate to Eligible Movies tab — confirm MovieCard shows: title, via actor, rating, runtime, MPAA badge
9. Navigate to home page — confirm Archive/Continue buttons wrap on narrow screen; "+ Start a new session" button fills width

**BUG-4: CSV round-trip**
10. Export an existing session as CSV (from the session DropdownMenu → Export CSV)
11. Re-import that CSV using "Import a chain"
12. Verify: imported session has same number of steps as original, no duplicate rows, actor names match (not blank or misspelled)

**ENH-1: Actor selection speed**
13. On an active session (current movie watched), click an actor from Eligible Actors
14. Observe: Eligible Movies tab loads faster than before (subjective — <2 seconds is the target)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 items pass. Or describe which items failed with observed behavior.</resume-signal>
</task>

</tasks>

<verification>
Human verification covers all 5 items. Automated: NAS containers up.
</verification>

<success_criteria>
- All 5 items verified PASS on live NAS by user
- Chain history shows actor names for all movie picks (no blank VIA columns for auto-resolved picks)
- Mobile portrait mode shows all required MovieCard fields without truncation
- CSV round-trip produces no duplicates and no blank actor names
- Actor selection loads eligible movies noticeably faster
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes/05-05-SUMMARY.md`
</output>
