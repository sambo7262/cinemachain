---
phase: 020-now-playing-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/GameSession.tsx
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/pages/WatchHistoryPage.tsx
autonomous: true
requirements: [POLISH-02]

must_haves:
  truths:
    - "App.tsx content wrapper uses px-4 on mobile and px-6 on sm+, matching NavBar exactly"
    - "No page has redundant horizontal padding nested inside the App.tsx wrapper"
    - "GameSession sub-header retains its own px-4 sm:px-6 padding (exception)"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Content wrapper with px-4 sm:px-6"
      contains: "px-4 sm:px-6"
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "Main content area without redundant horizontal padding"
    - path: "frontend/src/pages/SearchPage.tsx"
      provides: "Search page without redundant horizontal padding"
    - path: "frontend/src/pages/WatchHistoryPage.tsx"
      provides: "Watch history page without redundant horizontal padding"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/NavBar.tsx"
      via: "Matching px-4 sm:px-6 padding values"
      pattern: "px-4 sm:px-6"
---

<objective>
Align content padding across the entire app so the content left/right edges match the NavBar logo left-edge and settings icon right-edge on all viewports. Fix the double-padding problem where App.tsx and individual pages both apply horizontal padding.

Purpose: On mobile portrait (~375px), double padding wastes ~21% of screen width. Fixing App.tsx to match NavBar and stripping redundant page-level horizontal padding reclaims that space.
Output: All content aligns to NavBar edges. No page has redundant horizontal padding.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/020-now-playing-polish/20-CONTEXT.md

<interfaces>
From frontend/src/App.tsx line 41:
  `<div className="max-w-[1400px] mx-auto px-6 w-full flex-1">`
  Change to: `<div className="max-w-[1400px] mx-auto px-4 sm:px-6 w-full flex-1">`

From frontend/src/components/NavBar.tsx line 14:
  `<div className="max-w-[1400px] mx-auto px-4 sm:px-6 h-14 flex items-center justify-between">`
  (This is the target alignment — do NOT change NavBar)

From frontend/src/pages/GameSession.tsx line 599:
  `<div className="flex-1 flex flex-col gap-4 px-4 sm:px-6 py-4 w-full">`
  Change to: `<div className="flex-1 flex flex-col gap-4 py-4 w-full">`
  (Remove px-4 sm:px-6, keep py-4)

From frontend/src/pages/GameSession.tsx line 538:
  `<header className="border-b border-border bg-background px-4 sm:px-6 py-3 ...">`
  DO NOT CHANGE — user confirmed this sub-header keeps its own inset

From frontend/src/pages/SearchPage.tsx line 245:
  `<div className="px-4 py-4 sm:px-6 sm:py-6">`
  Change to: `<div className="py-4 sm:py-6">`
  (Remove px-4 and sm:px-6, keep py-4 and sm:py-6)

From frontend/src/pages/WatchHistoryPage.tsx line 137:
  `<div className="px-4 py-4 sm:px-6 sm:py-6 space-y-4">`
  Change to: `<div className="py-4 sm:py-6 space-y-4">`
  (Remove px-4 and sm:px-6, keep py-4 sm:py-6 and space-y-4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix App.tsx content wrapper padding to match NavBar</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (full file — line 41 content wrapper)
    - frontend/src/components/NavBar.tsx (line 14 — target padding reference)
  </read_first>
  <action>
On line 41 of App.tsx, change:
```
<div className="max-w-[1400px] mx-auto px-6 w-full flex-1">
```
to:
```
<div className="max-w-[1400px] mx-auto px-4 sm:px-6 w-full flex-1">
```

This changes mobile padding from 24px (px-6) to 16px (px-4), matching NavBar's `px-4 sm:px-6`. Desktop (sm+) remains 24px.

No other changes to App.tsx.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain && grep -n "px-4 sm:px-6" frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/App.tsx contains `px-4 sm:px-6` in the max-w-[1400px] div
    - frontend/src/App.tsx does NOT contain a standalone `px-6` without `sm:` prefix in the content wrapper
  </acceptance_criteria>
  <done>App.tsx content wrapper padding matches NavBar: px-4 on mobile, px-6 on sm+</done>
</task>

<task type="auto">
  <name>Task 2: Strip redundant horizontal padding from page components</name>
  <files>frontend/src/pages/GameSession.tsx, frontend/src/pages/SearchPage.tsx, frontend/src/pages/WatchHistoryPage.tsx</files>
  <read_first>
    - frontend/src/pages/GameSession.tsx (line 599 — main content wrapper; line 538 — sub-header DO NOT TOUCH)
    - frontend/src/pages/SearchPage.tsx (line 245 — outer wrapper)
    - frontend/src/pages/WatchHistoryPage.tsx (line 137 — outer wrapper)
  </read_first>
  <action>
**GameSession.tsx** — line 599 (approximately):
Change:
```
<div className="flex-1 flex flex-col gap-4 px-4 sm:px-6 py-4 w-full">
```
to:
```
<div className="flex-1 flex flex-col gap-4 py-4 w-full">
```
Remove `px-4 sm:px-6`. Keep `py-4`. Do NOT touch the sub-header on line 538 (`border-b border-border bg-background px-4 sm:px-6 py-3`) per user decision D-3.

**SearchPage.tsx** — line 245 (approximately):
Change:
```
<div className="px-4 py-4 sm:px-6 sm:py-6">
```
to:
```
<div className="py-4 sm:py-6">
```
Remove `px-4` and `sm:px-6`. Keep `py-4` and `sm:py-6`.

**WatchHistoryPage.tsx** — line 137 (approximately):
Change:
```
<div className="px-4 py-4 sm:px-6 sm:py-6 space-y-4">
```
to:
```
<div className="py-4 sm:py-6 space-y-4">
```
Remove `px-4` and `sm:px-6`. Keep `py-4`, `sm:py-6`, and `space-y-4`.

**Scan for other pages:** Check GameLobby.tsx for any `px-4` or `px-6` horizontal padding that would double up. GameLobby currently has none (verified), so no change needed.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain/frontend && npx tsc --noEmit 2>&1 | head -5 && echo "---" && grep -n "px-4\|px-6" src/pages/GameSession.tsx src/pages/SearchPage.tsx src/pages/WatchHistoryPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/GameSession.tsx line ~599 content div contains `py-4` but NOT `px-4` or `px-6`
    - frontend/src/pages/GameSession.tsx sub-header line ~538 still contains `px-4 sm:px-6` (unchanged)
    - frontend/src/pages/SearchPage.tsx outer div contains `py-4 sm:py-6` but NOT `px-4` or `sm:px-6`
    - frontend/src/pages/WatchHistoryPage.tsx outer div contains `py-4 sm:py-6 space-y-4` but NOT `px-4` or `sm:px-6`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All page-level redundant horizontal padding removed. Content edges align with NavBar on all viewports. GameSession sub-header retains its own padding.</done>
</task>

</tasks>

<verification>
1. On mobile (375px viewport), content left-edge aligns with NavBar CinemaChain icon; no double-inset visible.
2. On desktop (1400px+), content still constrained to max-w-[1400px] with proper padding.
3. GameSession sub-header ("Now playing: ...") retains its distinct inset from the content area below.
4. SearchPage, WatchHistoryPage, GameLobby all align identically to NavBar edges.
5. `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- App.tsx wrapper uses `px-4 sm:px-6` matching NavBar
- No page component has redundant horizontal padding inside the App.tsx wrapper
- GameSession sub-header exception is preserved
- All pages visually align to NavBar edges on mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/020-now-playing-polish/20-02-SUMMARY.md`
</output>
