---
phase: 19-v2-bug-fixes-polish
plan: 06
type: execute
wave: 6
depends_on: [19-01, 19-02, 19-03, 19-04, 19-05]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [v2BUG-01]

must_haves:
  truths:
    - "All implemented requirements are checked off in REQUIREMENTS.md"
    - "MDBSYNC-01/02 marked as superseded with rationale"
    - "IMDB-01 marked as partially done with rationale"
    - "No regressions in existing v2 features after all Phase 19 changes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes"
  key_links: []
---

<objective>
Update REQUIREMENTS.md to mark implemented requirements as done, mark superseded requirements, and verify the full phase via human checkpoint.

Purpose: Per D-26 (mark done requirements), D-27 (MDBSYNC superseded), D-28 (IMDB-01 partial). This is housekeeping to keep the requirements checklist accurate.
Output: Updated REQUIREMENTS.md, human verification of entire phase
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-v2-bug-fixes-polish/19-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md checkboxes and notes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
**D-26: Mark implemented requirements as done:**

Change the following lines from `- [ ]` to `- [x]`:
- `NAV-01` (line ~23): `- [x] **NAV-01**: Top nav has three permanent items: Game Mode, Query Mode, Settings`
- `NAV-02` (line ~24): `- [x] **NAV-02**: Game Mode routes to session grid (existing lobby behaviour preserved)`
- `NAV-03` (line ~25): `- [x] **NAV-03**: Settings accessible globally from top nav at all times`
- `QMODE-06` (line ~35): `- [x] **QMODE-06**: User can request a movie from query results via Radarr`
- `SESS-01` (line ~38): `- [x] **SESS-01**: User can tag/save any movie in the eligible movies list during an active session`
- `SESS-02` (line ~39): `- [x] **SESS-02**: Saved movies persist for the duration of the session`
- `SESS-03` (line ~40): `- [x] **SESS-03**: Eligible movies list can be filtered to show only saved movies`
- `SESS-04` (line ~41): `- [x] **SESS-04**: User can shortlist 2-6 eligible movies; list filters to shortlisted items for comparison`
- `MDBLIST-02` (line ~47): `- [x] **MDBLIST-02**: IMDB ratings displayed alongside RT scores in eligible movies table, splash, and Now Playing tile`
- `SUGGEST-02` (line ~57): `- [x] **SUGGEST-02**: Suggested movies support the same actions as regular eligible movies (request, save, shortlist)`
- `WATCHED-01` (line ~62): `- [x] **WATCHED-01**: A Watched History nav item appears alongside Game Mode and Query Mode`
- `WATCHED-02` (line ~63): `- [x] **WATCHED-02**: Watched History shows all movies marked watched across all sessions in a tile or grid layout (user-toggleable)`
- `WATCHED-03` (line ~64): `- [x] **WATCHED-03**: Watched History is searchable by title; all Phase 14 MDBList watched-sync code is removed`

**D-27: Mark MDBSYNC-01/02 as superseded:**

Change MDBSYNC-01 and MDBSYNC-02 lines to:
```
- [x] **MDBSYNC-01**: ~~Every movie marked as watched (game session or Query Mode) is synced to the user's MDBList watched list in real time~~ — **Superseded:** Phase 14 MDBList watched-sync was intentionally removed in Phase 16 (Watched History). First-party Watch History replaces MDBList sync.
- [x] **MDBSYNC-02**: ~~Bulk sync on demand from Settings — push full existing watch history to MDBList; MDBList list ID configurable in Settings~~ — **Superseded:** See MDBSYNC-01.
```

**D-28: Mark IMDB-01 partially done:**

Change IMDB-01 line to:
```
- [~] **IMDB-01**: Actor rows have `imdb_person_id` stored; ChainHistory actor links point to `imdb.com/name/{id}` instead of TMDB — **Partial:** Movie links point to IMDB (with TMDB fallback) per Phase 17. Actor IMDB links (`imdb_person_id`) explicitly deferred — backfill cost not justified.
```

Also update the v2BUG-01 placeholder line to indicate Phase 19 completion:
```
- [x] **v2BUG-01**: Bugs logged during v2 development (Phases 13-18) resolved: rating dialog on mark-as-watched, mobile UI fixes, filter/search bugs, NR filter, session menu reorder, atomic delete-last-step, badge tooltips, sort defaults, UI polish
```
  </action>
  <verify>
    <automated>grep -c "\- \[x\]" .planning/REQUIREMENTS.md && grep "Superseded" .planning/REQUIREMENTS.md | head -2 && grep "Partial" .planning/REQUIREMENTS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md NAV-01, NAV-02, NAV-03 lines start with `- [x]`
    - REQUIREMENTS.md QMODE-06 line starts with `- [x]`
    - REQUIREMENTS.md SESS-01, SESS-02, SESS-03, SESS-04 lines start with `- [x]`
    - REQUIREMENTS.md MDBLIST-02 line starts with `- [x]`
    - REQUIREMENTS.md SUGGEST-02 line starts with `- [x]`
    - REQUIREMENTS.md WATCHED-01, WATCHED-02, WATCHED-03 lines start with `- [x]`
    - REQUIREMENTS.md MDBSYNC-01 contains "Superseded"
    - REQUIREMENTS.md MDBSYNC-02 contains "Superseded"
    - REQUIREMENTS.md IMDB-01 contains "Partial"
    - REQUIREMENTS.md v2BUG-01 line starts with `- [x]`
  </acceptance_criteria>
  <done>All implemented requirements checked off. MDBSYNC requirements marked superseded with rationale. IMDB-01 marked partial with rationale. v2BUG-01 marked complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 19: Rating dialog on mark-as-watched, mobile UI fixes (nav, badges, padding, session tiles, Watch History portrait), filter/search bugs (step-advance reset, cross-page search), NR filter toggle, sort defaults DESC, session menu reorder, atomic delete-last-step, save/shortlist button repositioning with optimistic UI, badge tooltips, session tile polish, requirements housekeeping.</what-built>
  <how-to-verify>
    1. **Rating Dialog:** In GameSession, mark a movie as watched — a slider dialog (1-10) should appear. Test Save and Skip. Repeat in SearchPage (mark watched from splash dialog). Check WatchHistoryPage splash uses the same slider component.
    2. **Mobile (320px viewport):** NavBar fits without horizontal scrolling. Eligible movies cards show no badge overflow. Pages have reduced left/right padding. Session tiles have tighter padding. Watch History mobile portrait shows poster + ratings (no genre).
    3. **Filter Reset:** In GameSession, set filters/search/sort, then advance a step — all should reset to defaults.
    4. **Cross-page Search:** Search eligible movies — results should include movies from ALL pages, not just current page. Same for sidebar genre/MPAA filters.
    5. **NR Filter:** Toggle "Hide NR" in eligible movies — NR-tagged movies disappear, movies with no MPAA rating remain visible.
    6. **Sort Defaults:** In GameSession, SearchPage, WatchHistoryPage — select any sort column for the first time. Direction should be DESC.
    7. **Session Menu:** Open session settings dropdown — order should be: Export CSV, Edit Session Name, Delete Last Step, Archive Session.
    8. **Delete Last Step:** On a session with at least 3 steps (movie-actor-movie), tap Delete Last Step. Both the movie AND actor step should be removed. Session should revert to the movie before the actor was picked.
    9. **Save/Shortlist Buttons:** Buttons are on the right side of movie cards (not overlaid on poster). Tapping updates immediately (no flicker/delay).
    10. **Badge Tooltips:** Hover over any rating badge (IMDB, RT, etc.) — tooltip should show the source name.
    11. **Session Tiles:** No step count text. Active tiles show a small poster thumbnail next to Continue button.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. All REQUIREMENTS.md checkboxes match actual implementation state
2. Human verifies all Phase 19 features work as expected
</verification>

<success_criteria>
- REQUIREMENTS.md accurately reflects implementation state
- All Phase 19 bugs and polish items verified working by human
- No regressions in existing v2 features
</success_criteria>

<output>
After completion, create `.planning/phases/19-v2-bug-fixes-polish/19-06-SUMMARY.md`
</output>
