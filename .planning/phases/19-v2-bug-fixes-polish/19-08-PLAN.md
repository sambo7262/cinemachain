---
phase: 19-v2-bug-fixes-polish
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/GameSession.tsx
  - frontend/src/components/MovieCard.tsx
autonomous: true
gap_closure: true
requirements: [v2BUG-01]

must_haves:
  truths:
    - "GameSession play space uses reduced horizontal padding on iPhone portrait so content fills the screen width"
    - "Movie selection tiles (MovieCard) are slightly taller on wider screens (sm+ breakpoint)"
  artifacts:
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "Reduced play space padding on mobile portrait"
    - path: "frontend/src/components/MovieCard.tsx"
      provides: "Taller poster images on wider screens"
  key_links:
    - from: "GameSession.tsx main content div"
      to: "Tailwind responsive padding classes"
      via: "px-2 sm:px-4 lg:px-6 (reduced from px-4 sm:px-6)"
      pattern: "px-2"
---

<objective>
Fix two layout issues: (1) reduce horizontal padding in the GameSession play space so iPhone portrait uses full screen width, and (2) make movie selection tiles slightly taller on wider screens.

Purpose: GAP-03 (excessive play space padding on mobile portrait) and GAP-04 (movie tiles too short on wider screens).
Output: Updated padding classes in GameSession.tsx and taller poster dimensions in MovieCard.tsx.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v2-bug-fixes-polish/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce play space horizontal padding on mobile (GAP-03)</name>
  <files>frontend/src/pages/GameSession.tsx</files>
  <read_first>frontend/src/pages/GameSession.tsx (lines 555, 616 — the header and main content wrapper padding classes)</read_first>
  <action>
Find the main content wrapper div at line 616:

```tsx
<div className="flex-1 flex flex-col gap-4 px-4 py-4 sm:px-6 max-w-5xl w-full mx-auto">
```

Change padding classes from `px-4 py-4 sm:px-6` to `px-2 py-4 sm:px-4 lg:px-6`:

```tsx
<div className="flex-1 flex flex-col gap-4 px-2 py-4 sm:px-4 lg:px-6 max-w-5xl w-full mx-auto">
```

This reduces mobile portrait padding from 16px (px-4) to 8px (px-2), gives tablets 16px (sm:px-4), and wider screens 24px (lg:px-6).

The header at line 555 (`px-4 sm:px-6`) is the global nav bar area -- per the UAT notes "global nav bar is fine", so leave the header padding unchanged.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain/frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
- grep -n "px-2 py-4 sm:px-4 lg:px-6" frontend/src/pages/GameSession.tsx returns exactly 1 match
- grep -n "px-4 py-4 sm:px-6" frontend/src/pages/GameSession.tsx returns 0 matches (old padding gone from main content wrapper)
- The header line (~555) still contains "px-4 sm:px-6" (unchanged)
  </acceptance_criteria>
  <done>Play space uses px-2 on mobile portrait (8px), px-4 on sm (16px), px-6 on lg (24px). Header nav padding unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Make movie selection tiles taller on wider screens (GAP-04)</name>
  <files>frontend/src/components/MovieCard.tsx</files>
  <read_first>frontend/src/components/MovieCard.tsx (full file — 103 lines)</read_first>
  <action>
In MovieCard.tsx, the poster image and placeholder both use `w-16 h-24` (64x96px). On wider screens (sm+), the tiles have more horizontal space but the poster stays small.

Change the poster dimensions to be responsive:

**Line 59 (img tag):**
From: `className="w-16 h-24 rounded object-cover flex-shrink-0"`
To: `className="w-16 h-24 sm:w-20 sm:h-[120px] rounded object-cover flex-shrink-0"`

**Line 64 (placeholder div):**
From: `className="w-16 h-24 rounded bg-muted flex-shrink-0"`
To: `className="w-16 h-24 sm:w-20 sm:h-[120px] rounded bg-muted flex-shrink-0"`

This increases the poster from 64x96px to 80x120px on sm+ breakpoints (640px+), making tiles visually taller on wider screens while keeping the compact size on mobile.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain/frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "sm:w-20 sm:h-\[120px\]" frontend/src/components/MovieCard.tsx returns exactly 2 (img + placeholder)
- grep -c "w-16 h-24" frontend/src/components/MovieCard.tsx returns exactly 2 (the base sizes still present)
  </acceptance_criteria>
  <done>MovieCard poster is 64x96px on mobile and 80x120px on sm+ screens, making tiles visually taller on wider viewports.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd frontend && npx tsc --noEmit`
- Dev server starts: `cd frontend && npm run dev`
- On mobile viewport: play space content extends closer to screen edges
- On wider viewport: movie tiles are noticeably taller with larger poster images
</verification>

<success_criteria>
- GAP-03: Play space horizontal padding reduced on mobile portrait (px-2 = 8px vs previous px-4 = 16px)
- GAP-04: Movie selection tiles use larger poster (80x120px) on sm+ screens
</success_criteria>

<output>
After completion, create `.planning/phases/19-v2-bug-fixes-polish/19-08-SUMMARY.md`
</output>
