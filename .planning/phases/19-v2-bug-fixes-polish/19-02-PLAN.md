---
phase: 19-v2-bug-fixes-polish
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - frontend/src/components/NavBar.tsx
  - frontend/src/pages/GameSession.tsx
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/pages/WatchHistoryPage.tsx
  - frontend/src/pages/GameLobby.tsx
  - frontend/src/components/RatingsBadge.tsx
autonomous: true
requirements: [v2BUG-01]

must_haves:
  truths:
    - "Nav does not cause horizontal scrolling on 320px mobile viewport"
    - "Ratings badges do not bleed off card edges on mobile"
    - "Global left/right page padding is reduced by ~20%"
    - "Session grid tiles have tighter padding with no text wrapping"
    - "Watch History mobile portrait shows poster + ratings (genre field dropped)"
  artifacts:
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Responsive nav with reduced font/padding at small breakpoints"
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "Reduced px-6 to px-4 on mobile, stacked badges on mobile"
    - path: "frontend/src/pages/GameLobby.tsx"
      provides: "Tighter session tile padding"
    - path: "frontend/src/pages/WatchHistoryPage.tsx"
      provides: "Mobile portrait: genre hidden, poster + ratings visible"
  key_links:
    - from: "frontend/src/components/NavBar.tsx"
      to: "all pages"
      via: "Shared nav component"
      pattern: "NavBar"
---

<objective>
Fix all mobile UI bugs: nav overflow, badge bleeding, global padding, session tile padding, and Watch History mobile portrait missing data.

Purpose: Per D-11 through D-15, mobile users experience horizontal scrolling, content overflow, and missing data. These fixes ensure the app is fully usable on 320px-768px viewports.
Output: Updated NavBar.tsx, GameSession.tsx, SearchPage.tsx, WatchHistoryPage.tsx, GameLobby.tsx
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-v2-bug-fixes-polish/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NavBar mobile overflow and global padding reduction</name>
  <files>
    frontend/src/components/NavBar.tsx
    frontend/src/pages/GameSession.tsx
    frontend/src/pages/SearchPage.tsx
    frontend/src/pages/WatchHistoryPage.tsx
    frontend/src/pages/GameLobby.tsx
  </files>
  <read_first>
    frontend/src/components/NavBar.tsx
    frontend/src/pages/GameSession.tsx
    frontend/src/pages/SearchPage.tsx
    frontend/src/pages/WatchHistoryPage.tsx
    frontend/src/pages/GameLobby.tsx
  </read_first>
  <action>
**NavBar.tsx (D-11):**

Current nav links use `px-3 py-1.5 text-sm` and the container uses `px-6`. On 320px, all items overflow.

Fix:
1. Change container `px-6` to `px-4 sm:px-6`
2. Change nav link `px-3 py-1.5 text-sm` to `px-2 py-1 text-xs sm:px-3 sm:py-1.5 sm:text-sm` on all three nav links (Game Mode, Search, Watch History)
3. Change `gap-1` on the nav links container to `gap-0.5 sm:gap-1`
4. The Settings icon link already has `min-w-[44px]` — keep as is for touch target
5. Consider hiding the "CinemaChain" wordmark on very small screens: add `hidden sm:flex` to the Logo link, and add a minimal icon-only Link on sm-down: `<Link to="/game" className="sm:hidden"><Film className="w-5 h-5" /></Link>`. This frees ~100px on mobile.

**Global padding reduction (D-13) — ~20% reduction:**

- GameSession.tsx line 487: change `px-6` to `px-4 sm:px-6` on header
- GameSession.tsx line 548: change `px-6 py-4` to `px-4 py-4 sm:px-6` on main content div
- SearchPage.tsx line 228: change `px-6 py-6` to `px-4 py-4 sm:px-6 sm:py-6`
- WatchHistoryPage.tsx: find the main container padding and reduce similarly — if using a wrapper div, change `px-6` to `px-4 sm:px-6`
- GameLobby.tsx: the lobby uses `max-w-2xl` — check for any `px-` on wrapper; reduce by same pattern
- NavBar.tsx: already changed above

**Session grid tile padding (D-14):**

In GameLobby.tsx, session tiles use `<CardContent className="flex items-center justify-between py-4 px-5">`. Change to `py-3 px-3 sm:px-5` to reduce inner padding. This prevents text wrapping in session titles on narrow screens.

**Watch History mobile portrait (D-15):**

In WatchHistoryPage.tsx, the list view table headers include a genre column. On mobile portrait, poster images and ratings are not shown.

Fix: In the table/list view:
1. Add `hidden sm:table-cell` to the genre `<th>` and corresponding `<td>` cells to hide genre on mobile portrait
2. Ensure poster column is NOT hidden on mobile — if it has `hidden sm:table-cell`, remove the `hidden` class
3. Ensure ratings column (`<RatingsBadge>`) is NOT hidden on mobile — if it has `hidden sm:table-cell`, remove the `hidden` class
4. For the tile view: ensure poster `<img>` is always rendered (no responsive hiding). If genre text takes space, hide genre with `hidden sm:block` on tiles too.
  </action>
  <verify>
    <automated>grep -n "px-2.*sm:px-3\|text-xs.*sm:text-sm" frontend/src/components/NavBar.tsx && grep -n "px-4.*sm:px-6" frontend/src/pages/GameSession.tsx frontend/src/pages/SearchPage.tsx | head -6</automated>
  </verify>
  <acceptance_criteria>
    - NavBar.tsx contains `text-xs sm:text-sm` on nav links
    - NavBar.tsx contains `px-2` or `px-2 py-1` on nav links (mobile size)
    - NavBar.tsx container has `px-4 sm:px-6` (not just `px-6`)
    - GameSession.tsx header has `px-4 sm:px-6` (not just `px-6`)
    - GameSession.tsx main content div has `px-4` at mobile breakpoint
    - SearchPage.tsx main container has `px-4` at mobile breakpoint
    - GameLobby.tsx CardContent has `px-3 sm:px-5` or similar reduced padding
    - WatchHistoryPage.tsx genre column has `hidden sm:table-cell`
  </acceptance_criteria>
  <done>NavBar fits 320px without horizontal scroll. All pages use reduced mobile padding. Session tiles have tighter padding. Watch History mobile portrait shows poster and ratings with genre hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ratings badge overflow on mobile card views</name>
  <files>
    frontend/src/pages/GameSession.tsx
    frontend/src/components/RatingsBadge.tsx
  </files>
  <read_first>
    frontend/src/components/RatingsBadge.tsx
    frontend/src/pages/GameSession.tsx
  </read_first>
  <action>
**D-12: Ratings badges bleeding off card on mobile**

The eligible movies card in GameSession.tsx (line ~1095) has a flex row layout: poster (w-16) + content area. The content area contains metadata line + RatingsBadge. On narrow screens, badges overflow.

Fix in GameSession.tsx eligible movie cards:
1. The content area div (line ~1096, `className="flex flex-col justify-start flex-1 pt-2 pb-1 pr-3 gap-0.5 min-w-0"`) already has `min-w-0` which should allow truncation. The issue is that `flex-wrap gap-1.5` in RatingsBadge allows badges to wrap but they may still overflow the card width.
2. Add `overflow-hidden` to the content area div: change to `flex flex-col justify-start flex-1 pt-2 pb-1 pr-3 gap-0.5 min-w-0 overflow-hidden`
3. In the metadata row (line ~1116, `className="flex items-center gap-1 text-xs text-muted-foreground"`), add `flex-wrap` so year/runtime/MPAA wrap instead of overflow.

Fix in RatingsBadge.tsx:
1. The wrapper div uses `className="flex flex-wrap gap-1.5 items-center"`. This is correct for wrapping.
2. Add `max-w-full` to ensure the flex container respects parent constraints: `className="flex flex-wrap gap-1.5 items-center max-w-full"`

Also check the "adding to chain" step view (the Now Playing / current movie card area in GameSession). If that area also shows ratings badges, apply the same `overflow-hidden` and `flex-wrap` fixes.
  </action>
  <verify>
    <automated>grep -n "overflow-hidden" frontend/src/pages/GameSession.tsx | head -5 && grep -n "max-w-full" frontend/src/components/RatingsBadge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GameSession.tsx eligible movie content area div contains `overflow-hidden`
    - GameSession.tsx metadata row contains `flex-wrap`
    - RatingsBadge.tsx wrapper div contains `max-w-full`
  </acceptance_criteria>
  <done>Ratings badges and metadata wrap correctly within card boundaries on mobile viewports — no horizontal overflow from card elements.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` compiles without errors
2. Visual check: open app at 320px width — nav fits, no horizontal scroll
3. Visual check: eligible movies cards — badges wrap, don't overflow
4. Visual check: session grid tiles — no text wrapping from excess padding
</verification>

<success_criteria>
- NavBar renders all items on 320px viewport without horizontal scrolling
- No ratings badge overflows card boundaries on any mobile viewport
- Global page padding reduced by ~20% on mobile
- Session tile padding tightened
- Watch History mobile portrait shows poster and ratings
</success_criteria>

<output>
After completion, create `.planning/phases/19-v2-bug-fixes-polish/19-02-SUMMARY.md`
</output>
