---
phase: 03-movie-game
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_game.py
  - backend/tests/test_radarr.py
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - GAME-05
  - GAME-06
  - GAME-07
  - GAME-08

must_haves:
  truths:
    - "pytest tests/test_game.py -x collects all stubs with no import errors"
    - "pytest tests/test_radarr.py -x collects all stubs with no import errors"
    - "All stubs fail with NotImplementedError or pytest.skip (RED — not passing)"
  artifacts:
    - path: "backend/tests/test_game.py"
      provides: "Test stubs for GAME-01 through GAME-08"
      min_lines: 80
    - path: "backend/tests/test_radarr.py"
      provides: "RadarrClient unit test stubs"
      min_lines: 30
  key_links:
    - from: "backend/tests/test_game.py"
      to: "backend/tests/conftest.py"
      via: "shared fixtures (db, mock clients)"
      pattern: "from conftest|fixtures"
---

<objective>
Create the Wave 0 test stubs that define all GAME-01 through GAME-08 behaviors before any implementation exists.

Purpose: Establish the RED phase for TDD — every test must be collectible and must fail (not error on import). These stubs become the acceptance contract for all subsequent waves.
Output: backend/tests/test_game.py and backend/tests/test_radarr.py with failing stubs for every requirement.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-movie-game/03-CONTEXT.md
@.planning/phases/03-movie-game/03-RESEARCH.md

<interfaces>
<!-- Existing test infrastructure — read before writing stubs -->

From backend/pytest.ini:
  asyncio_mode = auto
  testpaths = tests
  python_files = test_*.py
  python_functions = test_*

From backend/tests/conftest.py (existing — check before adding fixtures):
  - Already has DB session fixtures from Phase 2
  - Do NOT duplicate existing fixtures; add only game-specific ones

From backend/app/models/__init__.py (Phase 2 ORM — stubs reference these):
  - Movie: tmdb_id, title, year, poster_path, vote_average, genres
  - Actor: tmdb_id, name, profile_path
  - Credit: movie_id, actor_id, character, order
  - WatchEvent: tmdb_id, movie_id, source, watched_at

Phase 3 models NOT YET CREATED (stubs reference them by import path for later):
  - GameSession: id, status, current_movie_tmdb_id, created_at, updated_at
  - GameSessionStep: id, session_id, step_order, movie_tmdb_id, actor_tmdb_id, actor_name, movie_title
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Game session test stubs (GAME-01 through GAME-08)</name>
  <files>backend/tests/test_game.py</files>
  <behavior>
    All tests must be collectible (no import errors) and must FAIL or skip.
    Use pytest.mark.xfail or raise NotImplementedError in stub body.

    Required test functions (one per bullet):
    - test_create_session: POST /game/sessions creates session, returns 201 with id and status="active"
    - test_create_session_with_title_search: POST /game/sessions with movie title (not just watched) creates session
    - test_create_session_conflict: POST /game/sessions when active session exists returns 409
    - test_get_active_session: GET /game/sessions/active returns current session
    - test_eligible_actors_excludes_picked: GET /game/sessions/{id}/eligible-actors excludes actors in session steps
    - test_eligible_actors_empty_when_all_picked: GET /game/sessions/{id}/eligible-actors returns empty list when all cast picked
    - test_eligible_movies: GET /game/sessions/{id}/eligible-movies returns actor's unwatched filmography
    - test_eligible_movies_combined_view: GET /game/sessions/{id}/eligible-movies without actor param returns all eligible across all current eligible actors
    - test_pick_actor_persisted: POST /game/sessions/{id}/pick-actor records step, returns updated session
    - test_pick_actor_already_picked: POST /game/sessions/{id}/pick-actor with already-picked actor returns 409
    - test_sort_movies: eligible-movies with sort=rating returns descending vote_average order
    - test_all_movies_toggle: eligible-movies with all_movies=true returns watched movies with watched=True flag; watched movies have selectable=False
    - test_watched_not_selectable: watched movie in eligible-movies has selectable=False
    - test_request_movie_radarr: POST /game/sessions/{id}/request-movie calls RadarrClient.add_movie when movie not in Radarr
    - test_request_movie_skip_radarr: POST /game/sessions/{id}/request-movie skips Radarr when movie already exists; returns status="already_in_radarr"
    - test_pause_session: POST /game/sessions/{id}/pause sets status="paused", returns 200
    - test_resume_session: POST /game/sessions/{id}/resume sets status="active", returns 200
    - test_end_session: POST /game/sessions/{id}/end sets status="ended", returns 200
    - test_import_csv: POST /game/sessions/import-csv with valid CSV rows creates session with pre-populated steps
  </behavior>
  <action>
    Create backend/tests/test_game.py.

    Structure:
    1. Imports: pytest, pytest_asyncio (if needed), httpx AsyncClient, app from app.main — match existing test file patterns (see test_movies.py or test_plex_webhook.py for reference).
    2. Use the existing conftest.py async_client fixture (check conftest.py first — it likely provides this).
    3. Each test function body: `pytest.fail("not implemented")` — this ensures RED state (collectible, explicitly failing).
    4. Group tests with comments: # GAME-01, # GAME-02, etc.
    5. Do NOT import GameSession or GameSessionStep yet — those models don't exist. Stub bodies don't need them.

    Test for RadarrClient mock: import unittest.mock; use AsyncMock to mock RadarrClient methods.
    For test_request_movie_radarr and test_request_movie_skip_radarr, note that the mock will be injected via app.state — check how existing tests mock TMDBClient (in test_tmdb.py or conftest.py) for the pattern.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && pytest tests/test_game.py --collect-only -q 2>&1 | head -30</automated>
  </verify>
  <done>pytest --collect-only finds all 19 test functions in test_game.py with no import errors. Running pytest tests/test_game.py returns FAILED (not ERROR) for each test.</done>
</task>

<task type="auto">
  <name>Task 2: RadarrClient test stubs (GAME-08)</name>
  <files>backend/tests/test_radarr.py</files>
  <action>
    Create backend/tests/test_radarr.py with unit test stubs for RadarrClient.

    Required test functions:
    - test_movie_exists_true: RadarrClient.movie_exists returns True when Radarr returns list with matching tmdbId
    - test_movie_exists_false: RadarrClient.movie_exists returns False when Radarr returns empty list
    - test_movie_exists_filters_by_tmdb_id: movie_exists filters list in Python — handles Radarr bug where tmdbId param returns all movies (any(m["tmdbId"] == tmdb_id for m in results))
    - test_add_movie_success: RadarrClient.add_movie returns Radarr response dict on 201
    - test_add_movie_already_exists_400: RadarrClient.add_movie treats 400 as success, returns {"status": "already_exists"} (not raises)
    - test_lookup_movie_found: RadarrClient.lookup_movie returns first result from Radarr lookup list
    - test_lookup_movie_not_found: RadarrClient.lookup_movie returns None when Radarr returns empty list
    - test_get_root_folder: RadarrClient.get_root_folder returns path string from first folder
    - test_get_quality_profile_id: RadarrClient.get_quality_profile_id returns int from first profile

    All stubs: use unittest.mock.AsyncMock or respx to mock httpx calls. Body: pytest.fail("not implemented").
    Mirror the existing TMDBClient test structure in test_tmdb.py for consistency.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && pytest tests/test_radarr.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>pytest --collect-only finds all 9 test functions in test_radarr.py with no import errors.</done>
</task>

</tasks>

<verification>
cd /Users/Oreo/Projects/backend && pytest tests/test_game.py tests/test_radarr.py --collect-only -q
Expected: 28 tests collected, 0 errors. All tests in FAILED state (not ERROR) when run without --collect-only.
</verification>

<success_criteria>
- 19 stubs in test_game.py, 9 stubs in test_radarr.py
- pytest --collect-only reports 28 items, 0 errors
- No import errors on collection
- Every test explicitly fails (RED) — no accidental passes
</success_criteria>

<output>
After completion, create .planning/phases/03-movie-game/03-01-SUMMARY.md
</output>
