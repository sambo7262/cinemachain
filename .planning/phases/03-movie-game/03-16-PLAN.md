---
phase: 03-movie-game
plan: "16"
type: execute
wave: 10
depends_on:
  - "03-14"
  - "03-15"
files_modified: []
autonomous: false
requirements:
  - GAME-01
  - GAME-04
  - GAME-05
  - GAME-06
  - GAME-07
gap_closure: true

must_haves:
  truths:
    - "GAME-01: User can start a session from lobby and end it — banner clears immediately on End Session"
    - "GAME-04: Actor selected in one move cannot appear in the Eligible Actors panel on subsequent moves"
    - "GAME-05: Sort controls change the movie order in the Eligible Movies table"
    - "GAME-06: Unwatched/All toggle changes which movies appear in the Eligible Movies table"
    - "GAME-07: Watched movies appear in the table but are not clickable (row does not open confirmation)"
    - "NavBar persists across lobby and game session pages"
    - "Eligible Movies table populates on tab open without requiring actor selection first"
  artifacts:
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Persistent nav bar"
    - path: "frontend/src/pages/GameLobby.tsx"
      provides: "End-session clear + Start CTA heading"
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "State machine UI + compact tables"
  key_links:
    - from: "GameLobby.tsx endMutation.onSuccess"
      to: "queryClient cache"
      via: "setQueryData(['activeSession'], null) — synchronous clear"
      pattern: "setQueryData"
    - from: "GameSession.tsx eligibleMovies query"
      to: "/api/game/sessions/{id}/eligible-movies"
      via: "enabled: !!sid && !!session (not gated by activeTab)"
      pattern: "enabled.*session"
---

<objective>
Re-verify all GAME requirements after 03-14 and 03-15 execution. This is the final verification checkpoint for Phase 3.

Purpose: GAME-01, GAME-04, and GAME-07 have never had a clean passing verification in the live app. This checkpoint confirms all 8 GAME requirements are satisfied, allowing Phase 3 to close.
Output: Verification record. If any defect persists, document for a follow-up 03-17 plan. If all pass, update ROADMAP.md and STATE.md to mark Phase 3 complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-game/03-CONTEXT.md
@.planning/phases/03-movie-game/03-14-SUMMARY.md
@.planning/phases/03-movie-game/03-15-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Docker images, run backend test suite, and verify GAME-04 actor deduplication via API</name>
  <files></files>
  <action>
    Run the Docker rebuild to push 03-14 and 03-15 code changes into the running containers:

    ```bash
    cd /Users/Oreo/Projects
    make rebuild
    ```

    If `make rebuild` is not available (dev machine without synobridge network), verify the frontend build is clean and inspect built artifacts:

    ```bash
    cd /Users/Oreo/Projects/frontend && npm run build 2>&1
    ```

    Verify the backend test suite still passes (no regressions from frontend changes):

    ```bash
    docker run --rm \
      -e DATABASE_URL=postgresql+asyncpg://test:test@localhost/test \
      -e TMDB_API_KEY=test \
      -e PLEX_URL=http://localhost:32400 \
      -e PLEX_TOKEN=test \
      -e RADARR_URL=http://localhost:7878 \
      -e RADARR_API_KEY=test \
      sambo7262/cinemachain-backend:latest \
      python -m pytest tests/ -x --tb=short -q 2>&1 | tail -20
    ```

    Expected: same pass/skip ratio as 03-13 (20 unit tests pass, ~9 skip without live DB).

    **GAME-04 actor deduplication — direct API verification:**

    GAME-04 was implemented in the backend during 03-05/03-06 (eligible-actors API filters out
    actors already used in prior steps). It was blocked from live verification in 03-13 by the
    session lifecycle bug (GAME-01). Now that 03-14 fixes session lifecycle, verify GAME-04
    directly against the running API:

    1. Start a game session via the API or UI and complete one full move (pick an actor, request a movie).
    2. Query the eligible actors endpoint for the new step:

    ```bash
    # Replace SESSION_ID and STEP_ID with values from the active session
    curl -s http://localhost:8000/api/game/sessions/${SESSION_ID}/eligible-actors?step_id=${STEP_ID} \
      | python3 -m json.tool | grep -i "name"
    ```

    3. Confirm the actor used in the prior step does NOT appear in the returned list.

    If the API is not reachable from the dev machine (NAS-only), note this in the summary and
    defer GAME-04 live verification to the human checkpoint in Task 2.

    Also verify NavBar.tsx, GameLobby.tsx, and GameSession.tsx are present in the frontend bundle:

    ```bash
    ls /Users/Oreo/Projects/frontend/dist/assets/*.js | head -5
    # Confirm dist/ was regenerated recently (within last few minutes)
    ```
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Frontend build passes clean. Backend tests pass same ratio as 03-13. dist/ contains updated bundle. GAME-04 API response confirmed to exclude the previously-used actor (or deferred to Task 2 checkpoint with note if NAS-only).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    03-14 fixes: End-session synchronous cache clear (setQueryData) + Eligible Movies combined view on mount.
    03-15 features: Global NavBar + session state machine UI (7 states) + compact table layout for actors and movies panels.
  </what-built>
  <how-to-verify>
    Deploy the rebuilt images to the NAS (docker compose pull + up, or rsync + rebuild on NAS), then perform the following checks:

    **GAME-01 — Session lifecycle (primary remaining defect):**
    1. Navigate to / — should see NavBar at top with CinemaChain logo and "Sessions" link
    2. If an active session exists: click "End Session" — banner should disappear IMMEDIATELY (no reload required)
    3. Start a new session from Watch History or Search tab — should succeed (no 409 error, navigate to /game/{id})

    **Eligible Movies combined view on mount (bug fix 3 from planning context):**
    4. In an active game session, click the "Eligible Movies" tab WITHOUT selecting any actor first
    5. Expect: table populates with movies from all eligible actors, each row showing "Via [Actor Name]"
    6. Expect: NOT an empty state

    **GAME-04 — Actor deduplication:**
    7. Start a new session, pick an actor, then request a movie (complete one full move)
    8. On the next move, return to Eligible Actors tab for the new current movie
    9. The actor picked in step 7 must NOT appear in the list

    **GAME-05 — Sort controls:**
    10. In Eligible Movies tab, use the "Sort by" dropdown — switch between By Rating, By Runtime, By Genre
    11. Confirm the table row order changes with each sort selection

    **GAME-06 — Watched toggle:**
    12. Toggle between "Unwatched Only" and "All Movies" — confirm watched movies appear/disappear

    **GAME-07 — Watched movies not selectable:**
    13. With "All Movies" toggle active, find a movie marked "Watched" (green badge in row)
    14. Click the watched movie row — confirm no confirmation dialog appears (row is non-interactive)

    **NavBar persistence (gap item 4):**
    15. Navigate between / and /game/{id} — NavBar should persist on both pages

    **Session state machine UI (gap item 7):**
    16. Observe the guidance panel below chain history:
        - New session (just started): "Starting with [Movie]. Pick an actor..."
        - After picking actor and requesting movie: "Added to Radarr. Mark it as watched..."
        - When status=awaiting_continue: "Marked as watched! Continue the chain" button
        - When paused: "Session paused. Resume when ready" + Resume button

    Report pass/fail for each numbered item above.
  </how-to-verify>
  <resume-signal>
    If ALL checks pass: type "all pass" — Phase 3 is complete, update ROADMAP.md and STATE.md.
    If any check fails: describe which numbered items failed and what was observed.
  </resume-signal>
</task>

</tasks>

<verification>
All 8 GAME requirements must have a confirmed PASS status:

| Requirement | Verified In | Expected Status |
|-------------|-------------|-----------------|
| GAME-01 | Checks 1-3 | PASS (session start/end lifecycle) |
| GAME-02 | Prior pass (03-13) | PASS (carried) |
| GAME-03 | Prior pass (03-13) + check 4-6 | PASS (eligible movies + combined view) |
| GAME-04 | Task 1 API check + check 7-9 | PASS (actor deduplication) |
| GAME-05 | Check 10-11 | PASS (sort controls) |
| GAME-06 | Check 12 | PASS (watched toggle) |
| GAME-07 | Check 13-14 | PASS (watched non-selectable) |
| GAME-08 | Prior pass (03-13) | PASS (Radarr request triggered) |
</verification>

<success_criteria>
- All 8 GAME requirements confirmed PASS by user in live NAS environment
- NavBar visible on all pages
- End Session banner clears immediately (synchronous)
- Eligible Movies combined view populates on tab open without actor pre-selection
- Compact table layout renders without horizontal overflow on TV display
- Phase 3 ROADMAP.md status updated to "Complete"
- STATE.md updated: current phase → Phase 4, status → ready
</success_criteria>

<output>
After checkpoint passes:
1. Create .planning/phases/03-movie-game/03-16-SUMMARY.md
2. Update .planning/ROADMAP.md Phase 3 status to "Complete (2026-03-15)"
3. Update .planning/REQUIREMENTS.md — confirm GAME-01 through GAME-08 all checked
4. Update .planning/STATE.md:
   - phase: Phase 4 — Query Mode
   - status: ready
   - stopped_at: Phase 3 complete — ready to plan Phase 4
</output>
