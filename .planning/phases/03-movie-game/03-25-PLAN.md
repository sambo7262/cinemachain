---
phase: 03-movie-game
plan: 25
type: execute
wave: 2
depends_on:
  - 03-24
files_modified:
  - Makefile
autonomous: false
gap_closure: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - GAME-05
  - GAME-06
  - GAME-07
  - GAME-08

must_haves:
  truths:
    - "Docker images rebuilt from 03-24 changes and pushed to Docker Hub"
    - "NAS containers running the new images"
    - "Session Home Page is the default landing view on /game/{id}"
    - "Mark as Watched button visible on Session Home Page for active+unwatched session"
    - "Continue the chain button visible on Session Home Page for awaiting_continue session"
    - "Back button in Tab View returns to Session Home Page"
    - "NavBar Sessions navigates directly to /game/{id}"
    - "Full game loop completable: Mark as Watched → Continue → pick actor → pick movie → home page returns with new movie"
    - "GAME-04: picked actor absent from Eligible Actors on next step"
  artifacts:
    - path: "Makefile"
      provides: "rebuild target for --no-cache Docker build"
      contains: "no-cache"
  key_links:
    - from: "Docker Hub"
      to: "NAS containers"
      via: "docker compose pull && docker compose up -d"
      pattern: "docker compose"
---

<objective>
Rebuild Docker images from 03-24 frontend changes and deploy to NAS. Human verifies the full session home page UX, then completes the full game loop to verify GAME-04 through GAME-08.

Purpose: No backend changes in 03-24 — only frontend. Rebuild is mandatory to ship the new images to NAS. This plan closes the 03-23 partial pass by verifying the complete game loop.
Output: Live NAS running updated images. Human checkpoint confirms all remaining GAME requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-movie-game/03-23-SUMMARY.md
@.planning/phases/03-movie-game/03-24-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker rebuild and push</name>
  <files>Makefile</files>
  <action>
Run the existing Makefile rebuild target to build both Docker images with `--no-cache` and push to Docker Hub. No backend changes in 03-24 — only the frontend image needs rebuilding. However, rebuild both to maintain consistent image tags.

Execute:
```bash
cd /Users/Oreo/Projects && make rebuild
```

The `rebuild` target in Makefile (added in 03-23) runs:
```
docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend
docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend
docker push sambo7262/cinemachain-backend:latest
docker push sambo7262/cinemachain-frontend:latest
```

Wait for both pushes to complete before proceeding.

If the rebuild target does not exist or fails, run the commands directly:
```bash
docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend && \
docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend && \
docker push sambo7262/cinemachain-backend:latest && \
docker push sambo7262/cinemachain-frontend:latest
```
  </action>
  <verify>
    <automated>docker images | grep cinemachain</automated>
  </verify>
  <done>Both sambo7262/cinemachain-backend:latest and sambo7262/cinemachain-frontend:latest are pushed to Docker Hub. `docker images | grep cinemachain` shows both images with a recent Created timestamp.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: NAS deploy and full game loop verification</name>
  <what-built>
03-24 GameSession.tsx view state refactor (view: "home" | "tabs" replacing showSessionHome) and NavBar active session routing. Both Docker images rebuilt and pushed.
  </what-built>
  <how-to-verify>
**Step 1: Deploy to NAS**

SSH into the NAS and update containers:
```bash
cd /volume1/docker/cinemachain
docker compose pull
docker compose up -d
docker compose ps
```
Confirm all containers healthy (backend, frontend, postgres).

**Step 2: Session Home Page is default landing (Gap 1)**

Navigate to `/game/{id}` for an existing active session (or create a new session from GameLobby).
- Expected: Session Home Page hub is shown immediately — current movie title, "Now in queue" or "Now watching" label, no Tab View.
- If movie is not yet watched: "Mark as Watched" button is visible.
- If session is awaiting_continue: "Continue the chain" button is visible (green).
- Tab View (Eligible Actors / Eligible Movies) should NOT be visible without clicking Continue.

**Step 3: NavBar Sessions routing (Gap 3)**

While the session is active, click "Sessions" in the NavBar.
- Expected: Navigate directly to /game/{id} — land on Session Home Page hub. NOT GameLobby.

**Step 4: Mark as Watched flow**

On Session Home Page with an active+unwatched session, click "Mark as Watched".
- Expected: Button calls the API; session status transitions to awaiting_continue; page now shows "Continue the chain" button.

**Step 5: Continue the chain → Tab View → Back button (Gap 2)**

Click "Continue the chain" from Session Home Page.
- Expected: Tab View opens with "← Back to session" button visible, Eligible Actors tab active and populated.

Click "← Back to session".
- Expected: Session Home Page is shown again with current movie and appropriate button (Continue the chain if awaiting_continue; Mark as Watched if active+unwatched).

**Step 6: Full game loop — GAME-04, GAME-05, GAME-06, GAME-07, GAME-08**

From Session Home Page → Continue the chain → Tab View:
1. **Pick an actor** (e.g., Actor A) from Eligible Actors tab — confirm Eligible Movies tab populates with that actor's films.
2. **Sort controls (GAME-05):** Change sort from "By Rating" to "By Runtime" — confirm rows visibly reorder. Change to "By Genre" — confirm again.
3. **Toggle filter (GAME-06/07):** Click "Unwatched Only" / "All Movies" toggle — confirm visible rows change. Click a watched movie row (opacity-40, cursor-not-allowed) — confirm NO confirm dialog fires. Click an unwatched movie row — confirm dialog fires.
4. **Confirm a movie** — click unwatched movie, confirm selection dialog. Confirm Radarr notification appears ("Added to Radarr queue." or "Already in your library...") on Session Home Page (GAME-08 mid-session).
5. **Session Home Page shown after confirmation** — confirm page shows new current movie with "Mark as Watched" button.
6. **Second move — actor dedup (GAME-04):** Mark new movie as watched → Continue the chain → Eligible Actors tab. Confirm Actor A from step 1 is ABSENT from the list.
  </how-to-verify>
  <resume-signal>
Type "approved" if all steps pass, or describe which step failed and what was observed.
  </resume-signal>
</task>

</tasks>

<verification>
Full verification is the human checkpoint in Task 2. Automated pre-check (post Task 1):

```bash
docker images | grep cinemachain
```
Expected: Two images with recent Created timestamps (sambo7262/cinemachain-backend:latest, sambo7262/cinemachain-frontend:latest).
</verification>

<success_criteria>
- Docker images pushed to Hub with 03-24 frontend changes
- NAS containers running new images (docker compose ps shows all healthy)
- Session Home Page is default view on /game/{id} (CONFIRMED live)
- Mark as Watched button visible and functional on Session Home Page (GAME-01)
- Continue the chain button visible and functional on Session Home Page for awaiting_continue (GAME-02/GAME-03)
- Back button in Tab View returns to Session Home Page (UX confirmed)
- NavBar Sessions navigates directly to active session home page (UX confirmed)
- Sort controls reorder Eligible Movies (GAME-05 CONFIRMED live)
- Toggle filter changes visible movie set; watched movies non-selectable (GAME-06/GAME-07 CONFIRMED live)
- Radarr notification on Session Home Page after mid-session movie confirm (GAME-08 CONFIRMED live)
- Picked actor absent from Eligible Actors on next step (GAME-04 CONFIRMED live)
- Phase 3 score: 8/8 requirements verified
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-game/03-25-SUMMARY.md`
</output>
