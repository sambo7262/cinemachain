---
phase: 03-movie-game
plan: 23
type: execute
wave: 3
depends_on: [03-21, 03-22]
files_modified:
  - Makefile
autonomous: false
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05, GAME-06, GAME-07, GAME-08]
gap_closure: true

must_haves:
  truths:
    - "User starts a session and sees Radarr start notification on the session page"
    - "Mark as Watched -> awaiting_continue -> Continue the chain -> Eligible Actors tab populates — no reversion to Mark as Watched"
    - "Full game loop completes: pick actor -> Eligible Movies tab populates -> pick movie -> session home page shown"
    - "Actor picked in step N does not appear in Eligible Actors for step N+1"
    - "Sort controls produce visibly reordered rows in Eligible Movies"
    - "Session home page shows current movie and previous movie after confirmation"
    - "Plex webhook endpoint returns 404 (removed)"
  artifacts:
    - path: "Makefile"
      provides: "rebuild target with --no-cache for backend and frontend"
      contains: "cinemachain-backend"
  key_links:
    - from: "Docker images rebuilt"
      to: "NAS containers"
      via: "docker push + NAS docker compose pull && up -d"
      pattern: "docker push.*cinemachain"
    - from: "NAS backend"
      to: "continue-chain endpoint"
      via: "POST /api/game/sessions/{id}/continue-chain returns 200"
      pattern: "continue-chain"
---

<objective>
Rebuild and push Docker images with 03-21 and 03-22 changes, deploy to NAS, and run human verification of the complete game loop.

Purpose: 03-21 adds the continue-chain endpoint and removes Plex webhook. 03-22 fixes handleContinue, adds Radarr fallback, implements session home page, and fixes thumbnails. This plan deploys both and verifies the full GAME-01 through GAME-08 loop passes on NAS hardware.

Output:
- Docker images sambo7262/cinemachain-backend:latest and sambo7262/cinemachain-frontend:latest rebuilt, pushed, and deployed on NAS
- Human verification: all 5 test scenarios pass
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-movie-game/03-21-SUMMARY.md
@.planning/phases/03-movie-game/03-22-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and push Docker images</name>
  <files>Makefile</files>
  <read_first>
    - /Users/Oreo/Projects/Makefile (read to understand existing rebuild targets before adding or running)
  </read_first>
  <action>
Run the following commands from `/Users/Oreo/Projects` to rebuild both images with no cache and push to Docker Hub:

```bash
docker build --no-cache -t sambo7262/cinemachain-backend:latest /Users/Oreo/Projects/backend
docker push sambo7262/cinemachain-backend:latest
docker build --no-cache -t sambo7262/cinemachain-frontend:latest /Users/Oreo/Projects/frontend
docker push sambo7262/cinemachain-frontend:latest
```

If the Makefile has a `rebuild` target, use `make rebuild` instead — it already contains the --no-cache flags and push steps. Read the Makefile first to determine whether to use `make rebuild` or run docker commands directly.

Do NOT skip `--no-cache`. Previous sessions have confirmed that Docker layer caching can serve stale frontend builds on NAS hardware.
  </action>
  <verify>
    <automated>docker images | grep -E "cinemachain-(backend|frontend)" | grep latest && echo "Images present"</automated>
  </verify>
  <acceptance_criteria>
    - `docker images | grep cinemachain | grep latest` shows both backend and frontend images with a recent CREATED timestamp (within the last 30 minutes)
    - `docker push` exits 0 for both images
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to NAS and run full game loop verification</name>
  <what-built>
    03-21: POST /game/sessions/{id}/continue-chain endpoint (awaiting_continue -> active WITHOUT resetting current_movie_watched). Plex webhook router removed entirely (plex.py deleted, main.py updated).

    03-22: handleContinue calls api.continueChain — not resumeSession. Radarr start notification fallback reads from session polling response. Session home page after movie confirmation (shows current + previous movie). Eligible Movies thumbnails enlarged (w-12 h-[4.5rem]).
  </what-built>
  <how-to-verify>
**Step 0 — Deploy on NAS:**

On the NAS, run:
```bash
docker compose pull && docker compose up -d
```

Watch backend startup logs for "Running upgrade ... -> 0003" to confirm Alembic migration ran (if it hasn't already from 03-19/03-20 deploy). Wait for all containers to be healthy.

**Test 1 — Plex webhook removed (NEW-01):**

```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://NAS_IP:8000/api/webhooks/plex
```

Expected: 404. (The endpoint no longer exists.)

**Test 2 — Session start + Radarr notification (GAME-01, GAME-08 start path):**

1. Navigate to the lobby. If an active session exists, end it first.
2. Search for a movie (e.g., "Cast Away") and tap it to create a session.
3. Confirm: a blue Radarr notification appears at the top of the session page. It should read "Added to Radarr queue." or "Already in your library — waiting for watched event."
4. Confirm: the Eligible Actors tab shows a locked message: "Watch Cast Away to unlock eligible actors."
5. Confirm: a "Mark as Watched" button is visible in the session state panel.

**Test 3 — Continue the chain fix (root defect — GAME-01, GAME-02, GAME-03):**

1. Click "Mark as Watched."
2. Confirm: status transitions to "awaiting_continue" and a green "Continue the chain" button appears.
3. Click "Continue the chain."
4. CRITICAL PASS CRITERION: the UI must NOT revert to showing "Mark as Watched." The Eligible Actors tab must populate with cast members of the starting movie. Both tabs must show content (actors and movies in combined view), not the locked clock icon.
5. Confirm: Eligible Movies tab (combined view) also loads — either immediately or within 5-10 seconds if background pre-fetch is still running.

**Test 4 — Full game loop + actor dedup + sort + session home page (GAME-04, GAME-05, GAME-06, GAME-07, GAME-08, NEW-02):**

1. From the Eligible Actors tab, pick any actor (e.g., Tom Hanks).
2. Confirm: Eligible Movies tab switches to that actor's filmography.
3. Switch sort between "By Rating" / "By Runtime" / "By Genre" — confirm rows visibly reorder each time.
4. Toggle "Unwatched Only" / "All Movies" — confirm the set of visible rows changes.
5. Click a watched movie row — confirm NO confirmation dialog fires (watched movies are non-selectable).
6. Click an unwatched movie. Confirm the "Select [movie] as your next movie?" confirmation dialog appears. Confirm it.
7. Confirm: a Radarr notification appears (GAME-08 mid-session path).
8. Confirm: the session home page appears showing the confirmed movie under "Now in queue" and the previous movie under "Previous movie."
9. Click "Mark as Watched" on the home page.
10. Confirm: status transitions to "awaiting_continue" again.
11. Click "Continue the chain."
12. Confirm: Eligible Actors tab populates — and the actor picked in step 1 does NOT appear in the new list (GAME-04 actor dedup).

**Test 5 — Thumbnail size:**

1. In the Eligible Movies tab, confirm that movie poster thumbnails are visibly larger than in previous sessions (approximately 48x72px instead of 32x48px).

**Test 6 — Pagination (regression check):**

1. In the Eligible Movies tab, if a "Load more" button appears, confirm it shows additional results when clicked.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 core tests pass (Tests 2, 3, 4, 5 required; Test 1 and Test 6 are bonus).
    Describe any failures so a 03-24 gap plan can be written if needed.
    Phase 3 closes when approved.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 3 closes when all 8 GAME requirements pass human verification on NAS hardware:

- GAME-01: Session start shows Radarr notification + watched gate; Continue the chain does NOT revert UI
- GAME-02: Eligible Actors tab populates after Continue the chain
- GAME-03: Eligible Movies tab populates (actor-scoped and combined view)
- GAME-04: Actor picked in step N absent from Eligible Actors in step N+1
- GAME-05: Sort controls produce visible row reordering
- GAME-06: Watched toggle shows/hides watched movies
- GAME-07: Watched movies are non-selectable (click does nothing)
- GAME-08: Radarr notification on session start AND on mid-session movie pick

Plus new requirements:
- NEW-01: Plex webhook returns 404 (endpoint removed)
- NEW-02: Session home page visible after movie confirmation
</verification>

<success_criteria>
- Both Docker images rebuilt with --no-cache, pushed to sambo7262/cinemachain-*:latest
- NAS containers updated (docker compose pull + up -d)
- Human verification Test 3 (Continue the chain) passes — no reversion to Mark as Watched state
- Human verification Test 4 completes the full game loop including session home page and actor dedup
- Human verification: "approved" received
- Phase 3 marked complete in STATE.md and ROADMAP.md after approval
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-game/03-23-SUMMARY.md` following the template at `.claude/get-shit-done/templates/summary.md`.

If all tests pass, update:
- `.planning/STATE.md`: set Phase 3 status to Complete
- `.planning/ROADMAP.md`: mark Phase 3 as [x] complete
- `.planning/REQUIREMENTS.md`: update GAME-01 through GAME-08 traceability to Complete
</output>
