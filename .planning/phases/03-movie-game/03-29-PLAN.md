---
phase: 03-movie-game
plan: 29
type: execute
wave: 21
depends_on: [03-28]
files_modified: []
autonomous: false
gap_closure: true
requirements: [GAME-04]

must_haves:
  truths:
    - "Full 6-step game loop completes on live NAS hardware without errors"
    - "Eligible Actors for the 2nd movie shows cast of that movie only (not intersection with prior movies)"
    - "Phase 3 can be declared complete after this plan"
  artifacts:
    - path: "backend/app/routers/game.py"
      provides: "03-28 changes running in production container"
  key_links:
    - from: "NAS Docker containers"
      to: "backend/app/routers/game.py"
      via: "docker compose pull + up -d"
      pattern: "docker compose"
---

<objective>
Rebuild Docker images with the 03-28 fix, push to Docker Hub, deploy to the NAS, and run the full 6-step game loop end-to-end to confirm GAME-04 is resolved and Phase 3 is complete.

Purpose: Verify the fix is live on real hardware before declaring Phase 3 done.
Output: Confirmed PASS on all 6 game loop steps; Phase 3 status updated to complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-movie-game/03-27-SUMMARY.md
@.planning/phases/03-movie-game/03-28-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild and push Docker images</name>
  <files>Makefile</files>
  <action>
Run the Makefile rebuild target to build both backend and frontend images with --no-cache and push to Docker Hub. This is the same pattern used in every prior deploy plan.

```bash
cd /Users/Oreo/Projects && make rebuild
```

The rebuild target builds sambo7262/cinemachain-backend:latest and sambo7262/cinemachain-frontend:latest with --no-cache and pushes both. If the Makefile rebuild target is not available or fails, run manually:

```bash
docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend
docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend
docker push sambo7262/cinemachain-backend:latest
docker push sambo7262/cinemachain-frontend:latest
```
  </action>
  <verify>
    <automated>docker images sambo7262/cinemachain-backend:latest --format "{{.CreatedAt}}"</automated>
  </verify>
  <done>Both images pushed to Docker Hub with a fresh build timestamp.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: NAS deploy + full game loop verification</name>
  <files></files>
  <action>
Deploy updated images to the NAS and verify the full 6-step game loop. SSH to NAS and run:

```
cd /volume1/docker/cinemachain
docker compose pull
docker compose up -d
```

Then run the full game loop test described in how-to-verify below.
  </action>
  <what-built>
    Docker images rebuilt and pushed with the 03-28 GAME-04 fix (background pre-fetch in request_movie + on-demand fallback in get_eligible_actors).
  </what-built>
  <how-to-verify>
    Run the full 6-step game loop using the same test sequence as 03-27:

    Step 1 — Deploy
    - SSH to NAS, run: docker compose pull and then docker compose up -d
    - Confirm backend and frontend containers are running

    Step 2 — Fresh session
    - Navigate to the app in your browser
    - If a session is active, end it
    - Start a new session with "Free Guy" as the starting movie
    - Confirm session home page appears showing Free Guy

    Step 3 — First movie flow
    - Click "Mark as Watched" for Free Guy
    - Confirm status transitions to awaiting_continue
    - Click "Continue the chain"
    - Confirm Eligible Actors tab is accessible and shows cast members of Free Guy

    Step 4 — Pick actor
    - Click on Ryan Reynolds in the Eligible Actors tab
    - Confirm actor is recorded in the chain

    Step 5 — Pick next movie
    - Open Eligible Movies tab
    - Select "Deadpool and Wolverine"
    - Confirm confirmation dialog appears and Radarr queues the movie
    - Confirm session advances to Deadpool and Wolverine as the new current movie

    Step 6 — GAME-04 verification (the fix being tested)
    - Click "Mark as Watched" for Deadpool and Wolverine
    - Click "Continue the chain"
    - Open the Eligible Actors tab
    - EXPECTED: Tab shows cast members of Deadpool and Wolverine who are NOT Ryan Reynolds
    - EXPECTED: Tab does NOT show only actors who appeared in both Free Guy AND Deadpool and Wolverine
    - Confirm at least 5-10 actors appear (e.g. Hugh Jackman, Blake Lively, etc.)

    If Step 6 shows a rich cast list for Deadpool and Wolverine (not just the intersection), the fix is working.
  </how-to-verify>
  <resume-signal>
    Type "PASS" if all 6 steps succeed, or describe which step failed and what was observed.
  </resume-signal>
  <verify>
    <automated>MISSING — human verification required for live NAS game loop</automated>
  </verify>
  <done>All 6 game loop steps pass on live NAS: deploy healthy, session starts, first movie flow works, actor pick recorded, movie advanced, Step 6 shows correct Eligible Actors for 2nd movie (cast minus picked actors, not intersection).</done>
</task>

</tasks>

<verification>
All 6 game loop steps pass on live NAS hardware:
1. Deploy — containers healthy
2. Fresh session — session home page loads
3. First movie flow — Mark as Watched + Continue the chain work
4. Actor pick — actor recorded in chain
5. Movie pick — Radarr queued, session advances
6. GAME-04 — Eligible Actors for 2nd movie shows that movie's full cast minus picked actors (not intersection)
</verification>

<success_criteria>
- Step 6 passes: Eligible Actors for Deadpool and Wolverine shows multiple cast members who have not been picked
- Phase 3 requirements GAME-01 through GAME-08 are all satisfied
- ROADMAP.md Phase 3 status updated to Complete
- STATE.md updated to reflect Phase 3 complete, Phase 4 ready to start
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-game/03-29-SUMMARY.md` following the summary template.

If the checkpoint PASSES, also:
- Update ROADMAP.md Phase 3 status from [~] to [x] Complete
- Update STATE.md current position to "Phase 3 complete; Phase 4 (Query Mode) ready to start"
</output>
