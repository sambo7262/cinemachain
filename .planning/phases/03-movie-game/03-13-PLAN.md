---
phase: 03-movie-game
plan: "13"
type: execute
wave: 7
depends_on:
  - "03-11"
  - "03-12"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - GAME-01
  - GAME-03
  - GAME-04
  - GAME-05
  - GAME-06
  - GAME-07
  - GAME-08

must_haves:
  truths:
    - "Browser at / shows CinemaChain lobby (not 'coming soon' placeholder)"
    - "User can start a new session by selecting a movie from watched history or title search"
    - "Existing session can be ended from the lobby — banner disappears and start tabs appear"
    - "Selecting an actor in the game populates the Eligible Movies tab with that actor's full filmography from TMDB"
    - "Pause button shows 'Pause' when active and 'Resume' when paused — clicking either toggles state visually"
    - "Sort controls (By Rating, By Runtime, By Genre) reorder the Eligible Movies list"
    - "Unwatched/All toggle changes which movies appear — watched movies show badge and are not clickable"
    - "Selecting a movie triggers a Radarr request and advances the session"
  artifacts:
    - path: "backend/app/routers/game.py"
      provides: "eligible-movies with on-demand TMDB fetch (from 03-11)"
    - path: "Makefile"
      provides: "rebuild target (from 03-11)"
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "pause/resume toggle (from 03-12)"
    - path: "frontend/src/pages/GameLobby.tsx"
      provides: "end-session refetch fix (from 03-12)"
  key_links:
    - from: "browser /"
      to: "GameLobby component"
      via: "React Router Route path='/'"
      pattern: "path.*/"
    - from: "eligible-movies tab"
      to: "TMDB actor filmography"
      via: "_ensure_actor_credits_in_db called before DB JOIN"
      pattern: "_ensure_actor_credits_in_db"
---

<objective>
Re-verify Phase 3 after remediation plans 03-11 and 03-12.

This plan executes the same verification as 03-10 but on the fixed codebase. It first runs `make rebuild` to force a fresh Docker image build (clearing the stale Phase 1 frontend layer), then conducts a live end-to-end walkthrough of the full game loop.

Purpose: Phase 3 cannot close until GAME-01 through GAME-08 (except GAME-02 which already passed) are confirmed working in the running container.
Output: All 8 GAME requirements verified (or specific remaining defects documented for further remediation).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-game/03-10-SUMMARY.md
@.planning/phases/03-movie-game/03-11-SUMMARY.md
@.planning/phases/03-movie-game/03-12-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Docker images and run backend test suite</name>
  <files></files>
  <action>
    Run the following commands in sequence:

    **Step 1 — Rebuild Docker images from local source (clears stale Phase 1 frontend):**
    ```bash
    cd /Users/Oreo/Projects && make rebuild
    ```
    Wait for the build to complete. Both images must build successfully before proceeding.

    **Step 2 — Verify backend test suite still passes:**
    ```bash
    cd /Users/Oreo/Projects/backend && pytest tests/ -x -q 2>&1 | tail -15
    ```
    Expected: 19 passed (or more), 0 failures. The `_ensure_actor_credits_in_db` addition must not break any existing tests.

    **Step 3 — Spot check the eligible-movies change is live:**
    ```bash
    docker exec cinemachain-backend python -c "from app.routers.game import _ensure_actor_credits_in_db; print('helper present in running container')"
    ```
    If the container is running the new image, this import will succeed. If it fails, the rebuild did not take effect — run `docker compose pull && docker compose up -d --force-recreate` as a fallback.

    **Step 4 — Verify the frontend is serving the correct app:**
    ```bash
    curl -s http://localhost:3111/ | grep -i "cinemachain\|coming soon\|vite\|react" | head -5
    ```
    Expected: Response contains "cinemachain" or Vite/React script references — NOT "coming soon". If "coming soon" is still present, the old image is cached — run `docker compose up -d --force-recreate --pull always` as a fallback.

    Record results for each step.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    Backend test suite green. `_ensure_actor_credits_in_db` importable from running container. Frontend serving React app (not "coming soon" placeholder).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The following defects from 03-10 have been fixed:
    1. Docker rebuild (03-11 Task 2): `make rebuild` forces --no-cache build clearing stale Phase 1 frontend
    2. Eligible-movies on-demand TMDB fetch (03-11 Task 1): actor filmography fetched from TMDB before DB JOIN
    3. Pause/resume toggle (03-12 Task 1): header button shows "Pause" or "Resume" based on session status
    4. End-session lifecycle (03-12 Task 2): endMutation uses refetchQueries for immediate banner clear
  </what-built>
  <how-to-verify>
    Verify in a browser at http://localhost:3111 (or the Tailscale IP if on NAS).

    **Defect 1 check — Routing:**
    1. Navigate to http://localhost:3111/
    2. Expected: CinemaChain lobby with "Watch History", "Search Title", "Import Chain" tabs — NOT the "coming soon" placeholder

    **Defect 2 check — Session lifecycle:**
    3. If an active session exists from previous testing: click "End Session" in the active session banner
    4. Expected: Banner disappears; three-tab start panel appears immediately (within 1-2 seconds)
    5. Start a new session: click "Search Title" tab, search for any movie (e.g. "Inception"), click the result
    6. Expected: Navigates to /game/{id} — game session page loads

    **Defect 3 check — Eligible movies:**
    7. In the game session, click any actor in the Eligible Actors tab
    8. Expected: Eligible Movies tab populates with multiple movies (not just the starting movie). If this is the first time this actor's filmography is queried, there may be a 1-3 second TMDB fetch delay — wait for the list to populate.
    9. Confirm at least 5+ movies appear in the list

    **Defect 4 check — Pause toggle:**
    10. Click the "Pause" button in the game session header
    11. Expected: Button label changes to "Resume" within 1-2 seconds (after cache invalidation + refetch)
    12. Click "Resume"
    13. Expected: Button label changes back to "Pause"

    **GAME-04 — Actor exclusion:**
    14. Pick an actor (via Eligible Actors tab → actor click → movie selection flow)
    15. Return to Eligible Actors tab on the new movie
    16. Expected: Previously picked actor does NOT appear in the list

    **GAME-05/06/07 — Sort, filter, selectability:**
    17. In Eligible Movies tab, try "By Rating", "By Runtime", "By Genre" sort options — list reorders
    18. Click "Unwatched Only" toggle to switch to "All Movies" — watched movies appear with Watched badge
    19. Watched movies should NOT be clickable (cursor-not-allowed, no action on click)

    **GAME-08 — Radarr request:**
    20. Select an unwatched movie: click it, confirm the dialog
    21. Expected: Session advances to the new movie as current; Radarr receives the add request (check Radarr UI or logs if accessible)
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "verified" — all checks passed; Phase 3 complete
    - "partial: [list remaining defects]" — some checks failed; document what failed and what steps reproduced it
    - "blocked: [reason]" — cannot verify (e.g., Docker build failed, container not running)
  </resume-signal>
</task>

</tasks>

<verification>
cd /Users/Oreo/Projects/backend && pytest tests/ -x -q 2>&1 | tail -5
Expected: All tests pass with 0 failures.
Live check: http://localhost:3111/ shows CinemaChain lobby.
</verification>

<success_criteria>
- GAME-01: User reaches CinemaChain lobby at / (not placeholder); can start a session by selecting a movie; can end an existing session and start a new one
- GAME-02: Eligible Actors panel shows cast of current movie (already verified in 03-10 — confirm still working)
- GAME-03: Selecting an actor populates Eligible Movies tab with that actor's full TMDB filmography (not just the starting movie)
- GAME-04: Picked actors are excluded from the Eligible Actors panel on subsequent moves
- GAME-05: Sort controls (By Rating, By Runtime, By Genre) reorder the movie list
- GAME-06: All Movies toggle shows watched + unwatched; Unwatched Only (default) hides watched movies
- GAME-07: Watched movies are not clickable (opacity badge, no selection action)
- GAME-08: Movie selection triggers Radarr request and session advances
- Pause button toggles correctly: "Pause" → "Resume" → "Pause"
</success_criteria>

<output>
After completion, create .planning/phases/03-movie-game/03-13-SUMMARY.md

If all GAME requirements verified: update ROADMAP.md Phase 3 status to "Complete" and STATE.md current position to Phase 4.
If partial pass: create a 03-14-PLAN.md for remaining defects using the same gap-closure pattern.
</output>
