---
phase: 03-movie-game
plan: 27
type: execute
wave: 2
depends_on: [03-26]
files_modified: []
autonomous: false
gap_closure: true
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05, GAME-06, GAME-07, GAME-08]

must_haves:
  truths:
    - "Full game loop completes end-to-end on live NAS: start session, mark watched, continue chain, pick actor, pick movie, Mark as Watched visible for 2nd movie"
    - "2nd movie Mark as Watched button is present and functional on Session Home Page"
    - "GAME-04 actor deduplication holds across chain steps"
  artifacts:
    - path: "Makefile"
      provides: "rebuild target for --no-cache Docker build and push"
      contains: "rebuild"
  key_links:
    - from: "backend/app/routers/game.py request_movie"
      to: "NAS live container"
      via: "Docker Hub push + docker compose pull"
      pattern: "current_movie_watched = False"
---

<objective>
Rebuild Docker images with the 03-26 fix, push to Docker Hub, deploy to NAS, and verify the full game loop end-to-end including the 2nd movie Mark as Watched button.

Purpose: The 03-26 fix only takes effect when the backend container is rebuilt and redeployed. This plan closes Phase 3 by confirming the complete game loop works on live hardware.

Output: Phase 3 fully verified and closed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-game/03-25-SUMMARY.md
@.planning/phases/03-movie-game/03-26-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild and push Docker images</name>
  <files></files>
  <action>
From the project root, run the Makefile rebuild target to build both backend and frontend images with --no-cache and push to Docker Hub:

    make rebuild

This is the same target used in 03-25 (commit 1b9dfac). Both images must be pushed before NAS deploy proceeds. Confirm both pushes complete without error in terminal output.

If make rebuild is unavailable or fails, fall back to:
    docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend
    docker push sambo7262/cinemachain-backend:latest
    docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend
    docker push sambo7262/cinemachain-frontend:latest
  </action>
  <verify>
    <automated>docker images sambo7262/cinemachain-backend:latest --format "{{.Repository}}:{{.Tag}} {{.CreatedAt}}"</automated>
  </verify>
  <done>Both backend and frontend images pushed to Docker Hub with a timestamp after 03-26 fix was applied. Terminal shows "pushed" confirmation for both images.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    03-26 fix applied (current_movie_watched=False reset in request_movie) + Docker images rebuilt and pushed to Docker Hub.

    To deploy to NAS: SSH in and run:
        docker compose pull
        docker compose up -d

    Then test the full game loop:
  </what-built>
  <how-to-verify>
    Step 1 — Deploy: SSH to NAS, run `docker compose pull && docker compose up -d`, confirm containers start.

    Step 2 — Start or reuse a session: Navigate to the app. If no active session, start one. If a session is already at the 2nd movie or later, end it and start fresh to test from clean state.

    Step 3 — First movie flow (Steps 1-5, already confirmed in 03-25 but re-verify quickly):
      - Session Home Page shows as default landing on /game/{id}
      - Mark as Watched button visible (status=active, current_movie_watched=false)
      - Click Mark as Watched — button disappears, Continue the chain appears
      - Click Continue the chain — Tab View opens with Eligible Actors tab

    Step 4 — Pick actor and movie (Step 6 — THIS IS THE GAP):
      - Pick an eligible actor from Eligible Actors tab
      - Switch to Eligible Movies tab, select a movie, confirm selection
      - App should return to Session Home Page showing the 2nd movie

    Step 5 — 2nd movie Mark as Watched (THE CRITICAL CHECK):
      - On Session Home Page, the Mark as Watched button MUST be visible
      - Click it — confirm it works (Continue the chain appears)

    Step 6 — GAME-04 actor dedup:
      - Click Continue the chain from 2nd movie
      - On Eligible Actors tab, confirm the actor picked in Step 4 does NOT appear in the list

    Expected result at Step 5: Mark as Watched button is present. This was the failing step in 03-25.
  </how-to-verify>
  <resume-signal>Type "PASS" if all steps including Step 5 pass, or describe what failed and at which step.</resume-signal>
</task>

</tasks>

<verification>
Full game loop verified on live NAS:
- Steps 1-5 (already passing from 03-25) remain passing
- Step 6: 2nd movie Mark as Watched button is present and functional
- GAME-04: actor picked in step N does not appear in eligible actors for step N+1
</verification>

<success_criteria>
Human confirms "PASS" — full game loop completes end-to-end with the 2nd movie Mark as Watched button visible and functional. Phase 3 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-game/03-27-SUMMARY.md` following the standard summary template.

If PASS: Update STATE.md — Phase 3 complete, progress to Phase 4.
</output>
