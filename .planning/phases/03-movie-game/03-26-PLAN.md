---
phase: 03-movie-game
plan: 26
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/game.py
autonomous: true
gap_closure: true
requirements: [GAME-04, GAME-05]

must_haves:
  truths:
    - "After selecting the 2nd movie, the Session Home Page shows the Mark as Watched button"
    - "current_movie_watched is False immediately after request_movie completes"
  artifacts:
    - path: "backend/app/routers/game.py"
      provides: "request_movie endpoint with current_movie_watched reset"
      contains: "session.current_movie_watched = False"
  key_links:
    - from: "backend/app/routers/game.py request_movie"
      to: "GameSession.current_movie_watched"
      via: "direct attribute assignment before db.commit()"
      pattern: "current_movie_watched = False"
---

<objective>
Fix the request_movie endpoint to reset current_movie_watched=False when advancing to the new movie.

Purpose: After continue-chain intentionally leaves current_movie_watched=True (so eligible-actors/movies gates stay open), request_movie must reset the flag to False so the Session Home Page condition (active + !current_movie_watched) is met for the 2nd movie, restoring the Mark as Watched button.

Output: One-line fix in backend/app/routers/game.py closing the full game loop state machine.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-game/03-25-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset current_movie_watched=False in request_movie</name>
  <files>backend/app/routers/game.py</files>
  <read_first>backend/app/routers/game.py lines 757-815 (the request_movie endpoint)</read_first>
  <action>
In the request_movie endpoint (starting at line 757), immediately after line 797:

    session.current_movie_tmdb_id = body.movie_tmdb_id

add the single line:

    session.current_movie_watched = False

The insertion point is between line 797 (`session.current_movie_tmdb_id = body.movie_tmdb_id`) and line 798 (`await db.commit()`). The fix must appear before `await db.commit()` so the reset is persisted atomically with the movie advancement.

Do NOT change anything else in the endpoint. Do not modify the Radarr call, step creation, re-fetch, or response shape.

Context for why:
- mark_current_watched sets current_movie_watched=True and advances to awaiting_continue
- continue_chain transitions awaiting_continue -> active, intentionally preserving current_movie_watched=True (this unlocks the eligible-actors/movies gates)
- request_movie picks the new movie and must reset current_movie_watched=False so the Session Home Page condition (status==='active' && current_movie_watched===false) is met, revealing the Mark as Watched button for the 2nd movie
  </action>
  <verify>
    <automated>grep -n "current_movie_watched = False" /Users/Oreo/Projects/backend/app/routers/game.py</automated>
  </verify>
  <done>
grep output shows at least two occurrences of `current_movie_watched = False` in game.py — one in resume_session (line ~370, pre-existing) and one in request_movie (new, between the current_movie_tmdb_id assignment and db.commit on lines ~797-798). The request_movie endpoint body contains both `session.current_movie_tmdb_id = body.movie_tmdb_id` and `session.current_movie_watched = False` before `await db.commit()`.
  </done>
</task>

</tasks>

<verification>
After the fix:
- `grep -n "current_movie_watched = False" backend/app/routers/game.py` returns two lines: one in resume_session, one in request_movie
- The request_movie block (lines ~784-798) reads in order: add new_step, db.add(new_step), session.current_movie_tmdb_id = ..., session.current_movie_watched = False, await db.commit()
</verification>

<success_criteria>
session.current_movie_watched = False is present in the request_movie endpoint body, between the current_movie_tmdb_id assignment and db.commit(). No other lines changed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-game/03-26-SUMMARY.md` following the standard summary template.
</output>
