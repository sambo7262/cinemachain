---
phase: 23-smart-metadata-backfill
plan: 02
plan_id: "23-02"
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified: []
autonomous: false
requirements:
  - HEALTH-03

must_haves:
  truths:
    - "Migration 0020 ran successfully on the live NAS PostgreSQL instance (`alembic upgrade head` completed without error)"
    - "Post-migration, zero rows in `movies` have `mpaa_rating = ''` (all promoted to `'NR'`)"
    - "Docker stack restarted via `make rebuild` and backend container picked up the new code"
    - "Within ≤2 nightly cycles, `_backfill_mpaa_pass` log entry shows a few hundred rows processed (NOT the previous ~25k)"
    - "DB Health view in the live Settings page shows realistic addressable gap counts: `Missing MPAA` and `Missing RT score` each dropped from ~30k to a few hundred"
    - "The 1,459 never-fetched MDBList movies are cleared within 1-2 nightly cycles (the MDBList nightly job was already correct; just need to confirm it runs and closes the gap)"
    - "Frontend MPAA rendering shows no user-visible regression — pages display `'NR'` identically to how they previously displayed `''` (per pre-flight grep findings on MovieCard.tsx:84, SearchPage.tsx:174, GameSession.tsx:228)"
  artifacts:
    - path: ".planning/phases/23-smart-metadata-backfill/23-02-SUMMARY.md"
      provides: "NAS deployment evidence + observed log line counts + before/after DB Health screenshots-or-numbers"
  key_links:
    - from: "User's NAS Synology Docker environment"
      to: "v2.0.2 patch (cache.py sentinel fix + migration 0020 + audit query fix)"
      via: "git pull → make rebuild → alembic upgrade head → restart"
      pattern: "alembic upgrade head.*Running upgrade 0019 -> 0020"
    - from: "Nightly _backfill_mpaa_pass invocation"
      to: "Log line `_backfill_mpaa_pass: N movies need MPAA rating`"
      via: "scheduled background task"
      pattern: "_backfill_mpaa_pass: [0-9]+ movies need MPAA rating"
---

<objective>
Live-deploy the v2.0.2 patch to the NAS and confirm the four observable HEALTH-03 success conditions:
(a) `_backfill_mpaa_pass` log entry shows a few hundred rows processed (not 25k)
(b) DB Health view shows realistic addressable gaps for MPAA + RT
(c) the 1,459 never-fetched MDBList movies are processed and the gap closes
(d) no regression in frontend MPAA rendering — `"NR"` displays identically to current `""` handling

Purpose: This phase is half code + half operational evidence. The code changes in 23-01 are theoretically correct, but until the user sees the nightly log line shrink and the DB Health number drop on the live NAS, HEALTH-03 is not satisfied. This plan exists to gate that verification and capture the evidence.

Output:
- A SUMMARY.md documenting the deploy steps, observed log lines, before/after DB Health numbers, and frontend rendering checks
- An explicit user sign-off (via the human-verify checkpoint) confirming all four sub-conditions met
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-smart-metadata-backfill/23-CONTEXT.md
@.planning/phases/23-smart-metadata-backfill/23-01-PLAN.md
@.planning/phases/23-smart-metadata-backfill/23-01-SUMMARY.md
</context>

<threat_model>
<surface>Production NAS deploy + live data migration on 58k-row table</surface>

<risks>
  <risk id="R1" severity="medium">
    <name>Migration 0020 runs but takes longer than expected on production hardware</name>
    <vector>Synology NAS PostgreSQL on consumer-grade SSD may be slower than dev/test environments</vector>
    <mitigation>UPDATE filtered by `WHERE mpaa_rating = ''` is bounded by ~33k rows; even at 10ms/row that's &lt;6min. No DDL, no lock escalation. If it stalls, downgrade is one command (`alembic downgrade -1`).</mitigation>
  </risk>
  <risk id="R2" severity="low">
    <name>Frontend renders `"NR"` differently on a page we didn't grep</name>
    <vector>Pre-flight grep covered MovieCard.tsx, SearchPage.tsx, GameSession.tsx, WatchHistoryPage.tsx — but the user may notice a delta on a less-frequented page (Save dialog, Shortlist, etc.)</vector>
    <mitigation>Human-verify checkpoint asks the user to navigate primary game/search/watch paths and confirm; any aesthetic delta is acceptable per CONTEXT D2 Option 1 (NR badge is more honest than blank).</mitigation>
  </risk>
  <risk id="R3" severity="low">
    <name>Nightly cron didn't fire / scheduled job didn't pick up new code</name>
    <vector>If the docker rebuild didn't propagate or APScheduler didn't reload, the next nightly pass may still use the old code</vector>
    <mitigation>The verify checkpoint includes inspecting `docker logs cinemachain-backend` for the `_backfill_mpaa_pass` log line; if it's the old 25k number, the deploy didn't take and the user knows immediately.</mitigation>
  </risk>
  <risk id="R4" severity="medium">
    <name>"Within ≤2 nightly cycles" may take 2 calendar days to verify</name>
    <vector>This phase cannot be marked done until nightly cycles have run, which can lag the deploy by 24-48h depending on schedule</vector>
    <mitigation>The human-verify checkpoint is non-blocking on calendar time — user signals "verified" once they've observed the log line in the natural course of operation. Alternatively, they can manually invoke a one-shot via the existing `/cache/run-now` endpoint (added in earlier phase) to accelerate verification. Plan acknowledges the latency without forcing a specific calendar deadline.</mitigation>
  </risk>
  <risk id="R5" severity="low">
    <name>MDBList 1,459 backlog doesn't close in 2 nights at expected rate</name>
    <vector>If the user is on the free MDBList tier instead of paid, or if MDBList API rate-limits, the gap may close slower</vector>
    <mitigation>The success criterion is "gap closes" not "gap closes within N hours". User has paid tier per Phase 17 setup. If it lags, no code change needed — just acknowledge in SUMMARY.</mitigation>
  </risk>
</risks>

<verification>This plan is verification-only — no code is added or modified. All risk is operational (deploy correctness) not architectural. Rollback is `alembic downgrade -1` + redeploy previous tag.</verification>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Deploy v2.0.2 patch to NAS</name>
  <files></files>
  <read_first>
    - .planning/phases/23-smart-metadata-backfill/23-CONTEXT.md (Decision 5 — auto-only; nothing manual to trigger post-deploy)
    - .planning/phases/23-smart-metadata-backfill/23-01-SUMMARY.md (confirm Plan 23-01 actually shipped its changes)
  </read_first>
  <action>
    This is the deploy step. The user runs the standard Phase-22-style deploy on the NAS.

    Required deploy sequence (Claude documents this in the SUMMARY; user executes on NAS):
    1. `git pull` (on NAS) — fetch the v2.0.2 commit(s) from Plan 23-01
    2. `make rebuild` (on NAS) — rebuild backend container with the modified cache.py + settings.py
    3. The backend container's startup runs `alembic upgrade head` automatically (per existing project convention — verify by checking entrypoint script). If NOT automatic, run `docker exec cinemachain-backend alembic upgrade head` manually.
    4. Confirm migration applied: `docker exec cinemachain-postgres psql -U cinemachain -d cinemachain -c "SELECT version_num FROM alembic_version;"` should return `0020`.
    5. Confirm sentinel migration ran: `docker exec cinemachain-postgres psql -U cinemachain -d cinemachain -c "SELECT COUNT(*) FROM movies WHERE mpaa_rating = '';"` should return `0`.
    6. Confirm 'NR' population: `docker exec cinemachain-postgres psql -U cinemachain -d cinemachain -c "SELECT COUNT(*) FROM movies WHERE mpaa_rating = 'NR';"` should return ~33,000 (the previously-empty rows).

    Claude's role in this task: write the deploy-runbook block into 23-02-SUMMARY.md, capture the exact commands and expected outputs above. Actual execution is on the user's NAS (which Claude does not have shell access to — that's the human-verify gate in Task 2).

    Note: if the existing project entrypoint already runs `alembic upgrade head` on container start (per Phase 22's deploy), no manual alembic call is needed — `make rebuild` does the full job.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-smart-metadata-backfill/23-01-SUMMARY.md &amp;&amp; grep -q "0020" .planning/phases/23-smart-metadata-backfill/23-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/23-smart-metadata-backfill/23-01-SUMMARY.md` exists (Plan 23-01 shipped before this plan runs)
    - 23-01-SUMMARY.md references migration `0020` (Plan 23-01 actually created the migration)
    - Deploy runbook block drafted in 23-02-SUMMARY.md (or in the task action notes — depending on whether SUMMARY exists yet) with all 6 commands listed above
  </acceptance_criteria>
  <done>
    Deploy runbook is documented and ready for the user to execute on the NAS. The next task (human-verify) asks the user to run it and report results.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live NAS verification of all 4 HEALTH-03 conditions</name>
  <what-built>
    All Plan 23-01 changes deployed to NAS:
    - Migration 0020 promoted ~33k `mpaa_rating = ''` rows to `'NR'`
    - `_backfill_mpaa_pass` now writes `'NR'` sentinel and queries `IS NULL` only
    - `/db-health` endpoint returns sentinel-aware addressable-gap counts
    - No frontend file modified (Decision 5) — frontend consumes existing endpoint shape
  </what-built>
  <how-to-verify>
    Run all 6 commands from the Task 1 deploy runbook on the NAS, then verify each of the four HEALTH-03 sub-conditions:

    **(a) `_backfill_mpaa_pass` log shows a small work set (NOT 25k):**
    Within 24h of deploy (or immediately if the user manually triggers the existing nightly via `/cache/run-now`), inspect the backend container logs:
    ```
    docker logs cinemachain-backend 2>&1 | grep "_backfill_mpaa_pass"
    ```
    Expected output: a line of the form `_backfill_mpaa_pass: N movies need MPAA rating` where N is in the low hundreds (single-digit thousands at most). If N is still ~25,000 the fix didn't take — investigate.

    **(b) DB Health view shows realistic addressable gaps:**
    Open the Settings page in the browser, navigate to the DB Health view. Expected values:
    - `Missing MPAA`: ~hundreds (was 33,383)
    - `Missing RT score`: ~1,459 (was 36,695; should now equal `Never MDBList fetched`)
    - `Missing IMDB ID`: ~1,459 (was 2,910)
    - `Missing IMDB rating`: ~1,459 (was 2,903)
    - `Missing overview`: ~1,915 (unchanged — no sentinel for overview)
    - `Never MDBList fetched`: ~1,459 (unchanged)

    The `Missing RT score`, `Missing IMDB ID`, `Missing IMDB rating`, and `Never MDBList fetched` numbers should converge (all reflect the truly-NULL set, which is approximately the never-fetched set).

    **(c) MDBList backlog closes within 1-2 nights:**
    Wait 1-2 nightly cycles. Re-open DB Health. Expected: `Never MDBList fetched` and the three MDBList-derived `Missing X` counts have all dropped toward zero (or to a residual small number of MDBList-404 cases that fail consistently).

    **(d) No frontend regression on MPAA rendering:**
    Navigate the primary user-facing pages and confirm `'NR'` renders identically to how `''` previously rendered:
    - **Game session page** (`/game/...`): movies that previously showed no MPAA badge should still show no badge OR a clean "NR" badge (per `MovieCard.tsx:84` fallback). No broken layout.
    - **Search page** (`/search`): movie cards display same as before. Note: per pre-flight, line 452 (`{movie.mpaa_rating ?? "—"}`) means formerly-empty rows now show "NR" instead of blank in the small-text fallback. This is acceptable per Decision 2 Option 1 (more honest than blank).
    - **Watch History page**: per `WatchHistoryPage.tsx:379` (`splashMovie.mpaa_rating ? ` ${splashMovie.mpaa_rating}` : ""`), formerly-empty splash text now shows " NR" — acceptable visual delta.

    If any of the four conditions fails, do NOT mark the checkpoint approved — report the specific failure and Claude will diagnose.

    Note on calendar latency: condition (a) can be observed immediately via `/cache/run-now` (manual trigger of the nightly job). Condition (c) genuinely requires 1-2 nightly cycles to fully verify; the user may approve this checkpoint based on observed direction (counts dropping) rather than full closure.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all 4 HEALTH-03 conditions met, OR describe specific failures (e.g., "log still shows 25k", "MPAA badge renders wrong on /search", "alembic upgrade failed with X")</resume-signal>
</task>

</tasks>

<verification_criteria>
- Plan 23-01 fully shipped (commits present, 23-01-SUMMARY.md exists)
- NAS deploy executed by user (`make rebuild` + `alembic upgrade head`)
- Migration 0020 confirmed applied (alembic_version = 0020; movies with `mpaa_rating = ''` count = 0)
- `_backfill_mpaa_pass` log line shows small work set (not 25k) — captured in 23-02-SUMMARY.md
- DB Health view shows the expected count drops — captured in 23-02-SUMMARY.md (screenshot or numeric copy)
- MDBList backlog closure observed or trending toward closure
- Frontend MPAA rendering checked on Game / Search / Watch History pages — no regression
- User signed off via `approved` checkpoint resume signal
</verification_criteria>

<success_criteria>
Plan 23-02 is complete when:
- User has executed the deploy runbook on the NAS
- All four HEALTH-03 sub-conditions are verified (a, b, c, d above)
- 23-02-SUMMARY.md captures observed log lines, DB Health numbers (before/after), and frontend check notes
- Checkpoint `Task 2` resumed with `approved`
- Phase 23 is then ready to ship as v2.0.2: tag, push, update STATE.md milestone progress
</success_criteria>

<output>
After completion, create `.planning/phases/23-smart-metadata-backfill/23-02-SUMMARY.md` covering:
- Deploy runbook commands executed (with timestamps)
- Migration 0020 confirmation: alembic_version row + row count for `mpaa_rating = ''` (should be 0) and `mpaa_rating = 'NR'` (should be ~33k)
- Pre-deploy vs post-deploy DB Health numbers (numeric table)
- Observed `_backfill_mpaa_pass` log line (raw log excerpt)
- Frontend rendering check notes (per page)
- Any unexpected deltas or accepted cosmetic changes
- Recommendation to tag v2.0.2 and push to origin
</output>
