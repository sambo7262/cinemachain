---
phase: 09-navigation-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/GameSession.tsx
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "Archiving a session from GameSession redirects to /game, not /"
    - "Clicking End Session redirects to /game, not /"
    - "No navigate('/') call remains in GameSession.tsx"
  artifacts:
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "Corrected post-archive navigation targets"
      contains: "navigate(\"/game\")"
  key_links:
    - from: "archiveMutation.onSuccess"
      to: "/game"
      via: "navigate('/game')"
      pattern: "navigate\\(\"/game\"\\)"
    - from: "End Session button onClick"
      to: "/game"
      via: "api.archiveSession.then(() => navigate('/game'))"
      pattern: "navigate\\(\"/game\"\\)"
---

<objective>
Fix the two `navigate("/")` calls in GameSession.tsx so they redirect to `/game` after archiving a session. This is a correctness fix — without it, users land on a blank page (or flash through the catch-all) after ending or archiving a session.

Purpose: Plan 02 removes the `/` route. These two call sites are the only places in the codebase that navigate to `/` programmatically. They must be updated before the route change goes live.
Output: GameSession.tsx with both navigate targets updated to "/game".
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- GameSession.tsx — the two call sites (verified by codebase grep) -->

Call site 1 (line ~363) — archiveMutation.onSuccess callback:
```tsx
const archiveMutation = useMutation({
  mutationFn: () => api.archiveSession(sid),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["activeSessions"] })
    navigate("/")   // <-- CHANGE TO navigate("/game")
  },
})
```

Call site 2 (line ~714) — End Session button inline handler:
```tsx
<Button
  variant="destructive"
  onClick={() => {
    api.archiveSession(sid).then(() => navigate("/"))   // <-- CHANGE TO navigate("/game")
  }}
>
  End Session
</Button>
```

These are the ONLY two navigate("/") calls in the entire codebase (confirmed by grep during research).
No other files need updating for this change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace both navigate("/") with navigate("/game") in GameSession.tsx</name>
  <files>frontend/src/pages/GameSession.tsx</files>
  <action>
Make exactly two line changes in GameSession.tsx:

Change 1 — line ~363, inside archiveMutation.onSuccess:
  FROM: `navigate("/")`
  TO:   `navigate("/game")`

Change 2 — line ~714, inside the End Session button onClick handler:
  FROM: `api.archiveSession(sid).then(() => navigate("/"))`
  TO:   `api.archiveSession(sid).then(() => navigate("/game"))`

These are surgical single-character changes (adding "game" to the path string).

Do NOT change anything else in this file — no restructuring, no refactoring, no changes to the archiveMutation logic, the button rendering, or any other navigate calls.

After editing, verify no `navigate("/")` remains in the file. The only navigate calls should be `navigate("/game")` (these two) and `navigate(\`/game/${...}\`)` (the session-specific routes, which are correct).
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain/frontend && npx tsc --noEmit 2>&1 | head -20 && grep -n 'navigate("/")' src/pages/GameSession.tsx</automated>
  </verify>
  <done>
- TypeScript compiles without errors
- `grep 'navigate("/")' GameSession.tsx` returns no output (zero matches)
- `grep 'navigate("/game")' GameSession.tsx` returns exactly two matches (lines ~363 and ~714)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- `grep -n 'navigate("/")' frontend/src/pages/GameSession.tsx` → no output
- `grep -n 'navigate("/game")' frontend/src/pages/GameSession.tsx` → 2 matches
</verification>

<success_criteria>
Both navigate("/") calls in GameSession.tsx are updated to navigate("/game"). TypeScript compiles clean. No other changes to the file.
</success_criteria>

<output>
After completion, create `.planning/phases/09-navigation-redesign/09-04-SUMMARY.md`
</output>
