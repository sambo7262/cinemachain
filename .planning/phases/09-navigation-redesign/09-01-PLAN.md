---
phase: 09-navigation-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/NavBar.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-03

must_haves:
  truths:
    - "NavBar always shows exactly three items: Game Mode, Search, Settings"
    - "Game Mode link is active when on /game or inside /game/:sessionId"
    - "Search link is active when on /search"
    - "Settings remains icon-only with no persistent active highlight"
    - "Logo links to /game, not /"
  artifacts:
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Three-item nav with correct active-state logic"
      contains: "isGameModeActive"
  key_links:
    - from: "NavBar.tsx"
      to: "/game"
      via: "Logo Link to prop"
      pattern: "to=\"/game\""
    - from: "NavBar.tsx"
      to: "/search"
      via: "Search Link element"
      pattern: "to=\"/search\""
---

<objective>
Rewrite NavBar.tsx to replace the current Sessions/Archived two-item layout with the permanent three-item nav: Game Mode (/game) | Search (/search) | Settings icon (/settings).

Purpose: This is the user-visible entry point for every page. Getting the labels, routing targets, and active-state logic right here unblocks all other navigation work.
Output: Updated NavBar.tsx with correct active-state logic, logo link fix, and the three permanent items.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current NavBar.tsx — full replacement target -->
<!-- File: frontend/src/components/NavBar.tsx -->

Current active-state variables:
  const isSessionsActive = location.pathname === "/" || location.pathname.startsWith("/game/")
  const isArchivedActive = location.pathname === "/archived"

Current nav items:
  <Link to="/">Sessions</Link>
  <Link to="/archived">Archived</Link>
  <Link to="/settings" aria-label="Settings"><SettingsIcon /></Link>

Logo currently links to: to="/"

Existing CSS pattern for active item:
  isActive ? "bg-accent text-accent-foreground"
           : "text-muted-foreground hover:text-foreground hover:bg-accent/50"

Settings icon uses a different pattern (no active bg, ever):
  className="min-w-[44px] min-h-[44px] flex items-center justify-center text-muted-foreground hover:text-foreground transition-colors"

Imports already present: Link, useLocation, Film, Settings as SettingsIcon, cn
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NavBar with Game Mode / Search / Settings items</name>
  <files>frontend/src/components/NavBar.tsx</files>
  <action>
Replace the contents of NavBar.tsx with the updated three-item nav. Specific changes (per D-01 through D-04 from CONTEXT.md):

1. Remove `isArchivedActive` variable entirely.
2. Replace `isSessionsActive` with:
   `const isGameModeActive = location.pathname === "/game" || location.pathname.startsWith("/game/")`
3. Add:
   `const isSearchActive = location.pathname === "/search"`
4. Change the logo `<Link to="/">` to `<Link to="/game">` — prevents logo click going to a no-route path.
5. Replace the Sessions link (currently `to="/"`) with:
   `<Link to="/game" className={cn(..., isGameModeActive ? "bg-accent text-accent-foreground" : "text-muted-foreground hover:text-foreground hover:bg-accent/50")}>Game Mode</Link>`
6. Replace the Archived link with:
   `<Link to="/search" className={cn(..., isSearchActive ? "bg-accent text-accent-foreground" : "text-muted-foreground hover:text-foreground hover:bg-accent/50")}>Search</Link>`
7. Keep the Settings icon link exactly as-is — same className, same aria-label, no active state class applied (existing pattern: icon-only buttons don't show persistent active bg).

Do NOT add any animations, transitions beyond what already exists, or structural layout changes to the nav container.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- NavBar.tsx compiles without TypeScript errors
- File contains `isGameModeActive`, `isSearchActive`, no `isArchivedActive`
- Logo `to` prop is `/game`
- Three link elements: one to `/game` labeled "Game Mode", one to `/search` labeled "Search", one to `/settings` (icon)
- Active class applied to Game Mode when on /game or /game/*; to Search when on /search; Settings has no active class ever
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- NavBar.tsx: grep confirms `to="/game"` (logo), `isGameModeActive`, `isSearchActive`, text "Game Mode", text "Search"
- NavBar.tsx: grep confirms NO `isArchivedActive`, NO `to="/"`, NO text "Sessions", NO text "Archived"
</verification>

<success_criteria>
NavBar renders three permanent items — Game Mode, Search, Settings — with correct active-state logic. TypeScript compiles clean. No changes to any other file.
</success_criteria>

<output>
After completion, create `.planning/phases/09-navigation-redesign/09-01-SUMMARY.md`
</output>
