---
phase: 07-production-deployment
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - README.md
autonomous: false
requirements: [PROD-02, PROD-03]

must_haves:
  truths:
    - "A user unfamiliar with CinemaChain can follow the README to deploy it"
    - "README covers prerequisites, setup steps, env var reference, and troubleshooting"
    - "Security review confirms no hardcoded credentials in source code or Docker images"
    - "No debug endpoints or leaked secrets exist in the codebase"
    - "Docker Hub repos are public — docker pull succeeds without authentication"
  artifacts:
    - path: "README.md"
      provides: "Complete deployment guide"
      min_lines: 100
      contains: "docker compose up"
    - path: "README.md"
      provides: "Architecture overview"
      contains: "PostgreSQL"
    - path: "README.md"
      provides: "Troubleshooting section"
      contains: "Troubleshooting"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Variable reference matches .env.example contents"
      pattern: "TMDB_API_KEY|RADARR_URL|DB_PASSWORD"
    - from: "README.md"
      to: "compose.yaml"
      via: "Setup steps reference compose commands"
      pattern: "docker compose"
---

<objective>
Write a comprehensive README.md, perform a full security audit of the codebase, and verify Docker Hub repos are publicly pullable. The README enables any Docker Compose user to deploy CinemaChain. The security audit confirms no credentials are baked into images or hardcoded in source.

Purpose: Complete the production-readiness of CinemaChain for public sharing.
Output: README.md at repo root; security audit findings documented in task verification.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-production-deployment/07-CONTEXT.md
@.planning/phases/07-production-deployment/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security audit — grep for hardcoded credentials and verify Docker safety</name>
  <read_first>
    - backend/Dockerfile
    - frontend/Dockerfile
    - .gitignore
    - .env.example
    - compose.yaml
  </read_first>
  <files>
    (no files modified — audit only; findings feed into Task 2 README and may produce fixes if issues found)
  </files>
  <action>
Run the following security checks and document results. If any check FAILS, fix the issue before proceeding.

**Check 1: Hardcoded credentials in source code**
```bash
grep -rn "522b85eb\|iD1FozfudrS\|01ac4dd5\|316ff836\|192\.168\.86\.\|changeme" --include="*.py" --include="*.ts" --include="*.tsx" --include="*.yaml" --include="*.yml" --include="*.json" backend/ frontend/src/ compose.yaml
```
Expected: No matches. If found in any file other than test fixtures with obviously fake data, replace with placeholder or env var reference.

**Check 2: .env not committed to git**
```bash
git ls-files .env
```
Expected: Empty output (file not tracked).

**Check 3: .gitignore coverage**
Verify `.gitignore` contains `.env` (line-level match, not substring of another pattern).

**Check 4: Docker images don't bake secrets**
- `backend/Dockerfile`: Confirm no `ENV` directives setting API keys or tokens. Confirm no `COPY .env .` line. The existing Dockerfile copies source with `COPY . .` but the `.dockerignore` or comment confirms `.env` is excluded. If no `.dockerignore` exists, create one with `.env` entry.
- `frontend/Dockerfile`: Multi-stage build — confirm no secrets in build args or ENV. Frontend has no secrets (static SPA served by nginx).

**Check 5: Port bindings**
Verify in compose.yaml:
- Backend: `8111:8000` on all interfaces (correct — needed for LAN/Tailscale access)
- Frontend: `3111:80` on all interfaces (correct — same reason)
- Postgres: `127.0.0.1:5433:5432` (correct — localhost only)

**Check 6: No debug endpoints**
```bash
grep -rn "debug\|/test\|/admin" --include="*.py" backend/app/routers/ | grep -i "route\|router\|app\.\(get\|post\|put\|delete\)"
```
Expected: No unprotected debug/test/admin endpoints.

**If any check fails:** Fix the issue in the relevant file. If a `.dockerignore` needs to be created for the backend, create `backend/.dockerignore` with:
```
.env
.env.*
__pycache__
*.pyc
.pytest_cache
.coverage
```
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects && echo "=== Check 1: Hardcoded creds ===" && grep -rn "522b85eb\|iD1FozfudrS\|01ac4dd5\|316ff836" --include="*.py" --include="*.ts" --include="*.tsx" --include="*.yaml" backend/ frontend/src/ compose.yaml 2>/dev/null | wc -l | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL" && echo "=== Check 2: .env not tracked ===" && test -z "$(git ls-files .env)" && echo "PASS" || echo "FAIL" && echo "=== Check 3: .gitignore ===" && grep -q "^\.env$" .gitignore && echo "PASS" || echo "FAIL" && echo "=== Check 4: Postgres port ===" && grep -q "127.0.0.1:5433:5432" compose.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "522b85eb\|iD1FozfudrS\|01ac4dd5\|316ff836" --include="*.py" --include="*.ts" --include="*.yaml" backend/ frontend/src/ compose.yaml` returns no matches
    - `git ls-files .env` returns empty
    - `.gitignore` contains the line `.env`
    - `compose.yaml` contains `127.0.0.1:5433:5432` for postgres
    - `backend/Dockerfile` does NOT contain `ENV TMDB_API_KEY` or `ENV PLEX_TOKEN` or `COPY .env`
    - If `backend/.dockerignore` was missing, it now exists and contains `.env`
  </acceptance_criteria>
  <done>Security audit passes all 6 checks. No hardcoded credentials in source. No secrets in Docker images. Postgres not exposed to network. .env properly gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive README.md</name>
  <read_first>
    - .env.example
    - compose.yaml
    - backend/Dockerfile
    - frontend/Dockerfile
  </read_first>
  <files>README.md</files>
  <action>
Create `README.md` at the repo root. The README must be written for someone who knows Docker but may not know the *arr stack. It should contain these sections in this order:

**Section 1: Title and description**
```markdown
# CinemaChain

A chain-based movie discovery engine. Navigate cinema through shared actors — pick a movie, choose an actor from its cast, explore their filmography, pick your next movie, and keep the chain going. Never repeat an actor within a session.

Built for home media enthusiasts running Radarr. When you find a movie you want to watch, CinemaChain sends it straight to your Radarr download queue.
```

**Section 2: Features** (brief bullet list)
- Actor-chain Movie Game — discover movies through shared cast connections
- Multi-session support with naming, archiving, CSV export/import
- Radarr integration — request movies directly from the game
- Nightly TMDB cache — pre-fetches popular movie and actor data for fast browsing
- Responsive UI — works on desktop, tablet, and phone
- Docker Compose deployment — single command setup

**Section 3: Prerequisites**
- Docker and Docker Compose (v2)
- A TMDB account (free) for API access
- Radarr running and accessible on your network (for movie requests)

**Section 4: Quick Start**
Step-by-step numbered list:
1. Clone the repository: `git clone https://github.com/sambo7262/cinemachain.git && cd cinemachain`
2. Copy the environment template: `cp .env.example .env`
3. Edit `.env` with your values — at minimum: `DB_PASSWORD`, `TMDB_API_KEY`, `RADARR_URL`, `RADARR_API_KEY`
4. Start the stack: `docker compose up -d`
5. Open the UI: `http://localhost:3111`
6. (First run) Configure any additional settings in the in-app Settings page

Include a note: "First startup may take a few minutes as the database initializes and the TMDB cache begins populating."

**Section 5: Getting API Keys**
Two subsections:

*TMDB:*
1. Create a free account at themoviedb.org
2. Go to Settings > API > Request an API Key
3. Copy the "API Key (v3 auth)" value
4. Set `TMDB_API_KEY` in your .env

*Radarr:*
1. Open your Radarr web UI
2. Go to Settings > General > Security
3. Copy the API Key
4. Set `RADARR_URL` (e.g., `http://192.168.1.100:7878`) and `RADARR_API_KEY` in your .env

**Section 6: Environment Variables**
A markdown table with columns: Variable | Required | Default | Description

Include ALL variables from the final .env.example:
| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| `DB_PASSWORD` | Yes | — | PostgreSQL password for the cinemachain database user |
| `TMDB_API_KEY` | Yes | — | TMDB API key (v3) for movie and actor metadata |
| `RADARR_URL` | Yes | — | URL of your Radarr instance (e.g., `http://192.168.1.100:7878`) |
| `RADARR_API_KEY` | Yes | — | Radarr API key for sending download requests |
| `DATA_DIR` | No | `./data` | Host path for all volume bind-mounts |
| `PUID` | No | `1000` | User ID for file ownership in containers |
| `PGID` | No | `1000` | Group ID for file ownership in containers |
| `RADARR_QUALITY_PROFILE` | No | `HD+` | Radarr quality profile name for requested movies |
| `TMDB_CACHE_TIME` | No | `03:00` | Time (UTC, 24h) to run the nightly TMDB cache refresh |
| `TMDB_CACHE_TOP_N` | No | `5000` | Number of top-voted movies to pre-cache nightly |
| `TMDB_CACHE_TOP_ACTORS` | No | `1500` | Number of popular actors to pre-warm in the nightly cache |
| `TMDB_CACHE_RUN_ON_STARTUP` | No | `false` | Run cache refresh on app startup (useful for first deploy) |
| `SETTINGS_ENCRYPTION_KEY` | No | (empty) | Fernet key for encrypting DB-stored settings (see .env.example for generation command) |

**Section 7: Architecture**
A simple ASCII diagram or prose description:
```
                    +-------------------+
                    |    Frontend       |
                    |  (React + Nginx)  |
                    |   :3111 -> :80    |
                    +--------+----------+
                             |
                             v
                    +-------------------+
                    |    Backend        |
                    | (FastAPI + Uvicorn)|
                    |   :8111 -> :8000  |
                    +--------+----------+
                             |
                +------------+------------+
                |                         |
                v                         v
      +-------------------+    +--------------------+
      |   PostgreSQL      |    |   External APIs    |
      |   :5433 (local)   |    |  TMDB / Radarr     |
      +-------------------+    +--------------------+
```

Brief prose: "The frontend is a React SPA served by Nginx. It communicates with the FastAPI backend, which manages game sessions, caches TMDB data in PostgreSQL, and forwards movie requests to Radarr."

**Section 8: Troubleshooting**
Common issues with solutions:

1. **Database not starting** — Check `docker compose logs postgres`. Ensure `DB_PASSWORD` is set in `.env`. If changing the password after first run, delete the postgres data directory (`$DATA_DIR/postgres`).

2. **Radarr requests failing** — Verify `RADARR_URL` is reachable from the Docker host (not `localhost` — use the LAN IP). Check `RADARR_API_KEY` is correct. Backend and Radarr must be on the same network or have routable connectivity.

3. **TMDB data not loading** — Confirm `TMDB_API_KEY` is valid. Check backend logs: `docker compose logs backend`. On first startup, set `TMDB_CACHE_RUN_ON_STARTUP=true` to populate the cache immediately.

4. **Permission errors on volumes** — Ensure `PUID` and `PGID` match the user running Docker. On Linux: `id $(whoami)`. On Synology NAS: `id your_docker_username` via SSH.

5. **Port conflicts** — Backend uses 8111, frontend uses 3111, Postgres uses 5433. If any conflict, edit `compose.yaml` ports section (change only the left/host side).

**Section 9: NAS Users (Synology, UNRAID, etc.)**
Brief note: "The default configuration works on any Docker host. NAS users may want to override `DATA_DIR` to use a persistent storage path (e.g., `DATA_DIR=/volume1/docker/appdata/cinemachain` on Synology). PUID/PGID should match your NAS Docker user."

**Section 10: License** (if applicable — check if LICENSE file exists; if not, omit this section)

Do NOT include:
- Plex integration instructions (removed)
- Sonarr integration instructions (removed)
- Development/contribution guidelines (not needed for v1)
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects && test -f README.md && echo "PASS: README exists" || echo "FAIL" && wc -l README.md | awk '{print ($1 >= 100) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}' && grep -q "docker compose up" README.md && echo "PASS: setup command present" || echo "FAIL" && grep -q "TMDB_API_KEY" README.md && echo "PASS: TMDB_API_KEY documented" || echo "FAIL" && grep -q "RADARR_URL" README.md && echo "PASS: RADARR_URL documented" || echo "FAIL" && grep -q "TMDB_CACHE_TOP_ACTORS" README.md && echo "PASS: TMDB_CACHE_TOP_ACTORS documented" || echo "FAIL" && grep -q "Troubleshooting" README.md && echo "PASS: troubleshooting section" || echo "FAIL" && grep -q "DATA_DIR" README.md && echo "PASS: DATA_DIR documented" || echo "FAIL" && grep -c "PLEX_TOKEN\|SONARR_URL\|plex\|sonarr" README.md | xargs -I{} test {} -eq 0 && echo "PASS: no Plex/Sonarr refs" || echo "WARN: Plex/Sonarr mentioned"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root with at least 100 lines
    - `grep -q "docker compose up" README.md` succeeds
    - `grep -q "## Quick Start" README.md` succeeds (or similar heading)
    - `grep -q "TMDB_API_KEY" README.md` succeeds
    - `grep -q "RADARR_URL" README.md` succeeds
    - `grep -q "DB_PASSWORD" README.md` succeeds
    - `grep -q "DATA_DIR" README.md` succeeds
    - `grep -q "TMDB_CACHE_TOP_ACTORS" README.md` succeeds
    - `grep -q "SETTINGS_ENCRYPTION_KEY" README.md` succeeds
    - `grep -q "Troubleshooting" README.md` succeeds
    - `grep -q "Architecture" README.md` succeeds
    - README does NOT mention Plex or Sonarr in setup instructions
  </acceptance_criteria>
  <done>README.md committed at repo root. Covers: what CinemaChain is, prerequisites, step-by-step setup, API key instructions, full env var reference (including TMDB_CACHE_TOP_ACTORS), architecture overview, and troubleshooting. A Docker Compose user can follow it to deploy without prior knowledge of the project.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Docker Hub repos are publicly pullable</name>
  <what-built>Plans 07-01 and 07-02 prepare CinemaChain for public distribution. A locked decision requires both Docker Hub repos to be marked public so users can pull images without authentication. This checkpoint verifies that.</what-built>
  <how-to-verify>
    1. Run: `docker pull sambo7262/cinemachain-backend:latest` (should succeed without Docker Hub login)
    2. Run: `docker pull sambo7262/cinemachain-frontend:latest` (should succeed without Docker Hub login)
    3. If either fails with "access denied" or "authentication required":
       - Go to https://hub.docker.com/repository/docker/sambo7262/cinemachain-backend/settings
       - Go to https://hub.docker.com/repository/docker/sambo7262/cinemachain-frontend/settings
       - Change Visibility to "Public" for both repos
    4. Re-run the pull commands to confirm success after making repos public
  </how-to-verify>
  <resume-signal>Type "approved" once both `docker pull` commands succeed without authentication, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `README.md` exists and has 100+ lines
2. All env vars from .env.example are documented in the README variable reference table
3. Security audit passed (no hardcoded creds, .env gitignored, Postgres localhost-only, no secrets in images)
4. No Plex or Sonarr references in setup instructions
5. `docker pull sambo7262/cinemachain-backend:latest` succeeds without authentication
6. `docker pull sambo7262/cinemachain-frontend:latest` succeeds without authentication
</verification>

<success_criteria>
- README.md enables a new user to deploy CinemaChain from scratch using only Docker Compose
- Security audit confirms the repo is safe to make public
- Docker Hub repos are publicly accessible (pull without login)
- All PROD-01, PROD-02, PROD-03 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-deployment/07-02-SUMMARY.md`
</output>
