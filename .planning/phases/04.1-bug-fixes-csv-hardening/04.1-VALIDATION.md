---
phase: 4.1
slug: bug-fixes-csv-hardening
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-18
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) / manual browser test (frontend) |
| **Config file** | `backend/pytest.ini` or `backend/setup.cfg` (if exists) |
| **Quick run command** | `cd backend && python -m pytest tests/ -x -q` |
| **Full suite command** | `cd backend && python -m pytest tests/ -v` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && python -m pytest tests/ -x -q`
- **After every plan wave:** Run `cd backend && python -m pytest tests/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 4.1-BUG01 | 01 | 1 | BUG-01 | unit | `cd /Users/Oreo/Projects/backend && python -m pytest tests/test_game.py::test_eligible_movies_excludes_chain_movies -x -q` | ✅ | ⬜ pending |
| 4.1-BUG03 | 01 | 1 | BUG-03 | unit | `cd /Users/Oreo/Projects/backend && python -m pytest tests/test_game.py::test_suggestions_long_chain_fallback -x -q` | ✅ | ⬜ pending |
| 4.1-BUG02 | 01 | 1 | BUG-02 | tsc + grep | `grep -n "splitCsvLine" /Users/Oreo/Projects/frontend/src/pages/GameLobby.tsx && cd /Users/Oreo/Projects/frontend && npx tsc --noEmit 2>&1 \| head -5` | ✅ | ⬜ pending |
| 4.1-DEPLOY | 02 | 2 | all | deploy | `docker compose ps 2>&1 \| grep -E "(backend\|frontend\|db)"` | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Note: test_suggestions_long_chain_fallback (BUG-03) is written in Plan 01 Task 1 but only runs GREEN
> after Plan 01 Task 2 applies the early-return fix. Task 1 verify runs only the BUG-01 test;
> Task 2 verify runs both BUG-01 and BUG-03 together.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CSV import with quoted comma-in-title | BUG-02 | Frontend UI interaction | Upload `test_comma.csv` with `"Knives Out, 2019"` as a title; verify chain loads correctly |
| Chain movie appears in eligible picks | BUG-01 | Requires NAS deploy + running session | Start session, advance chain, verify CSV-imported movie no longer appears as suggestion |
| Suggestions appear on long chain (10+ steps) | BUG-03 | Requires running session with long history | Create 10+ step chain, verify suggestions panel returns results |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify commands
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] No Wave 0 stubs needed — tests added directly to tests/test_game.py
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
