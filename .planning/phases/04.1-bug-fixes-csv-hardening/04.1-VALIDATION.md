---
phase: 4.1
slug: bug-fixes-csv-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) / manual browser test (frontend) |
| **Config file** | `backend/pytest.ini` or `backend/setup.cfg` (if exists) |
| **Quick run command** | `cd backend && python -m pytest tests/ -x -q` |
| **Full suite command** | `cd backend && python -m pytest tests/ -v` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && python -m pytest tests/ -x -q`
- **After every plan wave:** Run `cd backend && python -m pytest tests/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 4.1-BUG01 | 01 | 1 | BUG-01 | deploy | `docker compose up --build -d` | ✅ | ⬜ pending |
| 4.1-BUG02 | 01 | 1 | BUG-02 | unit | `cd backend && python -m pytest tests/test_csv_parser.py -v` | ❌ W0 | ⬜ pending |
| 4.1-BUG03 | 01 | 1 | BUG-03 | unit | `cd backend && python -m pytest tests/test_suggestions.py -v` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/test_csv_parser.py` — stubs for BUG-02 (comma-in-title CSV parsing)
- [ ] `backend/tests/test_suggestions.py` — stubs for BUG-03 (empty eligible_actors fallback)

*Frontend CSV parsing fix (BUG-02) requires manual browser verification — upload a CSV with a quoted comma-in-title field and confirm correct import.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CSV import with quoted comma-in-title | BUG-02 | Frontend UI interaction | Upload `test_comma.csv` with `"Knives Out, 2019"` as a title; verify chain loads correctly |
| Chain movie appears in eligible picks | BUG-01 | Requires NAS deploy + running session | Start session, advance chain, verify CSV-imported movie no longer appears as suggestion |
| Suggestions appear on long chain (10+ steps) | BUG-03 | Requires running session with long history | Create 10+ step chain, verify suggestions panel returns results |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
