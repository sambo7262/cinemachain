---
phase: 04.1-bug-fixes-csv-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "NAS deployment is running with 04-08, 04-09, and all Phase 4.1 code changes active"
    - "CSV-imported session chain movies do not appear in the Eligible Movies tab"
    - "Suggestions tab returns results on a session with 10+ steps (long chain)"
    - "CSV import with a quoted comma-in-title field succeeds (no truncation or column shift)"
  artifacts:
    - path: "backend/app/routers/game.py"
      provides: "Deployed version includes 04-09 chain exclusion + 04.1-01 BUG-03 early-return fix"
    - path: "frontend/src/pages/GameLobby.tsx"
      provides: "Deployed version includes RFC 4180 CSV parser and accept='.csv,.xlsx'"
  key_links:
    - from: "NAS Docker images"
      to: "game.py get_suggestions"
      via: "Docker rebuild + docker compose up -d on NAS"
      pattern: "docker compose"
    - from: "NAS Docker images"
      to: "frontend parseCSV"
      via: "Docker rebuild + docker compose up -d on NAS"
      pattern: "docker compose"
---

<objective>
Deploy all Phase 4.1 code changes (04-08, 04-09, and 04.1-01 fixes) to the NAS via Docker rebuild, then
human-verify all three bugs are resolved in the live environment.

Purpose: Phase 4.1 complete gate — all three known bugs verified FIXED on the production NAS before
Phase 5 (Production Deployment) begins.
Output: NAS running fixed code; human verification confirms BUG-01, BUG-02, BUG-03 resolved.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.1-bug-fixes-csv-hardening/04.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker rebuild and NAS deploy</name>
  <files>docker-compose.yaml, Makefile</files>
  <read_first>
    - docker-compose.yaml (verify service names and image tags before rebuild)
    - Makefile (verify rebuild target — uses `--no-cache` per 03-11 decision)
  </read_first>
  <action>
  Run a Docker rebuild of both backend and frontend images, then deploy to the NAS.

  Follow the established rebuild pattern from 03-11 (force `--no-cache` to avoid stale image cache):

  ```bash
  # From project root on the NAS (or local machine if images pushed to registry)
  make rebuild
  # OR if no Makefile target:
  docker compose build --no-cache backend frontend
  docker compose up -d
  ```

  Verify containers are healthy after deploy:
  ```bash
  docker compose ps
  # All services should show "Up" or "running" status
  ```

  Wait for backend health check to pass:
  ```bash
  curl http://localhost:8000/health
  # Expected: {"status":"ok"} or similar 200 response
  ```

  If Alembic migrations are pending, run them before verifying:
  ```bash
  docker compose exec backend alembic upgrade head
  ```
  (No new migrations expected in Phase 4.1 — all bug fixes are code-only changes.)
  </action>
  <verify>
    <automated>docker compose ps 2>&1 | grep -E "(backend|frontend|db)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows backend, frontend, and db containers all in "Up" or "running" state
    - `curl http://localhost:8000/health` returns HTTP 200
    - No container shows "Restarting" or "Exited" status
  </acceptance_criteria>
  <done>All containers healthy; NAS running Phase 4.1 code</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — BUG-01, BUG-02, BUG-03 resolved on live NAS</name>
  <what-built>
  Docker rebuild deployed with:
  - BUG-01 fix (04-09): CSV chain movies excluded from Eligible Movies via chain_movie_ids set
  - BUG-02 fix (04.1-01): Frontend RFC 4180 CSV parser (quoted comma-in-title fields)
  - BUG-03 fix (04.1-01): Suggestions genre-affinity fallback reachable when all actors exhausted
  </what-built>
  <how-to-verify>
  **Test BUG-01: CSV chain movie eligibility**
  1. Open the app and navigate to GameLobby
  2. Import a CSV with 3+ rows (use the app's own Export CSV from an existing session, or create manually)
     - The exported CSV format: order, movie_name, actor_name
     - Example: `1,Fight Club,\n2,Se7en,Brad Pitt\n3,Seven Years in Tibet,Brad Pitt`
  3. After the session imports successfully, navigate to the session
  4. On the Eligible Movies tab: verify that movies already in the chain (Fight Club, Se7en, Seven Years in Tibet)
     do NOT appear in the list — even with "All Movies" toggle on
  5. PASS: chain movies absent from eligible list. FAIL: any chain movie appears as selectable or visible.

  **Test BUG-02: CSV comma-in-title parsing**
  1. Create a test CSV file on your computer with a comma in a movie title:
     ```
     order,movie_name,actor_name
     1,"Knives Out, 2019",
     2,"Beauty and the Beast, The",Daniel Craig
     ```
  2. Import this CSV in GameLobby
  3. PASS: both rows parsed correctly (movie names shown as "Knives Out, 2019" and "Beauty and the Beast, The"
     in the validation report — not truncated at the comma or split into wrong columns)
  4. FAIL: movie name shows as "Knives Out" (truncated) or "2019" appears as the actor name

  **Test BUG-03: Suggestions on long chains**
  1. Navigate to any active session with 5+ steps (or start a new one and advance it)
  2. Mark the current movie as watched, then open the Suggested tab
  3. PASS: suggestions appear (even if chain is shorter than 130 steps, verify the tab is not empty
     after a few picks — if it was previously returning empty on your test sessions, confirm it now populates)
  4. For a thorough test: find or create a session where the current movie has only 1 or 2 cast members
     and both have been picked — suggestions should still return genre-affinity fallback results

  **Regression check**
  5. Confirm normal gameplay still works: pick an actor → see their movies → select a movie → chain advances
  </how-to-verify>
  <read_first>
    - .planning/STATE.md (Known Bugs section for expected outcomes)
  </read_first>
  <resume-signal>
  Type one of:
  - "all pass" — all three bugs confirmed resolved; Phase 4.1 complete
  - "BUG-01 fail: [description]" — chain movies still appearing
  - "BUG-02 fail: [description]" — CSV comma parsing still broken
  - "BUG-03 fail: [description]" — suggestions still empty
  - "partial: [details]" — some pass, some fail
  </resume-signal>
</task>

</tasks>

<verification>
All three bugs confirmed FIXED on live NAS:
- BUG-01: CSV chain movies absent from Eligible Movies tab
- BUG-02: CSV with "Knives Out, 2019" imports with correct title
- BUG-03: Suggestions tab non-empty on sessions where current movie actors are exhausted
</verification>

<success_criteria>
- NAS containers all healthy after rebuild
- BUG-01 PASS: chain movies not in eligible list after CSV import
- BUG-02 PASS: comma-in-title CSV parses with full title intact
- BUG-03 PASS: suggestions returns results on long/exhausted chains
- Normal gameplay unaffected (regression check pass)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-bug-fixes-csv-hardening/04.1-02-SUMMARY.md`
</output>
