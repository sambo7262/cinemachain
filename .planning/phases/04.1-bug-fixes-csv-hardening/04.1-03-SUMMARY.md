---
phase: 04.1-bug-fixes-csv-hardening
plan: "03"
subsystem: api
tags: [fastapi, game, suggestions, fallback, pytest]

# Dependency graph
requires:
  - phase: 04.1-02
    provides: BUG-03 initial code fix deployed; regression test for long-chain fallback added
provides:
  - get_suggestions fallback gate unconditional (no genre_freq guard)
  - genre_score > 0 filter removed from fallback loop
  - zero-watch-history test variants covering full BUG-03 failure surface
affects: [phase-5-production-deployment, get_suggestions]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fallback block fires whenever top5 < 5 regardless of watch history state"
    - "Zero-score candidates included in fallback; sorted last by vote_average"

key-files:
  created: []
  modified:
    - backend/app/routers/game.py
    - backend/tests/test_game.py

key-decisions:
  - "BUG-03 root cause confirmed: two independent conditions in get_suggestions fallback — `and genre_freq` gate and `if genre_score > 0:` filter — both prevented fallback on zero-watch-history chains"
  - "Fallback now unconditional on top5 length; genre_freq still used for scoring (emptiness means all scores=0, sorted last)"
  - "genre_score=0 movies are valid suggestions when no better options exist; sort key (genre_score desc, vote_average desc) already handles ordering correctly"

patterns-established:
  - "BUG-03 regression tests use asyncpg-skip pattern (skip locally, GREEN in Docker) per existing project convention"

requirements-completed: []

# Metrics
duration: 10min
completed: 2026-03-18
---

# Phase 04.1 Plan 03: BUG-03 Fallback Gate Fix Summary

**BUG-03 closed: removed `and genre_freq` gate and `if genre_score > 0:` filter from get_suggestions fallback, plus two new regression test variants covering zero-watch-history failure paths**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-18T17:40:00Z
- **Completed:** 2026-03-18T17:50:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Removed `and genre_freq` from fallback gate (line 1764) — fallback now fires whenever top5 < 5, regardless of whether WatchEvents exist
- Removed `if genre_score > 0:` filter from fallback loop (line 1783) — zero-scored movies now included and ranked last by vote_average
- Added `test_suggestions_no_watch_history`: zero WatchEvents, genres populated on chain movies
- Added `test_suggestions_no_watch_history_null_genres`: zero WatchEvents AND chain movie genres=NULL (the exact NAS failure scenario)
- Full backend test suite: 20 passed, 59 skipped, 0 failures

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix get_suggestions fallback gate and genre_score filter in game.py** - `543a2e5` (fix)
2. **Task 2: Add zero-watch-history test variant to test_game.py** - `fb7a0c5` (test)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `backend/app/routers/game.py` - Two targeted edits: fallback gate simplified to `if len(top5) < 5:`, genre_score filter removed
- `backend/tests/test_game.py` - Two new test functions appended after existing test_suggestions_long_chain_fallback

## Decisions Made

- BUG-03 root cause was two independent bugs, not one: the gate prevented fallback entry when genre_freq was empty, and the filter prevented any candidates from being added even if the gate was somehow bypassed
- Zero-score fallback candidates are valid suggestions — they represent movies from the full DB that happen to have no genre overlap with the user's history; sorted last so genre-matched movies still appear first when available
- Both new test variants use tmdb_ids in the 99020-99027 and 88020-88021 ranges to avoid collisions with existing test data

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Both edits were surgical single-line changes. Tests skip locally per established asyncpg pattern.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- BUG-03 is now fully fixed at the code level; the NAS failure path (long chain, zero WatchEvents, genres=NULL) is covered by `test_suggestions_no_watch_history_null_genres`
- Docker rebuild and NAS deploy required to verify all three BUG-03 variants GREEN in production
- Phase 5 Production Deployment is unblocked pending NAS verification

---
*Phase: 04.1-bug-fixes-csv-hardening*
*Completed: 2026-03-18*

## Self-Check: PASSED

- SUMMARY.md: FOUND
- game.py: FOUND
- test_game.py: FOUND
- commit 543a2e5: FOUND
- commit fb7a0c5: FOUND
