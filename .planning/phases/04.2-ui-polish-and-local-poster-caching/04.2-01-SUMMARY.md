---
phase: 04.2-ui-polish-and-local-poster-caching
plan: "01"
subsystem: testing

tags: [vitest, pytest, tdd, stubs]

# Dependency graph
requires: []
provides:
  - backend/tests/test_static.py StaticFiles smoke-test stub
  - backend/tests/test_game.py Phase 4.2 stubs (poster-wall endpoint, step_count session counter)
  - frontend/src/components/__tests__/PosterWall.test.tsx render stubs (pass via try/catch)
  - frontend/src/components/__tests__/SessionCounters.test.tsx RED stubs for new props
affects:
  - 04.2-02 (StaticFiles mount — test_static.py becomes GREEN)
  - 04.2-04 (PosterWall component — PosterWall.test.tsx becomes GREEN)
  - 04.2-05 (SessionCounters props — SessionCounters.test.tsx becomes GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dynamic import with module-level variable const PATH = '...' prevents Vite/Vitest build-time import resolution failure for stubs referencing components that don't exist yet"
    - "Non-async def test_ functions in pytest skip cleanly via conftest client fixture ImportError path"

key-files:
  created:
    - backend/tests/test_static.py
    - frontend/src/components/__tests__/PosterWall.test.tsx
    - frontend/src/components/__tests__/SessionCounters.test.tsx
  modified:
    - backend/tests/test_game.py

key-decisions:
  - "PosterWall.test.tsx uses const POSTER_WALL_PATH = '../PosterWall' variable to prevent Vite build-time static import resolution failure; @vite-ignore comment alone insufficient when path is a string literal"
  - "SessionCounters.test.tsx is intentionally RED — references stepCount, uniqueActorCount, createdAt props that don't exist until Plan 05; failure is expected behavior for TDD RED phase"

patterns-established:
  - "Stub pattern for non-existent components: variable path + @vite-ignore + try/catch + expect(true).toBe(true) on failure"

requirements-completed: []

# Metrics
duration: 8min
completed: 2026-03-17
---

# Phase 4.2 Plan 01: Wave 0 Test Stubs Summary

**Five Wave 0 test stubs created across backend (test_static.py, test_game.py) and frontend (PosterWall.test.tsx, SessionCounters.test.tsx) to anchor TDD for Phase 4.2 implementation plans**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-17T23:00:00Z
- **Completed:** 2026-03-17T23:08:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created backend/tests/test_static.py with StaticFiles mount smoke test (skips until Plan 02 adds mount)
- Appended two Phase 4.2 stubs to test_game.py: poster-wall endpoint list check, step_count session counter skip
- Created frontend PosterWall.test.tsx with try/catch stubs that pass even when component is absent
- Created frontend SessionCounters.test.tsx as RED TDD stubs for stepCount, uniqueActorCount, createdAt props

## Task Commits

Each task was committed atomically:

1. **Task 1: Backend test stubs** - `2851c4f` (test)
2. **Task 2: Frontend test stubs** - `418bf11` (test)

## Files Created/Modified
- `backend/tests/test_static.py` - New: StaticFiles mount smoke test stub
- `backend/tests/test_game.py` - Appended: two Phase 4.2 stubs at end of file
- `frontend/src/components/__tests__/PosterWall.test.tsx` - New: render stubs using variable-path dynamic import
- `frontend/src/components/__tests__/SessionCounters.test.tsx` - New: RED stubs for new stat props

## Decisions Made
- Used `const POSTER_WALL_PATH = "../PosterWall"` variable instead of string literal in dynamic import — Vite's import analysis resolves string literals at build time even inside try/catch blocks; variable prevents static analysis and lets the try/catch handle the runtime failure
- SessionCounters RED failures are intentional and accepted per plan — they mark the TDD RED phase for Plan 05

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] PosterWall.test.tsx dynamic import caused build-time failure despite try/catch**
- **Found during:** Task 2 (Frontend test stubs)
- **Issue:** `await import("../PosterWall")` inside try/catch failed at Vite build/transform time — Vite's import analysis resolves static string paths before runtime, causing the entire test file to fail to load
- **Fix:** Extracted path to module-level const variable (`const POSTER_WALL_PATH = "../PosterWall"`); variable path bypasses Vite's static analysis while `/* @vite-ignore */` comment provides belt-and-suspenders suppression
- **Files modified:** frontend/src/components/__tests__/PosterWall.test.tsx
- **Verification:** `npm test -- --run` shows PosterWall.test.tsx PASS (2 tests pass via try/catch)
- **Committed in:** 418bf11 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** Necessary fix for the test file to load at all. No scope creep.

## Issues Encountered
- Vitest/Vite performs build-time static import analysis even for dynamic imports with string literals inside try/catch; the fix (variable path) is a known pattern for stubs referencing non-existent modules.

## Next Phase Readiness
- All Wave 0 test stubs in place; backend suite is green (38 skipped, 0 failures)
- Frontend: PosterWall tests pass (2/2); SessionCounters has 1 expected RED failure (1/2 pass)
- Plan 02 can begin implementation of StaticFiles mount — test_static.py will activate
- Plan 04 can implement PosterWall component — PosterWall.test.tsx will activate
- Plan 05 can add SessionCounters props — SessionCounters.test.tsx RED tests will turn GREEN

---
*Phase: 04.2-ui-polish-and-local-poster-caching*
*Completed: 2026-03-17*
