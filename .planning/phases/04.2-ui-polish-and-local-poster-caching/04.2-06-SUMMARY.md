---
phase: 04.2-ui-polish-and-local-poster-caching
plan: "06"
subsystem: infra
tags: [docker, compose, volumes, nas, deploy, verification]

# Dependency graph
requires:
  - phase: 04.2-05
    provides: All Phase 4.2 frontend features (PosterWall, SessionCounters, random pick, dead-end, GameLobby stats)
provides:
  - compose.yaml static posters volume bind-mount for poster persistence across container restarts
  - NAS deploy and human verification of all Phase 4.2 features
affects: [phase-5-production-deployment]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Host bind-mount to /volume1/docker/appdata/cinemachain/ for NAS data persistence (established pattern)"

key-files:
  created: []
  modified:
    - compose.yaml

key-decisions:
  - "04.2-06 verification: poster wall (5a) FAILED — no posters visible after 6-movie chain on NAS; deferred to bug smash"
  - "04.2-06 verification: random pick button (5c) FAILED — button present but permanently disabled (enabled state never clears); deferred to bug smash"
  - "04.2-06 verification: Started stat (5b) showed -420min ago — timezone/offset calculation bug; flagged for bug smashing"
  - "04.2-06 verification: GameLobby stats (5e) display but need refinement — flagged for bug smashing"
  - "04.2-06 verification: Dead-end detection (5d) WAIVED — hard to test, low-risk; marked passed"

patterns-established: []

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-03-17
---

# Phase 4.2 Plan 06: NAS Deploy and Verification Summary

**compose.yaml updated with static posters volume bind-mount; NAS deploy verified with partial Phase 4.2 feature pass (2 fails, 1 waived, 2 bugs deferred to bug smashing)**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-17
- **Completed:** 2026-03-17
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added `/volume1/docker/appdata/cinemachain/static:/app/static` volume bind-mount to backend service in compose.yaml — posters written by nightly job now persist across container restarts
- NAS deployed and human verification checkpoint completed with results recorded
- Identified 2 failing features (poster wall, random pick button) and 2 bugs (Started timezone, GameLobby stats refinement) for bug smashing

## Task Commits

Each task was committed atomically:

1. **Task 1: Add static posters volume to compose.yaml** - `0be4d43` (chore)
2. **Task 2: Human verification** — No commit (verification result only)

**Plan metadata:** (docs commit — this summary)

## Files Created/Modified

- `compose.yaml` - Added static posters volume bind-mount under backend service volumes

## Decisions Made

- Static posters volume follows established NAS bind-mount convention (`/volume1/docker/appdata/cinemachain/` prefix) — no named Docker volumes used, consistent with existing data volume
- Verification results accepted as-is; failing items (5a, 5c) and bugs (5b, 5e) deferred to bug smashing phase rather than blocking plan completion

## Verification Outcomes

| Check | Description | Result |
|-------|-------------|--------|
| 5a | Poster wall animated background | FAIL — No posters visible after 6-movie chain |
| 5b | Session stats "Started" value | PASS with bug — Shows "-420min ago" (timezone offset error) |
| 5c | Random Pick button | FAIL — Button present but permanently disabled |
| 5d | Dead-end detection | WAIVED — Hard to test, low-risk |
| 5e | GameLobby session stats | PASS with refinement needed |

**Overall:** 2 clean passes (treating waived as pass), 2 fails, 2 bugs flagged for bug smashing.

## Deviations from Plan

None — plan executed as written. Verification results are partial; failures captured here as gaps, not as execution deviations.

## Issues Encountered

### Known bugs discovered during verification (deferred to bug smashing)

**Bug 1 — Poster wall not displaying (5a FAIL)**
- No posters were visible on the session page or app home page after creating a 6-movie chain on the NAS
- Likely causes: poster download pass has not run yet (nightly job), `/app/static/posters/` directory empty, or posterUrl path construction issue in frontend
- Deferred to bug smashing

**Bug 2 — Random Pick button permanently disabled (5c FAIL)**
- Button is rendered but stays in disabled state; enabled condition never clears
- Likely cause: the `filteredMovies.length > 0` (or equivalent) guard evaluating false even when eligible movies are present
- Deferred to bug smashing

**Bug 3 — "Started" stat shows negative time (5b PASS with bug)**
- "Started" value showed "-420min ago" on initial load — timezone offset calculation is off (likely UTC vs local offset not applied correctly in `formatSessionAge`)
- Flagged for bug smashing

**Bug 4 — GameLobby stats need refinement (5e PASS with refinement needed)**
- Stats display but visual or content refinement required
- Flagged for bug smashing

## User Setup Required

NAS deploy steps (performed by user):
- `make rebuild` on dev machine to build arm64 images
- `docker compose pull && docker compose up -d` on NAS
- Alembic migration 0006 (poster_local_path column) runs automatically via compose.yaml command

## Next Phase Readiness

- compose.yaml is deployment-ready with static posters volume
- Phase 4.2 infrastructure (migration 0006, StaticFiles mount, poster-wall endpoint, nightly download pass) is deployed on NAS
- 4 items require bug smashing before Phase 4.2 can be declared fully verified:
  1. Poster wall not displaying (root cause: empty posters dir or path bug)
  2. Random Pick button disabled state never clears
  3. Started stat timezone offset calculation
  4. GameLobby stats display refinement

---
*Phase: 04.2-ui-polish-and-local-poster-caching*
*Completed: 2026-03-17*
