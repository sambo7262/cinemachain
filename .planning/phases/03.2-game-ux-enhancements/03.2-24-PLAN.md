---
phase: 03.2-game-ux-enhancements
plan: "24"
type: execute
wave: 2
depends_on:
  - "03.2-20"
  - "03.2-21"
  - "03.2-22"
  - "03.2-23"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "All 7 tests from 03.2-19 pass (Tests 1–7)"
    - "Test 3 PASS: Mark as Watched button appears after Back to Sessions → re-enter"
    - "Test 2 PASS: Combined-view Eligible Movies loads in under 5 seconds with no 504"
    - "Test 7 PASS: On-demand fallback returns full cast (not just 3 actors)"
    - "Cross-session actor conflict resolved: Session A picks do not block Session B actors"
    - "CSV import session shows correct watched_count and watched_runtime_minutes"
    - "Concession messages rotate every ~3 seconds"
    - "Empty-state text does not flash before movies load"
  artifacts:
    - path: "sambo7262/cinemachain-backend:latest"
      provides: "Docker image with plans 03.2-20 + 03.2-21 + 03.2-23 backend fixes"
      contains: "deployed to NAS"
    - path: "sambo7262/cinemachain-frontend:latest"
      provides: "Docker image with plan 03.2-22 frontend fixes"
      contains: "deployed to NAS"
  key_links:
    - from: "NAS Docker containers"
      to: "Live game at Tailscale IP"
      via: "docker compose pull && docker compose up -d on NAS"
      pattern: "containers healthy"
---

<objective>
Rebuild Docker images with fixes from plans 03.2-20 through 03.2-23; push to Docker Hub; deploy to NAS; run human verification of all round 4 failures plus regression checks for all prior round tests.

Purpose: Plans 03.2-20 (batch actor credits fetch), 03.2-21 (combined-view TMDB skip), 03.2-22 (Mark as Watched staleTime fix + minor UX), and 03.2-23 (CSV WatchEvent for prior steps) are code changes that require a Docker rebuild and NAS deploy before they can be verified on live hardware.

Output: Updated Docker images deployed to NAS; human verification confirms all round 3 and round 4 issues resolved.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-game-ux-enhancements/03.2-20-SUMMARY.md
@.planning/phases/03.2-game-ux-enhancements/03.2-21-SUMMARY.md
@.planning/phases/03.2-game-ux-enhancements/03.2-22-SUMMARY.md
@.planning/phases/03.2-game-ux-enhancements/03.2-23-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests, rebuild Docker images, push to Hub</name>
  <files></files>
  <read_first>
    - Makefile (confirm rebuild target syntax if it exists; otherwise use direct docker build commands)
  </read_first>
  <action>
**Step 1 — Backend tests:**
```bash
cd /Users/Oreo/Projects && python3 -m pytest backend/tests/ -q 2>&1 | tail -5
```
Expected: no new failures beyond the pre-existing TS_AUTHKEY test. If new failures appear, STOP and investigate before rebuilding.

**Step 2 — Frontend TypeScript check:**
```bash
cd /Users/Oreo/Projects/frontend && npx tsc --noEmit 2>&1 | tail -5
```
Expected: exits 0. If TypeScript errors appear, fix them before proceeding.

**Step 3 — Rebuild backend image (no-cache):**
```bash
cd /Users/Oreo/Projects && docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend
```

**Step 4 — Rebuild frontend image (no-cache):**
```bash
cd /Users/Oreo/Projects && docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend
```

**Step 5 — Push both images:**
```bash
docker push sambo7262/cinemachain-backend:latest
docker push sambo7262/cinemachain-frontend:latest
```

Note: `docker compose up -d` will fail locally (synobridge network only on NAS). This is expected.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects && python3 -m pytest backend/tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/ -q` shows no NEW failures beyond pre-existing TS_AUTHKEY failure
    - `cd frontend && npx tsc --noEmit` exits 0
    - `docker images sambo7262/cinemachain-backend:latest` shows CREATED timestamp is today
    - `docker images sambo7262/cinemachain-frontend:latest` shows CREATED timestamp is today
    - Both `docker push` commands complete without error (no auth failures, no digest errors)
  </acceptance_criteria>
  <done>Both Docker images rebuilt with --no-cache and pushed to Docker Hub. Backend tests and TypeScript compilation confirmed clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Round 4 fixes deployed to NAS via Docker Hub pull:

**03.2-20 fix:** On-demand actor fallback rate-limit. New `_ensure_movie_cast_in_db` helper fetches ALL movie credits in a single TMDB `/movie/{id}/credits` call and batch-inserts them. Replaces the previous per-actor loop that triggered TMDB rate-limiting after ~3 actors.

**03.2-21 fix:** Combined-view 504 timeout. `_ensure_movie_details_in_db` and `_fetch_mpaa_rating` are now guarded behind `if actor_id is not None` — they only run in the actor-scoped path. Combined-view (no actor selected) returns DB-cached data immediately with no TMDB calls.

**03.2-22 fix:** Mark as Watched after remount. Session query now has `staleTime: 0` forcing the in-flight refetch to overwrite cached state before rendering. Empty-state text suppressed while `isFetching` is true. Concession messages now rotate every 3 seconds (was 2s).

**03.2-23 fix:** CSV import session counters. `import_csv_session` now creates `WatchEvent` records (source="csv_import") for all prior movie steps so `watched_count` and `watched_runtime_minutes` reflect the full chain history immediately after import.
  </what-built>
  <how-to-verify>
**Step 0: Deploy on NAS**
```bash
docker compose pull && docker compose up -d
```
Wait for both containers healthy.

---

**Test 1 — Cross-session watched contamination (round 3, must still pass)**

1. Find or create Session A with at least one watched movie.
2. Create a NEW Session B with a different starting movie.
3. In Session B: mark starting movie as watched, click "Continue the chain."
4. Open Eligible Movies tab (no actor selected).

PASS: Movies watched in Session A appear as selectable in Session B (not grayed out).
FAIL: Movies from Session A appear grayed out/non-selectable in Session B.

---

**Test 2 — Combined-view Eligible Movies loads fast (round 3 + round 4)**

1. Open an active session (current movie marked as watched).
2. Click "Continue the chain" → enter tabs view.
3. WITHOUT selecting an actor, click "Eligible Movies" tab.
4. Time from tab click to movies appearing.

PASS: Concession message appears within 1 second; movies load within 5 seconds; no 504 error.
FAIL: Loading takes more than 10 seconds, or nginx 504 appears.

Also verify: type a partial title in the search box — matching movies filter immediately.

---

**Test 3 — Mark as Watched after navigate-away and return (round 3 + round 4)**

1. Start a session with a current movie (not yet watched).
2. Mark the current movie as watched → "Continue the chain" → select actor → select movie → confirm. UI returns to Session Home showing the new movie.
3. Navigate away: click "Back to Sessions" (or NavBar).
4. Navigate back into the same session from the sessions list.

PASS: Session Home shows the "Mark as Watched" button for the new current movie within 2 seconds of loading. Button is enabled and reads "Mark as Watched".
FAIL: No Mark as Watched button appears; or the session shows "Continue the chain" instead (meaning it still thinks the old movie is watched).

---

**Test 4 — Concession loading messages (round 3, must still pass)**

Verify in three contexts:
- 4a: Eligible Actors tab loading → rotating concession message appears if fetch > 1s
- 4b: Eligible Movies tab (no actor) → concession message during combined-view fetch
- 4c: CSV import validation → concession message while validation runs

PASS: At least one concession message visible in each context; messages rotate approximately every 3 seconds (not 2s).
FAIL: Generic spinner only, or messages rotate too fast (sub-2s).

---

**Test 5 — CSV direct TMDB ID input (round 3, must still pass)**

1. Upload a CSV with at least one ambiguous row.
2. Validation report appears with flagged rows.

PASS: Each flagged row shows a "Or enter TMDB ID directly:" input field. Entering a valid TMDB ID resolves the row.
FAIL: No input field appears.

---

**Test 6 — CSV import last row is in-progress (round 3, must still pass)**

1. Import a CSV with 3+ rows.
2. Land on /game/:id after import.

PASS: Session Home shows the last CSV row's movie with a "Mark as Watched" button. Movie A and B appear in Chain History.
FAIL: Last row shown as already watched, or no Mark as Watched button.

---

**Test 7 — On-demand fallback returns full cast (round 4, NEW)**

This test requires a session where the background credits pre-fetch has NOT yet completed (i.e., a freshly created session before the background task finishes, or a session with a movie not yet in DB).

1. Create a brand-new session with a popular movie (e.g., search "Inception" or "The Martian").
2. Mark the starting movie as watched immediately (within a few seconds of session creation, before background pre-fetch completes).
3. Click "Continue the chain" → open "Eligible Actors" tab.

PASS: More than 5 actors appear in the Eligible Actors tab. (If background pre-fetch already completed, all actors show — still a pass.) The tab does NOT get stuck with only 3 actors.
FAIL: Exactly 3 actors appear and no more load.

---

**Test 7b — On-demand fallback: no 504 on Eligible Movies tab (round 4, NEW)**

1. Using the same new session from Test 7 (or a fresh session).
2. After marking starting movie as watched, click "Continue the chain."
3. WITHOUT selecting an actor, click "Eligible Movies" tab.

PASS: Movies appear within 5 seconds. No 504 error.
FAIL: 504 nginx timeout after ~60 seconds.

---

**Test 8 — CSV import session counters (round 4, NEW)**

1. Import a CSV with 4 rows: Movie A → Actor X → Movie B → Actor Y → Movie C → Actor Z → Movie D.
2. After import, land on /game/:id.
3. Observe the session counters in the header (Movies Watched and Total Runtime).

PASS: Session counters show 3 movies watched (A, B, C — all rows except the current in-progress Movie D) and a non-zero total runtime (sum of runtimes for A, B, C — may show 0h 0m only if runtime data is missing from DB for those movies, which is acceptable for a first import).
FAIL: Session counters show 0 movies watched even though the imported chain has prior completed movies.

---

**Test 9 — Empty-state suppression during fetch (round 4, NEW)**

1. Open any active session and navigate to the Eligible Movies tab (no actor selected).
2. While movies are loading (the concession message is rotating), observe the area below it.

PASS: No "No eligible movies found for this session." text appears while the fetch is in progress.
FAIL: Empty-state text flashes briefly before movies appear.

---

**Regression check — South Park chain (from round 2)**

The Martian → Matt Damon → Good Will Hunting → Minnie Driver → South Park: Bigger Longer Uncut chain returns eligible actors. At least one actor shown after South Park.

PASS: Eligible actors load for South Park: Bigger Longer Uncut.
FAIL: Zero actors returned.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 9 tests + regression check pass.
If any test fails, describe: which test number, what you observed, loading times, button states, actor counts, error messages.
  </resume-signal>
</task>

</tasks>

<verification>
Human verify is the primary gate. All 9 tests + regression check must pass for Phase 03.2 round 4 gap closure to be considered complete.
</verification>

<success_criteria>
- Test 1 PASS: Cross-session contamination fixed (no regression)
- Test 2 PASS: Combined-view under 5 seconds, no 504
- Test 3 PASS: Mark as Watched visible after navigate-away and return
- Test 4 PASS: Concession messages in all three loading contexts (~3s rotation)
- Test 5 PASS: CSV direct TMDB ID input resolves flagged rows
- Test 6 PASS: CSV last row is in-progress with Mark as Watched button
- Test 7 PASS: On-demand fallback returns full cast (not just 3)
- Test 7b PASS: Eligible Movies tab no 504 on on-demand fallback path
- Test 8 PASS: CSV import session counters show prior movies watched
- Test 9 PASS: Empty-state text does not flash during fetch
- Regression PASS: South Park chain eligible actors load
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-game-ux-enhancements/03.2-24-SUMMARY.md`
</output>
