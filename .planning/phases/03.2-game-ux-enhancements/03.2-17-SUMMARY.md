---
plan: 03.2-17
phase: "03.2"
status: complete
tasks_complete: 1/1
---

# Plan 03.2-17 Summary — Session Home Refetch Fix

## What Was Built

Added `refetchOnMount: "always"` to the session `useQuery` in `GameSession.tsx`.

## Commits

- `a60ce2d`: fix(03.2-17): add refetchOnMount: "always" to session query

## Task Results

| Task | Status | Notes |
|------|--------|-------|
| Task 1: Add refetchOnMount: "always" | ✓ Complete | Single option added to session useQuery at line 53 |

## Root Cause Closed

On remount (after Back to Sessions → re-enter session), React Query served stale cached session data with `current_movie_watched: true` from the prior state. With `refetchOnMount: "always"`, a fresh GET /session/{id} fires on every mount, ensuring the Mark as Watched button render condition (`status === "active" && !isWatched`) evaluates against live server data.

## Self-Check: PASSED
