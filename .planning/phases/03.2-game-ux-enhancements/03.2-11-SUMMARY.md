---
phase: 03.2-game-ux-enhancements
plan: "11"
subsystem: ui
tags: [react, react-query, tanstack-query, frontend, game-session]

# Dependency graph
requires:
  - phase: 03.2-game-ux-enhancements
    provides: backend combined-view eligible-movies endpoint (actor_id=None returns all eligible movies)
provides:
  - Eligible Movies tab fetches all eligible movies on tab open without actor selection
  - queryKey null-stabilization preventing stale data on actor change
  - 1-second delayed loading spinner for slow combined-view fetches
affects: [03.2-game-ux-enhancements, 03-movie-game]

# Tech tracking
tech-stack:
  added: []
  patterns: [isFetching + delayed-spinner pattern (useState + useEffect + 1s timeout), null-stable React Query cache keys for optional params]

key-files:
  created: []
  modified:
    - frontend/src/pages/GameSession.tsx

key-decisions:
  - "selectedActor?.tmdb_id ?? null in queryKey: null vs undefined ensures React Query treats no-actor and actor-present as distinct cache entries; undefined would collapse the two into the same cache key"
  - "enabled condition remains !!sid && !!session && isWatched with no selectedActor requirement; combined-view fires on tab open without actor"
  - "Spinner gated on both showMoviesSpinner AND eligibleMoviesFetching to prevent stale spinner if state diverges"
  - "Empty state updated from Pick-an-actor gate to No-eligible-movies-found: reflects that no-actor now triggers a fetch not an empty state"

patterns-established:
  - "Delayed spinner pattern: useState(false) + useEffect with 1s setTimeout triggered by isFetching; clears immediately on fetch complete"
  - "Null-stable cache keys: use ?? null not ?? undefined for optional params in React Query queryKey arrays"

requirements-completed: [UX-03]

# Metrics
duration: 8min
completed: 2026-03-16
---

# Phase 03.2 Plan 11: No-Actor Eligible Movies and Stale Cache Fix Summary

**Eligible Movies tab now fetches all eligible movies on tab open without actor selection, using null-stable React Query cache keys to prevent stale data on actor switch**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-16T00:00:00Z
- **Completed:** 2026-03-16T00:08:00Z
- **Tasks:** 2 (1 read-only verification, 1 implementation)
- **Files modified:** 1

## Accomplishments
- Gap 3 closed: Eligible Movies tab fires a combined-view fetch immediately on tab open without any actor selected
- Regression 2 fixed: `selectedActor?.tmdb_id ?? null` in queryKey ensures React Query treats "no actor" and "actor 12345" as distinct cache entries — stale actor movies no longer flash when switching actors
- Loading spinner appears after 1 second of fetching to handle slow combined-view responses without flashing on fast responses
- Empty state updated: "No eligible movies found for this session." replaces the now-obsolete "Pick an actor..." gate
- Backend confirmed already correct: combined-view else-branch at game.py line 1065 handles actor_id=None correctly; no backend changes needed

## Task Commits

1. **Task 1: Verify backend combined-view (read-only)** — no commit (no changes; backend already correct)
2. **Task 2: Frontend changes** - `40e404e` (feat)

## Files Created/Modified
- `frontend/src/pages/GameSession.tsx` — queryKey null-stabilization, isFetching destructure, showMoviesSpinner state + useEffect, spinner JSX, updated empty state message

## Decisions Made
- `selectedActor?.tmdb_id ?? null` chosen over `selectedActor?.tmdb_id ?? undefined` because React Query treats `undefined` in queryKey arrays as absent — using `null` creates a distinct cache entry for the no-actor state, preventing cache collision with actor-scoped queries
- Spinner rendered OUTSIDE the `filteredMovies.length === 0` condition so it shows during initial load before any data arrives
- The `enabled` condition was NOT changed — `!!sid && !!session && isWatched` already correctly enables the query without requiring `selectedActor` (this was the root cause of Gap 3)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Gap 3 (UX-03) is now closed: Eligible Movies tab is immediately useful without actor selection
- Regression 2 (stale movie list on actor change) is fixed via null-stable queryKey
- Regression 1 (actor eligibility broken for _ensure_movie_details_in_db movies) remains open per STATE.md and requires a separate investigation plan
- Backend combined-view already handles the no-actor case — no backend deploy needed for this fix

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-16*
