---
phase: 03.2-game-ux-enhancements
plan: "27"
type: execute
wave: 4
depends_on:
  - "03.2-25"
  - "03.2-26"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "CSV import (20+ rows): eligible actors for last movie returns >0 actors"
    - "All uploaded actors excluded from eligible actors after CSV import"
    - "New session after CSV import shows eligible actors correctly (South Park chain works)"
    - "Session home after navigate-away: 'Continue the chain' button visible and navigates to actor tab"
    - "All 9 tests from 03.2-24 still pass (no regression)"
  artifacts:
    - path: "sambo7262/cinemachain-backend:latest"
      provides: "Docker image with 03.2-25 backend fixes"
      contains: "deployed to NAS"
    - path: "sambo7262/cinemachain-frontend:latest"
      provides: "Docker image with 03.2-26 frontend fix"
      contains: "deployed to NAS"
  key_links:
    - from: "NAS Docker containers"
      to: "Live game at Tailscale IP"
      via: "docker compose pull && docker compose up -d on NAS"
      pattern: "containers healthy"
---

<objective>
Rebuild both Docker images incorporating 03.2-25 (backend: Movie stub upsert + BackgroundTasks in CSV import) and 03.2-26 (frontend: Continue the chain dead-state fix), push to Docker Hub, deploy to NAS, and run human verification of the two round 5 failures plus regression checks for all prior passing tests.

Purpose: 03.2-25 and 03.2-26 are code-only changes. They cannot be verified without a Docker rebuild and NAS deploy. This plan activates both fixes and confirms they resolve the failures without introducing new regressions.

Output: Updated Docker images deployed to NAS; human verification confirms round 5 failures resolved and all prior tests still passing.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-game-ux-enhancements/03.2-25-SUMMARY.md
@.planning/phases/03.2-game-ux-enhancements/03.2-26-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests, rebuild Docker images, push to Hub</name>
  <files></files>
  <read_first>
    - Makefile (confirm rebuild target syntax if it exists; otherwise use direct docker build commands)
  </read_first>
  <action>
**Step 1 — Backend tests:**
```bash
cd /Users/Oreo/Projects && python3 -m pytest backend/tests/ -q 2>&1 | tail -10
```
Expected: no new failures beyond the pre-existing TS_AUTHKEY test. If new failures appear, STOP and investigate before rebuilding.

**Step 2 — Frontend TypeScript check:**
```bash
cd /Users/Oreo/Projects/frontend && npx tsc --noEmit 2>&1 | tail -10
```
Expected: exits 0. If TypeScript errors appear, fix them before proceeding.

**Step 3 — Rebuild backend image (no-cache):**
```bash
cd /Users/Oreo/Projects && docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend
```

**Step 4 — Rebuild frontend image (no-cache):**
```bash
cd /Users/Oreo/Projects && docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend
```

**Step 5 — Push both images:**
```bash
docker push sambo7262/cinemachain-backend:latest
docker push sambo7262/cinemachain-frontend:latest
```

Note: `docker compose up -d` will fail locally (synobridge network only on NAS). This is expected.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects && python3 -m pytest backend/tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/ -q` shows no NEW failures beyond pre-existing TS_AUTHKEY failure
    - `cd frontend && npx tsc --noEmit` exits 0
    - `docker images sambo7262/cinemachain-backend:latest` shows CREATED timestamp is today
    - `docker images sambo7262/cinemachain-frontend:latest` shows CREATED timestamp is today
    - Both `docker push` commands complete without error
  </acceptance_criteria>
  <done>Both Docker images rebuilt with --no-cache and pushed to Docker Hub. Backend tests and TypeScript compilation confirmed clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Round 5 fixes deployed to NAS via Docker Hub pull:

**03.2-25 fix A — _ensure_movie_cast_in_db Movie stub upsert:**
Before the cast member loop, `_ensure_movie_cast_in_db` now upserts a minimal Movie row with `on_conflict_do_nothing` so the FK target always exists — even for movies that were CSV-imported as GameSessionSteps without a corresponding `movies` table row. Movie PK is resolved once before the loop (not 20 redundant times inside it).

**03.2-25 fix B — import_csv_session background pre-fetch:**
`import_csv_session` now accepts `BackgroundTasks` as a FastAPI dependency and calls `_prefetch_credits_background(last_movie_id, tmdb)` unconditionally after the WatchEvent commit. This mirrors `create_session` and populates the last movie's cast in the background immediately after import, so the on-demand fallback (which now also has the Movie stub) is a safety net rather than the primary path.

**03.2-26 fix — Session home dead state:**
"Continue the chain" button now renders for BOTH `awaiting_continue` AND `active + isWatched` states. Clicking in the `active + isWatched` state (which exists when the user called `continueChain` successfully then navigated away) navigates directly to the actors tab without a redundant backend call. No more stuck/blank session home.
  </what-built>
  <how-to-verify>
**Step 0: Deploy on NAS**
```bash
docker compose pull && docker compose up -d
```
Wait for both containers healthy.

---

**Test NEW-1 — CSV import: eligible actors after import (round 5, BLOCKER)**

1. Create a new CSV import with 20+ rows (or at minimum 4+ rows covering: Movie A → Actor X → Movie B → Actor Y → Movie C → Actor Z → Movie D).
2. Import the CSV and land on /game/:id.
3. Mark Movie D (the current in-progress movie) as watched.
4. Click "Continue the chain."
5. Open the "Eligible Actors" tab.

PASS: More than 0 actors appear. The actors listed do NOT include Actor X, Actor Y, or Actor Z (they were picked in the uploaded chain and must be excluded).
FAIL: Zero actors appear, or uploaded actors (Actor X, Actor Y, Actor Z) appear in the eligible list.

---

**Test NEW-2 — New session after CSV import: eligible actors unaffected (round 5, BLOCKER)**

1. After completing Test NEW-1, create a brand new session (South Park chain or any popular movie — e.g. "The Martian").
2. Mark the starting movie as watched.
3. Click "Continue the chain."
4. Open the "Eligible Actors" tab.

PASS: Actors appear for the new session's starting movie. Count > 0.
FAIL: Zero actors appear (indicating TMDB rate-limit exhaustion or Movie-not-found failure persists for new sessions).

Note: If the background pre-fetch for the new session fires immediately after CSV import and hits rate-limiting, wait 60 seconds and retry before recording FAIL.

---

**Test NEW-3 — Session home after navigate-away: no dead state (round 5, BLOCKER)**

1. Start or use an existing active session (current movie not watched).
2. Mark the current movie as watched.
3. The button should change to "Continue the chain" (awaiting_continue state).
4. Click "Continue the chain" → verify you enter the tabs view (Eligible Actors tab opens).
5. Navigate away: click "Back to Sessions" (or use the NavBar).
6. Navigate back into the same session from the sessions list.
7. Observe the Session Home page.

PASS: Session Home shows the "Continue the chain" button. Clicking it navigates to the Eligible Actors tab without any backend error.
FAIL: Session Home shows NO button at all (dead state); or clicking "Continue the chain" causes an error.

---

**Regression: Tests 1–9 from 03.2-24 (must all still pass)**

Run each test from the 03.2-24 checkpoint verification as a quick smoke-check:

1. **Test 1 (cross-session watched):** Movies watched in Session A appear selectable in Session B. PASS / FAIL
2. **Test 2 (combined-view speed):** Eligible Movies tab loads within 5 seconds, no 504. PASS / FAIL
3. **Test 3 (Mark as Watched after navigate-away):** Mark as Watched button appears after re-entering session. PASS / FAIL
4. **Test 4 (concession messages):** Rotating messages in all three loading contexts (~3s rotation). PASS / FAIL
5. **Test 5 (CSV direct TMDB ID):** Flagged CSV rows show "Or enter TMDB ID directly:" input. PASS / FAIL
6. **Test 6 (CSV last row in-progress):** Last CSV row shows Mark as Watched, not pre-watched. PASS / FAIL
7. **Test 7 (on-demand fallback full cast):** Fresh session shows >5 actors, not stuck at 3. PASS / FAIL
8. **Test 8 (CSV import session counters):** 4-row CSV import shows 3 movies watched, non-zero runtime in header. PASS / FAIL
9. **Test 9 (empty-state suppression):** No "No eligible movies" flash during fetch. PASS / FAIL

South Park chain regression: The Martian → Matt Damon → Good Will Hunting → Minnie Driver → South Park: Bigger Longer Uncut returns eligible actors. PASS / FAIL
  </how-to-verify>
  <resume-signal>
Type "approved" if Test NEW-1, NEW-2, NEW-3 and all 9 regression tests pass.
If any test fails, describe: which test number, what you observed, actor counts, button states, any console errors.
  </resume-signal>
</task>

</tasks>

<verification>
Human verify is the primary gate. All three new tests (NEW-1, NEW-2, NEW-3) plus all 9 regression tests must pass for Phase 03.2 round 5 gap closure to be considered complete.
</verification>

<success_criteria>
- Test NEW-1 PASS: CSV import (20+ rows) shows >0 eligible actors; all uploaded actors excluded
- Test NEW-2 PASS: New session after CSV import shows eligible actors (no rate-limit bleed-over)
- Test NEW-3 PASS: Session home after navigate-away shows "Continue the chain"; no dead state
- Tests 1–9 PASS: All round 4 verified behaviors still working (no regression)
- South Park chain regression PASS: Eligible actors load for South Park: Bigger Longer Uncut
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-game-ux-enhancements/03.2-27-SUMMARY.md`
</output>
