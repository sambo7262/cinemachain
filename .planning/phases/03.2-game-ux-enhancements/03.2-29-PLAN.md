---
phase: 03.2-game-ux-enhancements
plan: "29"
type: execute
wave: 6
depends_on:
  - "03.2-28"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "South Park chain: The Martian → Matt Damon → Good Will Hunting → Minnie Driver → South Park returns >1 eligible actor"
    - "New session created after CSV import shows eligible actors (TMDB rate limit not exhausted)"
    - "All 9 regression tests from 03.2-27 still pass"
  artifacts:
    - path: "sambo7262/cinemachain-backend:latest"
      provides: "Docker image with 03.2-28 _prefetch_credits_background fix"
      contains: "deployed to NAS"
  key_links:
    - from: "NAS Docker containers"
      to: "Live game at Tailscale IP"
      via: "docker compose pull && docker compose up -d on NAS"
      pattern: "containers healthy"
---

<objective>
Rebuild the backend Docker image with the 03.2-28 _prefetch_credits_background fix, push to Docker Hub, deploy to NAS, and verify the South Park chain regression is resolved without breaking any previously passing tests.

Purpose: 03.2-28 is a backend-only code change. It cannot be verified without a Docker rebuild and NAS deploy. Frontend is unchanged since 03.2-26 — only the backend image needs rebuilding.

Output: Updated backend Docker image deployed to NAS; South Park chain regression confirmed resolved; all prior regression tests still passing.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-game-ux-enhancements/03.2-28-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests, rebuild backend image only, push to Hub</name>
  <files></files>
  <read_first>
    - compose.yaml (confirm backend image name sambo7262/cinemachain-backend:latest)
  </read_first>
  <action>
**Step 1 — Backend tests:**
```bash
cd /Users/Oreo/Projects && python3 -m pytest backend/tests/ -q 2>&1 | tail -10
```
Expected: no new failures beyond the pre-existing TS_AUTHKEY test. If new failures appear, STOP and investigate before rebuilding.

**Step 2 — Rebuild backend image only (no-cache):**
```bash
cd /Users/Oreo/Projects && docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend
```
Frontend image is unchanged since 03.2-26 — do NOT rebuild it.

**Step 3 — Push backend image:**
```bash
docker push sambo7262/cinemachain-backend:latest
```

Note: `docker compose up -d` will fail locally (synobridge network only on NAS). This is expected.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects && python3 -m pytest backend/tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/ -q` shows no NEW failures beyond pre-existing TS_AUTHKEY failure
    - `docker images sambo7262/cinemachain-backend:latest` shows CREATED timestamp is today
    - `docker push sambo7262/cinemachain-backend:latest` completes without error
  </acceptance_criteria>
  <done>Backend Docker image rebuilt with --no-cache and pushed to Docker Hub. Backend tests confirmed clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
03.2-28 fix deployed to NAS via Docker Hub pull (backend only):

**_prefetch_credits_background rewrite:**
Background pre-fetch now calls _ensure_movie_cast_in_db(movie_tmdb_id, tmdb, db) once (1 TMDB call to /movie/{id}/credits) instead of looping over up to 20 cast members and calling _ensure_actor_credits_in_db for each (20 calls to /person/{id}/movie_credits). For South Park cast members like Trey Parker and Matt Stone who have hundreds of film credits, the old path exhausted the TMDB rate limit during session creation. The new path is immune to large-filmography cast members.
  </what-built>
  <how-to-verify>
**Step 0: Deploy on NAS (backend only)**
```bash
docker compose pull && docker compose up -d
```
Wait for backend container healthy. Frontend container will report "up to date" (unchanged).

---

**Test SP-1 — South Park chain regression (BLOCKER)**

1. Start a new session with starting movie: "The Martian"
2. Mark The Martian as watched.
3. Click "Continue the chain." Open the Eligible Actors tab.
4. Select Matt Damon.
5. Select "Good Will Hunting" as the next movie.
6. Mark Good Will Hunting as watched.
7. Click "Continue the chain." Open the Eligible Actors tab.
8. Select Minnie Driver.
9. Select "South Park: Bigger Longer Uncut" as the next movie.
10. Mark South Park as watched.
11. Click "Continue the chain." Open the Eligible Actors tab.

PASS: More than 1 eligible actor appears. (Previously returned only Minnie Driver — she was the sole actor whose filmography was cached before rate limiting hit.)
FAIL: Zero or 1 eligible actor appears (Minnie Driver only) — indicates rate limit still exhausted or fix not deployed.

---

**Test SP-2 — New session after CSV import: eligible actors unaffected**

1. Import a CSV chain (any length, 4+ rows).
2. Immediately after import completes, create a brand new session (e.g. "The Martian").
3. Mark the starting movie as watched.
4. Click "Continue the chain." Open the Eligible Actors tab.

PASS: Actors appear for the new session's starting movie. Count > 0.
FAIL: Zero actors appear (rate limit exhausted by background pre-fetch triggered during CSV import).

Note: Wait 60 seconds after CSV import and retry before recording FAIL.

---

**Regression: All tests from 03.2-27 (must still pass)**

1. **Test NEW-1 (CSV import eligible actors):** CSV import (20+ rows) shows >0 eligible actors; uploaded actors excluded. PASS / FAIL
2. **Test NEW-2 (new session after CSV):** New session after CSV import shows eligible actors. PASS / FAIL
3. **Test NEW-3 (session home dead state):** Session home after navigate-away shows "Continue the chain." PASS / FAIL
4. **Test 1 (cross-session watched):** Movies watched in Session A selectable in Session B. PASS / FAIL
5. **Test 2 (combined-view speed):** Eligible Movies tab loads within 5 seconds. PASS / FAIL
6. **Test 3 (Mark as Watched after navigate-away):** Mark as Watched button appears after re-entering session. PASS / FAIL
7. **Test 4 (concession messages):** Rotating messages in loading contexts. PASS / FAIL
8. **Test 5 (CSV direct TMDB ID):** Flagged rows show TMDB ID input. PASS / FAIL
9. **Test 6 (CSV last row in-progress):** Last CSV row shows Mark as Watched, not pre-watched. PASS / FAIL
10. **Test 7 (on-demand fallback full cast):** Fresh session shows >5 actors. PASS / FAIL
11. **Test 8 (CSV import session counters):** 4-row CSV shows 3 movies watched, non-zero runtime. PASS / FAIL
12. **Test 9 (empty-state suppression):** No "No eligible movies" flash during fetch. PASS / FAIL
  </how-to-verify>
  <resume-signal>
Type "approved" if Test SP-1, SP-2, and all 12 regression tests pass.
If any test fails, describe: which test, what you observed, actor counts, any console errors.
  </resume-signal>
</task>

</tasks>

<verification>
Human verify is the primary gate. SP-1 (South Park >1 actor) and SP-2 (rate limit not exhausted after CSV) plus all 12 regression checks must pass.
</verification>

<success_criteria>
- Test SP-1 PASS: South Park chain returns >1 eligible actor (not just Minnie Driver)
- Test SP-2 PASS: New session after CSV import shows eligible actors (no rate-limit bleed-over)
- All 12 regression tests PASS: No regressions from 03.2-27 verified behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-game-ux-enhancements/03.2-29-SUMMARY.md`
</output>
