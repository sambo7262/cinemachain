---
phase: 03.2-game-ux-enhancements
plan: "02"
subsystem: database
tags: [alembic, sqlalchemy, postgres, migrations, movie-model]

requires:
  - phase: 03.1-ui-improvements-and-multi-session-support
    provides: Alembic migration 0004 (session name + archived_at) that 0005 chains from

provides:
  - Alembic migration 0005 adding vote_count INTEGER and mpaa_rating VARCHAR(10) to movies table
  - Movie ORM model updated with vote_count and mpaa_rating mapped columns

affects:
  - 03.2-03 (vote-floor sort fix uses vote_count)
  - 03.2 plans using MPAA rating display or filtering

tech-stack:
  added: []
  patterns:
    - "Hand-authored Alembic migration pattern: revision chains via down_revision string, nullable columns require no server_default or backfill"
    - "Optional ORM column placement: new nullable columns inserted between existing columns in declaration order matching migration"

key-files:
  created:
    - backend/alembic/versions/20260316_0005_movie_vote_count_mpaa.py
  modified:
    - backend/app/models/__init__.py

key-decisions:
  - "Both columns nullable=True — existing rows correctly represent 'unknown' with no backfill required"
  - "mpaa_rating VARCHAR(10) — covers longest MPAA cert NC-17 plus margin; no index needed (filter/display only, not join key)"
  - "vote_count and mpaa_rating inserted between runtime and fetched_at in Movie model to match logical field grouping"

patterns-established:
  - "Migration 0005 pattern: op.add_column with nullable=True; downgrade drops in reverse order"

requirements-completed: [UX-01, UX-02]

duration: 2min
completed: 2026-03-16
---

# Phase 03.2 Plan 02: Movie Vote Count and MPAA Rating Columns Summary

**Alembic migration 0005 and Movie ORM update adding nullable vote_count (INTEGER) and mpaa_rating (VARCHAR 10) columns as prerequisites for vote-floor sort and MPAA display in Phase 03.2**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-16T04:46:38Z
- **Completed:** 2026-03-16T04:48:44Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created migration 0005 chaining from 0004, adding vote_count and mpaa_rating as nullable columns to the movies table
- Updated Movie ORM model with two new mapped_column declarations placed between runtime and fetched_at
- Verified Python syntax validity of both files and confirmed Movie() instance exposes both new attributes

## Task Commits

Each task was committed atomically:

1. **Task 1: Write Alembic migration 0005** - `6af4520` (feat)
2. **Task 2: Add vote_count and mpaa_rating to Movie ORM model** - `52a5202` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified

- `backend/alembic/versions/20260316_0005_movie_vote_count_mpaa.py` - Migration 0005: adds vote_count INTEGER and mpaa_rating VARCHAR(10) to movies table, downgrade drops both
- `backend/app/models/__init__.py` - Movie ORM class: two new mapped_column declarations added between runtime and fetched_at

## Decisions Made

- Both columns nullable — no backfill required; existing rows represent "unknown" values correctly
- mpaa_rating VARCHAR(10) sufficient for all MPAA rating strings including "NC-17"
- No indexes on new columns — used for display and filtering, not join keys

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Alembic module not installed in local Python environment (runs in Docker). Verification adapted to use Python AST parse for syntax check and direct SQLAlchemy model import check (`from app.models import Movie; hasattr(m, 'vote_count')`), which confirmed both attributes present.

## User Setup Required

None - no external service configuration required. Migration 0005 will be applied automatically on next `alembic upgrade head` (Docker rebuild and NAS deploy).

## Next Phase Readiness

- Migration 0005 exists and is ready to apply against the live database
- Movie ORM model has vote_count and mpaa_rating — Plan 03 (vote-floor sort fix) and MPAA on-demand fetch can proceed
- Docker rebuild + `alembic upgrade head` required before new columns are live in production

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-16*
