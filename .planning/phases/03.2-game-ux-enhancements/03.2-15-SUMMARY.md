---
phase: 03.2-game-ux-enhancements
plan: "15"
subsystem: api
tags: [fastapi, sqlalchemy, game, watch-events, session-scoping]

# Dependency graph
requires:
  - phase: 03.2-game-ux-enhancements
    provides: get_eligible_movies and request_movie endpoints in game.py
provides:
  - Session-scoped watched exclusion in get_eligible_movies
  - Session-scoped watched validation in request_movie
  - Movies watched in Session A remain eligible in Session B
affects: [game-session, eligible-movies, cross-session-eligibility]

# Tech tracking
tech-stack:
  added: []
  patterns: [WatchEvent session-scoping via session.steps intersection (no schema change)]

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "Session-scoping achieved via session.steps tmdb_id intersection rather than WatchEvent.session_id FK column (plan incorrectly assumed column existed; steps intersection avoids schema migration while delivering identical semantics)"
  - "request_movie watched guard only fires when movie is in session steps and has a WatchEvent; movies not yet in any session step bypass the guard entirely"

patterns-established:
  - "Session-scoped WatchEvent query: filter by WatchEvent.tmdb_id.in_(session_movie_ids) where session_movie_ids = [s.movie_tmdb_id for s in session.steps]"

requirements-completed: [UX-01, UX-02, UX-03]

# Metrics
duration: 10min
completed: 2026-03-17
---

# Phase 03.2 Plan 15: Session-Scoped WatchEvent Eligibility Summary

**WatchEvent watched-movie exclusion in get_eligible_movies and request_movie scoped to current session via steps intersection, preventing cross-session contamination**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-17T06:00:00Z
- **Completed:** 2026-03-17T06:10:23Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- `get_eligible_movies` now excludes only movies watched in the current session (not all sessions globally)
- `request_movie` watched validation scoped to current session's step movie IDs
- Movie watched in Session A appears as eligible in Session B's eligible movies list

## Task Commits

Each task was committed atomically:

1. **Task 1: Scope WatchEvent watched exclusion to current session** - `6954135` (fix)

**Plan metadata:** (see final commit below)

## Files Created/Modified
- `backend/app/routers/game.py` - Updated get_eligible_movies and request_movie WatchEvent queries to filter by session.steps movie IDs

## Decisions Made
- Session-scoping via `WatchEvent.tmdb_id.in_(session_movie_ids)` where `session_movie_ids = [s.movie_tmdb_id for s in session.steps]` — avoids adding `session_id` column to WatchEvent (which would require migration + unique constraint change)
- `request_movie` guard wrapped in `if body.movie_tmdb_id in session_movie_ids:` pre-check — avoids DB query entirely for movies not yet in session steps

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] WatchEvent.session_id column does not exist**
- **Found during:** Task 1 (Scope WatchEvent query to current session)
- **Issue:** Plan states "WatchEvent.session_id column already exists (it has been present since Phase 3)" and directs adding `.where(WatchEvent.session_id == session_id)`. However, the WatchEvent model has no `session_id` column — migration 0001 creates watch_events with only id, tmdb_id, movie_id, source, watched_at. The column was never added.
- **Fix:** Achieved identical session-scoping semantics by filtering `WatchEvent.tmdb_id.in_(session_movie_ids)` where `session_movie_ids` is derived from the already-loaded `session.steps` relationship. No schema change required, no migration needed.
- **Files modified:** backend/app/routers/game.py
- **Verification:** `grep -n "WatchEvent.tmdb_id.in_(session_movie_ids)"` returns 2 matches; `grep -n "select(WatchEvent.tmdb_id)$"` returns 0 matches; pytest shows 20 passed, 1 pre-existing failure (test_required_env_vars_documented), same error count as baseline
- **Committed in:** 6954135 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical / plan factual error)
**Impact on plan:** Auto-fix delivers identical functional outcome (session-scoped eligibility) using existing schema. The plan's stated approach would have required a schema migration and unique constraint change — skipped cleanly.

## Issues Encountered
- Plan incorrectly stated `WatchEvent.session_id` column exists. The WatchEvent table was designed as a global watched-state store (from Plex sync heritage), never session-scoped. The `steps intersection` approach is architecturally cleaner and avoids breaking the global Plex-sync watch history.

## Next Phase Readiness
- Cross-session eligibility contamination is fixed; Docker rebuild + NAS deploy required for backend change to go live
- Phase 03.2 gaps and regressions addressed; Phase 4 (Query Mode) ready to start

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-17*
