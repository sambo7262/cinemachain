---
phase: "03.2"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/slider.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/collapsible.tsx
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "Slider, Checkbox, and Collapsible shadcn components exist in frontend/src/components/ui/"
    - "Each component follows the established shadcn/ui manual-write pattern"
    - "Components use Radix UI primitives already bundled in the project"
  artifacts:
    - path: "frontend/src/components/ui/slider.tsx"
      provides: "Radix-based range Slider for runtime filter"
      contains: "SliderPrimitive.Root"
    - path: "frontend/src/components/ui/checkbox.tsx"
      provides: "Radix-based Checkbox for genre and MPAA multi-select"
      contains: "CheckboxPrimitive.Root"
    - path: "frontend/src/components/ui/collapsible.tsx"
      provides: "Radix-based Collapsible for filter sidebar expand/collapse"
      contains: "CollapsiblePrimitive.Root"
  key_links:
    - from: "frontend/src/components/ui/slider.tsx"
      to: "@radix-ui/react-slider"
      via: "import * as SliderPrimitive from '@radix-ui/react-slider'"
      pattern: "radix-ui/react-slider"
    - from: "frontend/src/components/MovieFilterSidebar.tsx"
      to: "frontend/src/components/ui/slider.tsx"
      via: "import { Slider } from '@/components/ui/slider'"
      pattern: "from.*ui/slider"
---

<objective>
Write three missing shadcn/ui component files that the filter sidebar requires: Slider, Checkbox, and Collapsible.
These use Radix UI primitives already bundled via the existing shadcn setup.

Purpose: Plan 05 (frontend features) needs these primitives. Writing them in a separate plan avoids a 5-task plan and keeps context clean.
Output: Three new files in frontend/src/components/ui/.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-game-ux-enhancements/03.2-UI-SPEC.md
@.planning/phases/03.2-game-ux-enhancements/03.2-RESEARCH.md

<interfaces>
<!-- Design system tokens (from UI-SPEC.md): -->
<!-- zinc base, CSS variables, Tailwind v3, darkMode: ["class"] -->
<!-- Existing component pattern — button.tsx (reference for cn() usage): -->
```tsx
// frontend/src/components/ui/button.tsx (representative existing pattern)
import * as React from "react"
import { cva } from "class-variance-authority"
import { cn } from "@/lib/utils"
// ... cva variant definitions ...
// export const Button = React.forwardRef<...>(...)
```

<!-- Existing ui components to check for import pattern: -->
<!-- frontend/src/components/ui/button.tsx, badge.tsx, card.tsx, input.tsx, separator.tsx, tabs.tsx -->

<!-- Confirmed Radix packages available in project (via @radix-ui/* bundled with shadcn): -->
<!-- @radix-ui/react-slider, @radix-ui/react-checkbox, @radix-ui/react-collapsible -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Slider, Checkbox, and Collapsible shadcn components</name>
  <files>
    frontend/src/components/ui/slider.tsx,
    frontend/src/components/ui/checkbox.tsx,
    frontend/src/components/ui/collapsible.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/button.tsx (read to confirm cn() import path and forwardRef pattern used in project)
    - frontend/src/lib/utils.ts (confirm cn() export exists — should be `export function cn(...)`)
    - frontend/package.json (confirm @radix-ui/react-slider, @radix-ui/react-checkbox, @radix-ui/react-collapsible are listed as dependencies)
  </read_first>
  <action>
Read frontend/src/components/ui/button.tsx first to confirm the exact import path for `cn` and the React.forwardRef pattern used in this project. Then create three files.

**File 1: frontend/src/components/ui/slider.tsx**
Standard shadcn Slider component using @radix-ui/react-slider. Two-thumb range support.

```tsx
import * as React from "react"
import * as SliderPrimitive from "@radix-ui/react-slider"

import { cn } from "@/lib/utils"

const Slider = React.forwardRef<
  React.ElementRef<typeof SliderPrimitive.Root>,
  React.ComponentPropsWithoutRef<typeof SliderPrimitive.Root>
>(({ className, ...props }, ref) => (
  <SliderPrimitive.Root
    ref={ref}
    className={cn(
      "relative flex w-full touch-none select-none items-center",
      className
    )}
    {...props}
  >
    <SliderPrimitive.Track className="relative h-2 w-full grow overflow-hidden rounded-full bg-secondary">
      <SliderPrimitive.Range className="absolute h-full bg-primary" />
    </SliderPrimitive.Track>
    {props.defaultValue?.map((_, i) => (
      <SliderPrimitive.Thumb
        key={i}
        className="block h-5 w-5 rounded-full border-2 border-primary bg-background ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50"
      />
    ))}
    {props.value?.map((_, i) => (
      <SliderPrimitive.Thumb
        key={`v-${i}`}
        className="block h-5 w-5 rounded-full border-2 border-primary bg-background ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50"
      />
    ))}
  </SliderPrimitive.Root>
))
Slider.displayName = SliderPrimitive.Root.displayName

export { Slider }
```

Note: The thumb rendering uses both `defaultValue` and `value` to support controlled and uncontrolled modes. When both are undefined (not a real usage), no thumbs render. In practice, callers always pass `value` (controlled) for the range filter.

**File 2: frontend/src/components/ui/checkbox.tsx**
Standard shadcn Checkbox component using @radix-ui/react-checkbox.

```tsx
import * as React from "react"
import * as CheckboxPrimitive from "@radix-ui/react-checkbox"
import { Check } from "lucide-react"

import { cn } from "@/lib/utils"

const Checkbox = React.forwardRef<
  React.ElementRef<typeof CheckboxPrimitive.Root>,
  React.ComponentPropsWithoutRef<typeof CheckboxPrimitive.Root>
>(({ className, ...props }, ref) => (
  <CheckboxPrimitive.Root
    ref={ref}
    className={cn(
      "peer h-4 w-4 shrink-0 rounded-sm border border-primary ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground",
      className
    )}
    {...props}
  >
    <CheckboxPrimitive.Indicator
      className={cn("flex items-center justify-center text-current")}
    >
      <Check className="h-4 w-4" />
    </CheckboxPrimitive.Indicator>
  </CheckboxPrimitive.Root>
))
Checkbox.displayName = CheckboxPrimitive.Root.displayName

export { Checkbox }
```

**File 3: frontend/src/components/ui/collapsible.tsx**
Standard shadcn Collapsible — thin re-export of Radix primitives with display name.

```tsx
import * as CollapsiblePrimitive from "@radix-ui/react-collapsible"

const Collapsible = CollapsiblePrimitive.Root

const CollapsibleTrigger = CollapsiblePrimitive.CollapsibleTrigger

const CollapsibleContent = CollapsiblePrimitive.CollapsibleContent

export { Collapsible, CollapsibleTrigger, CollapsibleContent }
```

After writing all three files, verify that the `cn` import path used (`@/lib/utils`) matches the path used in the existing button.tsx. If it differs, adjust to match exactly.

Also verify that `lucide-react` is available (it is — confirmed by UI-SPEC.md: "Icon library: lucide-react"). The `Check` icon import in checkbox.tsx will work.
  </action>
  <verify>
    <automated>ls /Users/Oreo/Projects/frontend/src/components/ui/slider.tsx /Users/Oreo/Projects/frontend/src/components/ui/checkbox.tsx /Users/Oreo/Projects/frontend/src/components/ui/collapsible.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/slider.tsx` exists and contains `SliderPrimitive.Root`
    - `frontend/src/components/ui/slider.tsx` exports `Slider`
    - `frontend/src/components/ui/checkbox.tsx` exists and contains `CheckboxPrimitive.Root`
    - `frontend/src/components/ui/checkbox.tsx` exports `Checkbox`
    - `frontend/src/components/ui/collapsible.tsx` exists and contains `CollapsiblePrimitive.Root`
    - `frontend/src/components/ui/collapsible.tsx` exports `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`
    - All three files use `import { cn } from "@/lib/utils"` (or exact path confirmed from button.tsx)
    - `grep -r "SliderPrimitive" /Users/Oreo/Projects/frontend/src/components/ui/slider.tsx` returns content
    - `grep -r "CheckboxPrimitive" /Users/Oreo/Projects/frontend/src/components/ui/checkbox.tsx` returns content
    - `grep -r "CollapsiblePrimitive" /Users/Oreo/Projects/frontend/src/components/ui/collapsible.tsx` returns content
  </acceptance_criteria>
  <done>All three shadcn/ui primitive components written; exports verified by grep.</done>
</task>

</tasks>

<verification>
`grep -l "SliderPrimitive\|CheckboxPrimitive\|CollapsiblePrimitive" /Users/Oreo/Projects/frontend/src/components/ui/*.tsx` returns all three files.
</verification>

<success_criteria>
Slider, Checkbox, and Collapsible components exist in frontend/src/components/ui/ with correct Radix primitive imports and named exports.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-game-ux-enhancements/03.2-04-SUMMARY.md`
</output>
