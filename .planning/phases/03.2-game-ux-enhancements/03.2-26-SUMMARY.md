---
phase: 03.2-game-ux-enhancements
plan: "26"
subsystem: frontend
tags:
  - game-session
  - ux
  - dead-state-fix
dependency_graph:
  requires:
    - "03.2-24"
  provides:
    - active+isWatched state navigation to actors tab
  affects:
    - frontend/src/pages/GameSession.tsx
tech_stack:
  added: []
  patterns:
    - compound OR condition on session status + isWatched
key_files:
  created: []
  modified:
    - frontend/src/pages/GameSession.tsx
decisions:
  - "active+isWatched branch skips continueChain backend call — transition already occurred; calling again risks 422 on invalid state"
  - "setActiveTab setter already in scope from useState at line 36 — no additional imports needed"
metrics:
  duration: "~1 minute"
  completed_date: "2026-03-17"
  tasks_completed: 1
  files_modified: 1
requirements_closed:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
---

# Phase 03.2 Plan 26: Dead State Fix — Continue the Chain Button Summary

**One-liner:** Compound OR condition on `awaiting_continue || (active && isWatched)` eliminates the only known stuck state on the session home page where calling `continueChain` then navigating away left the user with no action button.

## What Was Built

Single surgical edit in `GameSession.tsx`: the "Continue the chain" button condition was expanded from `session.status === "awaiting_continue"` to `(session.status === "awaiting_continue" || (session.status === "active" && isWatched))`.

The click handler branches on status:
- `awaiting_continue` — calls `handleContinue()` as before (backend `continueChain` call + navigate to tabs)
- `active + isWatched` — skips the backend call (already done), directly calls `setView("tabs"); setActiveTab("actors")`

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Expand Continue the chain button condition | aeff8e0 | frontend/src/pages/GameSession.tsx |

## Verification Results

- `grep` confirms compound OR condition at line 362
- `grep` confirms `setView("tabs")` and `setActiveTab("actors")` in else-branch at lines 370-371
- Mark as Watched condition at line 381 unchanged (`active && !isWatched`)
- `npx tsc --noEmit` exits 0 with no errors

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- Modified file exists: `frontend/src/pages/GameSession.tsx` — confirmed
- Commit aeff8e0 exists in git log — confirmed
