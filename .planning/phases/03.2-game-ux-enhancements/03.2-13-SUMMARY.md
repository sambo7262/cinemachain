---
phase: 03.2-game-ux-enhancements
plan: "13"
subsystem: ui
tags: [react, react-query, gamesession, cache, setQueryData]

# Dependency graph
requires:
  - phase: 03.2-game-ux-enhancements
    provides: GameSession.tsx handleMovieConfirm with requestMovie call returning { status, session }
provides:
  - synchronous React Query cache update in handleMovieConfirm after requestMovie
  - Mark as Watched button visible after back-navigation to active session
affects:
  - GameSession.tsx session cache hydration

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "setQueryData before invalidateQueries: write the fresh response into cache synchronously so stale data is never served on re-mount, then let invalidateQueries schedule a background refresh"

key-files:
  created: []
  modified:
    - frontend/src/pages/GameSession.tsx

key-decisions:
  - "03.2-13: setQueryData([\"session\", sid], requestResult.session) inserted in handleMovieConfirm immediately after setView(\"home\") and before invalidateQueries — ensures current_movie_watched: false is in cache before user can navigate away"

patterns-established:
  - "Optimistic/synchronous cache: after any mutation that returns a full session DTO, call setQueryData before invalidateQueries to close the stale-window gap"

requirements-completed: [UX-01, UX-02, UX-03, UX-04, UX-05]

# Metrics
duration: 5min
completed: 2026-03-16
---

# Phase 03.2 Plan 13: Fix Stale Cache in handleMovieConfirm Summary

**`queryClient.setQueryData` added to `handleMovieConfirm` so `current_movie_watched: false` is written into the React Query cache synchronously after `requestMovie`, eliminating the stale-cache window that hid the Mark as Watched button on back-navigation.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-16T00:00:00Z
- **Completed:** 2026-03-16T00:05:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Identified root cause: `invalidateQueries` schedules async refetch, leaving `continue_chain` response (current_movie_watched: true) in cache during navigation
- Added `queryClient.setQueryData(["session", sid], requestResult.session)` synchronously after `requestResult` received
- TypeScript build passes — `requestResult.session` already typed as `GameSessionDTO`
- Mark as Watched button now visible immediately on return navigation to an active session after movie selection

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix stale cache in handleMovieConfirm** - `068d2f9` (fix)

## Files Created/Modified
- `frontend/src/pages/GameSession.tsx` - Added `queryClient.setQueryData(["session", sid], requestResult.session)` in `handleMovieConfirm` at line 168

## Decisions Made
- Insert `setQueryData` before `invalidateQueries` (not instead of) — synchronous write ensures correct state on immediate back-navigation; async refetch still runs to get fully fresh server data

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Mark as Watched button regression (gap-closure target of this plan) is resolved
- Remaining open blockers from 03.2-09 still require follow-up plans: Gap 3 (eligible movies without actor), Regression 1 (_ensure_movie_details_in_db corrupting credits), Regression 2 (stale movie list on actor change)

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-16*
