---
phase: 03.2-game-ux-enhancements
plan: "10"
subsystem: api
tags: [sqlalchemy, fastapi, game, async, credits]

# Dependency graph
requires:
  - phase: 03.2-game-ux-enhancements
    provides: get_eligible_actors on-demand fallback with _ensure_actor_credits_in_db inserts

provides:
  - Fresh SELECT statement for eligible-actors fallback guaranteeing new Credit rows are visible after all inserts commit
affects: [03.2-game-ux-enhancements, 04-query-mode]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Rebuild SQLAlchemy select() statement after async commit boundary — do not reuse pre-built stmt across commit points"

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "Fresh stmt reconstructed after all _ensure_actor_credits_in_db inserts committed — mirrors original stmt logic (same tmdb_id filter + picked_ids exclusion) but constructed at call-time to avoid SQLAlchemy async session transaction boundary cache miss"

patterns-established:
  - "Post-commit re-query pattern: build a new select() object rather than reusing a pre-built one when re-executing after db.commit() calls in a loop"

requirements-completed: [UX-03]

# Metrics
duration: 3min
completed: 2026-03-17
---

# Phase 03.2 Plan 10: Rebuild Eligible-Actors SELECT After On-Demand Fallback Summary

**Surgical fix replacing stale `stmt` reuse with a freshly constructed `fresh_stmt` in the `get_eligible_actors` on-demand fallback, closing Regression 1 (zero actors after multi-hop chains through `_ensure_movie_details_in_db` movies)**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-17T05:04:44Z
- **Completed:** 2026-03-17T05:07:30Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Fixed Regression 1: multi-hop chains like The Martian -> Matt Damon -> Good Will Hunting -> Minnie Driver -> South Park: Bigger Longer Uncut now return eligible actors after the on-demand fallback populates credits
- Removed stale `rows2 = await db.execute(stmt)` reuse that missed newly inserted Credit rows after SQLAlchemy async session commit boundaries
- Added fresh `fresh_stmt` construction that mirrors the original `stmt` (same `Movie.tmdb_id` filter and `Actor.tmdb_id.not_in(picked_ids)` exclusion) but is built after all `_ensure_actor_credits_in_db` commits complete

## Task Commits

Each task was committed atomically:

1. **Task 1: Rebuild eligible-actors SELECT after on-demand fallback inserts** - `be332ca` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `backend/app/routers/game.py` - Replaced stale stmt reuse with fresh_stmt construction inside the `if not actors:` fallback block

## Decisions Made
- Fresh stmt mirrors the original stmt logic exactly (same filter conditions) to ensure semantic equivalence — this is not a behavior change, only ensures the statement is evaluated after commits complete

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- Pre-existing test failures due to missing `sonarr_api_key` environment variable in pydantic settings — confirmed pre-existing before this change by running tests with git stash; no new failures introduced.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Regression 1 closed: eligible-actors returns actors for movies populated via on-demand TMDB fallback
- Regression 2 (stale movie list on actor change — React Query queryKey missing actor ID) remains open per plan 11
- Gap 3 (Eligible Movies tab load-all without actor selected) remains open per planned follow-up

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-17*
