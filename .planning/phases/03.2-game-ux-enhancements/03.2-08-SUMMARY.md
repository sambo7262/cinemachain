---
phase: 03.2-game-ux-enhancements
plan: "08"
subsystem: ui
tags: [react, typescript, gamesession, chainhistory, ux, layout]

# Dependency graph
requires:
  - phase: 03.2-05
    provides: GameSession.tsx with MovieFilterSidebar, SessionCounters, ChainHistory thumbnails, ineligible toggle
provides:
  - ChainHistory rendered at bottom of GameSession page (below Tabs)
  - Always-visible ineligible actors section (no toggle required)
  - Movies tab empty state directing user to pick an actor
affects: [03.2-deploy, phase-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Always-fetch ineligible pattern: api.getEligibleActors(sid, true) with queryKey omitting showIneligible — single fetch, client-side filter for eligible vs ineligible sections"
    - "Unconditional ineligible section: IIFE-based render checks length > 0, renders without toggle gate"

key-files:
  created: []
  modified:
    - frontend/src/pages/GameSession.tsx

key-decisions:
  - "Remove showIneligible useState entirely; always call getEligibleActors with include_ineligible=true; queryKey drops showIneligible entry to avoid stale cache buckets"
  - "ChainHistory placement after </Tabs>} closing in main flex-col container — satisfies UI-SPEC.md Chain History Position contract"
  - "Movies tab empty-state message updated to 'Pick an actor from the Eligible Actors tab to see movies.' for clear CTA"

patterns-established: []

requirements-completed: [UX-02, UX-03, UX-04]

# Metrics
duration: 2min
completed: 2026-03-16
---

# Phase 03.2 Plan 08: GameSession Layout and UX Gap Closures Summary

**Three targeted GameSession.tsx fixes: ChainHistory moved to page bottom, ineligible actors always visible without toggle, Movies tab shows actor-pick CTA when no actor selected**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-17T03:58:43Z
- **Completed:** 2026-03-17T04:00:13Z
- **Tasks:** 1 of 1
- **Files modified:** 1

## Accomplishments

- ChainHistory component relocated from top of main container to bottom of page (after Tabs panel), matching UI-SPEC.md layout contract
- Removed showIneligible toggle button entirely; ineligible actors always render below eligible list with "Already picked" badges and opacity-50 visual treatment
- Updated eligibleActors queryKey and queryFn: drops showIneligible from cache key, always calls `api.getEligibleActors(sid, true)` to include ineligible in single fetch
- Movies tab no-actor empty state now says "Pick an actor from the Eligible Actors tab to see movies." instead of the generic "No eligible movies found."

## Task Commits

1. **Task 1: Move ChainHistory to bottom, always-show ineligible actors, movies tab empty state** - `7a14650` (feat)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified

- `/Users/Oreo/Projects/frontend/src/pages/GameSession.tsx` - Removed ChainHistory from top, removed showIneligible state/toggle, updated queryKey/queryFn to always fetch ineligible, unconditional ineligible section render, updated movies empty state copy, added ChainHistory at bottom

## Decisions Made

- Remove `showIneligible` state entirely rather than keeping it defaulting to true — simplifies code, no toggle needed per updated requirements
- `queryKey: ["eligibleActors", sid, session?.current_movie_tmdb_id]` — drops `showIneligible` entry since behavior is now fixed (always true); avoids stale cache bucket from prior shape
- Empty-state condition check uses `eligibleActorsData.filter(a => a.is_eligible === false).length === 0` alongside `eligibleActors.length === 0` to show "No eligible actors found" only when truly empty

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. TypeScript compilation clean, all acceptance criteria satisfied.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All three gap-closure UX fixes applied to GameSession.tsx
- Docker rebuild and NAS deploy required to activate changes in production
- Phase 03.2 gap-closure plan set complete; Phase 4 (Query Mode) can proceed after deploy verification

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-16*
