---
phase: "03.2"
plan: "06"
type: execute
wave: 4
depends_on: ["03.2-05"]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05]

must_haves:
  truths:
    - "Docker images rebuilt and running on NAS with migration 0005 applied"
    - "User can confirm all 5 UX features work in the live browser session"
  artifacts:
    - path: "backend/alembic/versions/20260316_0005_movie_vote_count_mpaa.py"
      provides: "Migration applied to live NAS PostgreSQL database"
      contains: "revision: str = \"0005\""
  key_links:
    - from: "NAS Docker stack"
      to: "PostgreSQL movies table"
      via: "alembic upgrade head run inside backend container after rebuild"
      pattern: "alembic upgrade head"
---

<objective>
Rebuild Docker images with all Phase 03.2 changes, deploy to NAS, apply migration 0005, and run a human verify checkpoint against the live application.

Purpose: Phase 03.2 is only complete when all 5 features are confirmed working in production. Docker rebuild captures both backend (game.py changes) and frontend (filter sidebar, counters, thumbnails).
Output: Live NAS running Phase 03.2 with all 5 UX features verified.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend test suite before Docker rebuild</name>
  <files></files>
  <read_first>
    - .planning/phases/03.2-game-ux-enhancements/03.2-03-SUMMARY.md (confirm backend changes completed)
  </read_first>
  <action>
Run the full backend test suite to confirm no regressions before rebuilding:

```bash
cd /Users/Oreo/Projects/backend && pytest -x -q
```

Expected outcome: all previously passing tests pass; test_session_counters and test_step_thumbnails pass; test_eligible_movies_vote_floor, test_eligible_movies_mpaa_cached, test_eligible_actors_include_ineligible may still fail (they require a live DB with migration 0005 — acceptable at this stage).

If any previously passing test now fails, STOP and fix the regression before proceeding to Docker rebuild.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/backend && pytest -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -x -q` output does not show any FAILED test that was previously passing
    - `test_session_counters` shows PASSED
    - `test_step_thumbnails` shows PASSED
    - `test_create_session`, `test_create_session_conflict`, `test_multi_session`, `test_archive_session` all show PASSED
  </acceptance_criteria>
  <done>Backend test suite passes (minus the 3 stubs that need live DB — known acceptable).</done>
</task>

<task type="auto">
  <name>Task 2: Docker rebuild and NAS deploy</name>
  <files></files>
  <read_first>
    - .planning/STATE.md (confirm NAS Makefile rebuild target: `sambo7262/cinemachain-*:latest`)
  </read_first>
  <action>
Rebuild Docker images with `--no-cache` to pick up all Phase 03.2 changes (backend game.py, models, migration; frontend all modified files):

```bash
cd /Users/Oreo/Projects && make rebuild
```

If `make rebuild` is not available, run:
```bash
cd /Users/Oreo/Projects && docker build --no-cache -t sambo7262/cinemachain-backend:latest ./backend && docker build --no-cache -t sambo7262/cinemachain-frontend:latest ./frontend
```

After building, push images and deploy:
```bash
docker push sambo7262/cinemachain-backend:latest && docker push sambo7262/cinemachain-frontend:latest
```

On the NAS (via SSH or Synology terminal), pull and restart:
```bash
docker compose pull && docker compose up -d
```

Apply migration 0005:
```bash
docker compose exec backend alembic upgrade head
```

Confirm migration applied:
```bash
docker compose exec backend alembic current
```
Expected output includes `0005 (head)`.

Confirm new columns exist on NAS database:
```bash
docker compose exec db psql -U cinemachain -d cinemachain -c "\d movies" | grep -E "vote_count|mpaa_rating"
```
  </action>
  <verify>
    <automated>docker images | grep cinemachain</automated>
  </verify>
  <acceptance_criteria>
    - Docker images `sambo7262/cinemachain-backend:latest` and `sambo7262/cinemachain-frontend:latest` rebuilt with `--no-cache`
    - `alembic current` output on NAS shows `0005 (head)`
    - `\d movies` on NAS PostgreSQL shows `vote_count` and `mpaa_rating` columns
    - All NAS containers healthy: `docker compose ps` shows `backend`, `frontend`, `db` all running
  </acceptance_criteria>
  <done>NAS running Phase 03.2 backend and frontend; migration 0005 applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 03.2 features deployed to live NAS:
1. Movie filters (Runtime slider, Genre checkboxes, MPAA Rating checkboxes) in a collapsible sidebar
2. Movie title search (debounced 200ms, instant filter)
3. Ineligible actor toggle ("Show already-picked actors" button)
4. Chain history with real TMDB poster and actor profile thumbnails
5. Session counters (Watched count + Runtime) in session page header
  </what-built>
  <how-to-verify>
Start a live game session on the NAS. Then verify each feature:

**Feature 1 — Movie Filters (requires picking an actor first to see eligible movies):**
- Start a session, mark starting movie as watched, pick an actor
- On the Eligible Movies tab, confirm a "Filters" sidebar appears to the left of the movie list
- Expand "Runtime" — confirm a two-thumb slider appears; drag thumbs and confirm movie list narrows
- Expand "Genre" — confirm alphabetically sorted genre checkboxes; check one and confirm movie list filters
- Expand "MPAA Rating" — confirm G / PG / PG-13 / R / NR checkboxes; check one and confirm list filters
- When any filter is active, confirm sidebar header shows "Filters (N)" with a badge
- Confirm unchecking all filters restores the full list
- Confirm "Filters" categories start collapsed by default

**Feature 2 — Movie Title Search:**
- With eligible movies visible, type in the search box above the list
- Confirm the list filters instantly as you type (within 200ms of stopping)
- Type a title that matches no movies — confirm "No movies match your search." message appears
- Clear the search — confirm full list returns
- Confirm search + sidebar filters apply simultaneously (AND)

**Feature 3 — Ineligible Actor Toggle:**
- After picking at least one actor in the session, go to the Eligible Actors tab
- Confirm a "Show already-picked actors" button is visible
- Click it — confirm the button text changes to "Hide already-picked actors"
- Confirm the previously-picked actor appears below the eligible list in a separate section
- Confirm the ineligible actor row is visually muted (lower opacity) with an "Already picked" badge
- Confirm clicking an ineligible actor row does nothing (no selection, no error)
- Click "Hide already-picked actors" — confirm the ineligible section disappears

**Feature 4 — Chain History Thumbnails:**
- Advance the session to at least 2 movies (pick actor → pick next movie → mark watched)
- Scroll to the chain history table at the bottom of the session page
- Confirm movie rows show a poster-ratio thumbnail image (or grey placeholder if no poster available)
- Confirm actor rows show a circular thumbnail image (or initials placeholder if no profile image)
- Confirm TMDB images load correctly (not broken image icons)

**Feature 5 — Session Counters:**
- At the top of the session page (below the session name, above the tabs), confirm two counters are visible
- "Watched" counter shows the count of movies you've marked as watched
- "Runtime" counter shows total runtime in "Xh Ym" format (or "—" before any movies are watched)
- Mark a movie as watched and confirm the counters update on next page load

Report any feature that does not work as described above.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 features work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human approves all 5 features working in the live NAS application.
</verification>

<success_criteria>
Phase 03.2 complete: Docker deployed with migration 0005; all 5 UX features verified working in live browser session.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-game-ux-enhancements/03.2-06-SUMMARY.md`
</output>
