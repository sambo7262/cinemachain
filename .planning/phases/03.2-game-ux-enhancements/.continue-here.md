---
phase: 03.2-game-ux-enhancements
plan: "03.2-29"
task: 2
total_tasks: 2
status: blocked
last_updated: "2026-03-17T20:36:12.474Z"
---

<current_state>
03.2-29 (round 6 deploy/verify) is at the human-verify checkpoint. Task 1 (Docker rebuild + push) is complete. Task 2 (NAS verification) is partially complete — 11 of 12 tests passed. One test is failing:

**SP-1 FAIL — South Park: Bigger Longer Uncut returns 0 eligible actors consistently.**

All other tests passed:
- SP-2 PASS (new session after CSV import works)
- NEW-1/2/3 PASS (CSV import chain correctness, dead state fix)
- Tests 1-9 PASS (all round 4 regression checks)

The 03.2-29 checkpoint agent is suspended at: agentId af255edb5d80b618c
Resume signal file: .planning/phases/03.2-game-ux-enhancements/03.2-27-PLAN.md (checkpoint details)
</current_state>

<completed_work>

## This session (03.2 gap closure rounds 4-6):

- 03.2-20: `_ensure_movie_cast_in_db` helper — batch cast fetch replaces 20-call per-actor loop ✓
- 03.2-21: Guard TMDB enrichment in `get_eligible_movies` behind `actor_id is not None` ✓
- 03.2-22: `staleTime: 0` on session query; 3s concession rotation; empty-state suppressed during fetch ✓
- 03.2-23: WatchEvent batch-insert for prior CSV-imported steps ✓
- 03.2-24: Deploy/verify (round 4) — SUPERSEDED by failures found ✓ (summary written)
- 03.2-25: Fix `_ensure_movie_cast_in_db` Movie stub upsert before loop + CSV background pre-fetch ✓
- 03.2-26: Session home dead state fix (`active + isWatched` → "Continue the chain" → setView tabs) ✓
- 03.2-27: Deploy/verify (round 5) — SUPERSEDED by SP-1 failure ✓ (summary written)
- 03.2-28: `_prefetch_credits_background` reduced from 20 TMDB calls to 1 (delegates to `_ensure_movie_cast_in_db`) ✓
- 03.2-29: Task 1 complete (backend rebuilt + pushed). Blocked at verify gate.

Also written this session:
- Retroactive summaries for 03.2-12 and 03.2-19 (superseded deploy rounds)

</completed_work>

<remaining_work>

## Immediate: South Park regression (SP-1)

**Root cause not yet confirmed.** TMDB data is fine (movie ID 9473, 21 cast members in standard `cast` array). The bug is in the code execution path.

**Diagnostic approach for next session:**
1. Check backend container logs: `docker logs cinemachain-backend --tail 100` immediately after triggering a South Park eligible actors request on NAS
2. Look for DB integrity errors (likely a NOT NULL violation on `Actor.fetched_at` — the column is `Mapped[datetime]` without Optional but `pg_insert` doesn't apply Python-side defaults, potentially inserting NULL)
3. OR: `_ensure_movie_cast_in_db` might be succeeding for the background pre-fetch but the request-scoped on-demand fallback is not committing correctly

**Specific things to check in `_ensure_movie_cast_in_db` (backend/app/routers/game.py lines 353-414):**
- `Actor.fetched_at` — `Mapped[datetime]` without Optional + no server_default — pg_insert without this field may cause NOT NULL violation (silently caught in background task, causes 0 actors returned in on-demand path)
- The `on_conflict_do_nothing()` on Credit without `index_elements` — compare with `_ensure_actor_credits_in_db` which uses `index_elements=["movie_id", "actor_id"]`

**Suspected fix (to verify after diagnostics):**
Add `fetched_at=datetime.utcnow()` to `pg_insert(Actor).values(...)` in `_ensure_movie_cast_in_db`. Same fix may be needed for the Movie stub insert if `Movie.fetched_at` is also NOT NULL without server_default.

## After SP-1 fix:
- 03.2-30: Fix `_ensure_movie_cast_in_db` fetched_at (or whatever diagnostics reveal)
- 03.2-31: Rebuild backend + NAS deploy + final verify (SP-1 + regression smoke)
- Then: Phase 03.2 verification (gsd-verifier) → ROADMAP update → phase complete

## Known lower-priority items (Phase 4):
- Runtime 0 for CSV-imported movies (stubs lack runtime data — lazy enrichment gap)
- Nightly TMDB cache pre-population: top ~5000 movies by vote count, nightly batch job
  → eliminates all on-demand TMDB calls for mainstream films
  → eliminates rate-limiting issues entirely
  → stored in .planning/phases/03.2-game-ux-enhancements/03.2-CONTEXT.md (Phase 4 Optimization Idea section)

</remaining_work>

<decisions_made>

- `_prefetch_credits_background` changed from 20-call per-actor loop to single `_ensure_movie_cast_in_db` call — reduces TMDB calls from 20+ to 1 per session creation
- `_ensure_movie_cast_in_db` now upserts Movie stub before loop so CSV-imported sessions (which never insert Movie rows) can get credits populated
- Session home "Continue the chain" button expanded to cover `active + isWatched` dead state — no backend call needed, just `setView("tabs")`
- WatchEvent unique constraint is on `tmdb_id` only (no session_id) — this is a pre-existing design; the session-scoped watched check works by filtering WatchEvents to `tmdb_id IN session_movie_ids`
- Phase 4 caching optimization captured: top 5000 movies pre-populated nightly → eliminates all on-demand TMDB calls for mainstream films

</decisions_made>

<blockers>

- **SP-1 regression**: South Park: Bigger Longer Uncut (TMDB ID 9473) returns 0 eligible actors consistently. TMDB data confirmed good (21 cast members). Bug is in code — likely `Actor.fetched_at` NOT NULL violation in `_ensure_movie_cast_in_db` (not caught in on-demand path, OR caught silently in background path leaving 0 credits in DB). Requires diagnostic via docker logs.

- **03.2-29 checkpoint agent**: Still suspended (agentId: af255edb5d80b618c). Do NOT resume this agent — instead, write a retroactive summary for 03.2-29 noting it's superseded, then create 03.2-30 (fix) and 03.2-31 (verify) as new plans.

</blockers>

<context>
Phase 03.2 is nearly complete — 28 plans done, just this one persistent South Park bug remaining. All core functionality (session management, eligible actors/movies, CSV import, session home state machine, cross-session isolation, concession messages) is working correctly on NAS.

The South Park bug is isolated to `_ensure_movie_cast_in_db` not inserting credits correctly for that specific movie. The most likely cause is the `Actor.fetched_at` column being `Mapped[datetime]` (NOT NULL inferred) with only a Python-side `default=datetime.utcnow` — `pg_insert` raw SQL doesn't apply Python-side ORM defaults, so `fetched_at` gets NULL, triggering a NOT NULL violation.

The fix is a one-liner: add `fetched_at=datetime.utcnow()` to the `pg_insert(Actor).values(...)` call in `_ensure_movie_cast_in_db`. May also need the same for the Movie stub insert.

Note: `_ensure_actor_credits_in_db` works fine because it also uses `pg_insert(Actor).values(...)` WITHOUT `fetched_at`... wait, that's confusing. Need to verify whether `fetched_at` actually has a server_default in the Alembic migration (even if not in the ORM model). If the DB column has `DEFAULT NOW()`, then pg_insert without fetched_at would work fine. Check the migration file.

Start diagnostic with: `docker logs cinemachain-backend --tail 200` on NAS after triggering South Park eligible actors.
</context>

<next_action>
1. Check Alembic migration for `actors.fetched_at` column — does it have `server_default='now()'`?
   File: backend/alembic/versions/ (look for the initial migration that creates actors table)
2. If no server_default: add `fetched_at=datetime.utcnow()` to pg_insert(Actor) in _ensure_movie_cast_in_db + same for pg_insert(Movie) stub
3. If server_default exists: check backend logs on NAS for the actual error
4. Create plan 03.2-30 (fix) + 03.2-31 (verify)
5. Write retroactive summary for 03.2-29 (superseded by SP-1 failure)
</next_action>
