---
phase: 03.2-game-ux-enhancements
plan: "21"
subsystem: api
tags: [fastapi, tmdb, game, eligible-movies, performance]

requires:
  - phase: 03.2-game-ux-enhancements
    provides: get_eligible_movies combined-view branch (03.2-11) and _ensure_movie_details_in_db helper (03.2-07)

provides:
  - get_eligible_movies combined-view path returns immediately using only DB-cached data (no TMDB calls)
  - actor_id is not None guard wraps both _ensure_movie_details_in_db and _fetch_mpaa_rating

affects:
  - 03.2-game-ux-enhancements
  - Phase 4 (Query Mode) performance baseline

tech-stack:
  added: []
  patterns:
    - "actor_id is not None guard: enrichment-heavy blocks gated behind parameter presence check to avoid NAS timeout"

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "03.2-21: Both TMDB enrichment blocks merged under single `if actor_id is not None and hasattr(...)` guard — eliminates duplicate hasattr checks and makes combined-view vs actor-scoped branching explicit"

patterns-established:
  - "NAS timeout guard: any sequential TMDB HTTP loop in an endpoint must be gated behind a parameter that confirms the user actually requested that enrichment path"

requirements-completed:
  - UX-03

duration: 5min
completed: 2026-03-17
---

# Phase 03.2 Plan 21: TMDB Enrichment Guard Summary

**`if actor_id is not None` guard wrapping both _ensure_movie_details_in_db and _fetch_mpaa_rating in get_eligible_movies, eliminating 20-40+ sequential TMDB HTTP calls on NAS combined-view load**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-17T14:48:00Z
- **Completed:** 2026-03-17T14:53:32Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Combined-view (Eligible Movies tab without actor selected) now returns immediately from DB cache with no TMDB calls
- Eliminated the root cause of 504 timeout on large sessions (20+ movies) on NAS nginx 60s limit
- Two previously separate `if hasattr(request.app.state, "tmdb_client"):` blocks merged into one combined outer guard
- Actor-scoped path unchanged — still enriches genres, runtime, and MPAA rating on demand

## Task Commits

1. **Task 1: Guard _ensure_movie_details_in_db and _fetch_mpaa_rating behind actor_id check** - `a0c4570` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `backend/app/routers/game.py` - Lines 1224-1246: two TMDB enrichment blocks replaced by single `if actor_id is not None and hasattr(...)` combined guard

## Decisions Made

- Merged the two separate `if hasattr(request.app.state, "tmdb_client"):` checks into one outer guard — cleaner, avoids duplicate hasattr lookups, and makes the branching intent explicit in code

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - the edit was a single-block replacement matching the plan specification exactly.

## User Setup Required

None - no external service configuration required. Docker rebuild and NAS deploy required to activate the fix on production.

## Next Phase Readiness

- Combined-view Eligible Movies tab should now load in under 5 seconds on NAS (no TMDB calls in that path)
- Docker rebuild + NAS deploy required before verifying the 504 timeout is resolved in production
- Actor-scoped enrichment path remains intact and unchanged

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-17*
