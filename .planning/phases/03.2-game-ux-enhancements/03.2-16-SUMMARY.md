---
phase: 03.2-game-ux-enhancements
plan: "16"
subsystem: api
tags: [fastapi, tmdb, performance, game]

# Dependency graph
requires:
  - phase: 03.2-game-ux-enhancements
    provides: get_eligible_movies combined-view branch with DB-backed filmography
provides:
  - get_eligible_movies combined-view with no sequential TMDB calls; uses DB credits only
affects: [game-performance, eligible-movies, combined-view]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "03.2-16: Combined-view else-branch queries only DB credits; no on-demand TMDB enrichment during page load — eliminates 20+ sequential TMDB calls that caused 30-60s NAS load time"
  - "03.2-16: tmdb variable assignment removed from else-branch alongside the for-loop; _ensure_movie_details_in_db after both branches uses its own _tmdb2 binding and is unaffected"

patterns-established: []

requirements-completed: [UX-03]

# Metrics
duration: 5min
completed: 2026-03-16
---

# Phase 03.2 Plan 16: Remove Sequential TMDB Enrichment from Combined-View Summary

**Deleted 5-line _ensure_actor_credits_in_db for-loop from get_eligible_movies combined-view branch, eliminating 20+ sequential TMDB calls on Eligible Movies tab load**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-16T00:00:00Z
- **Completed:** 2026-03-16T00:05:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Removed `for aid in eligible_actor_tmdb_ids: await _ensure_actor_credits_in_db(aid, tmdb, db)` from the combined-view (else) branch of `get_eligible_movies`
- Removed the unused `tmdb: TMDBClient = request.app.state.tmdb_client` assignment that accompanied it
- Combined-view now returns immediately using credits already in DB — no sequential TMDB calls on page open
- Individual actor enrichment still handled lazily by the actor_id-scoped branch (line 1150) when a user selects a specific actor

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove sequential TMDB enrichment from combined-view branch** - `e052e1d` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `backend/app/routers/game.py` - Removed 5-line TMDB enrichment loop (comment + tmdb var + for-loop) from combined-view else-branch

## Decisions Made
- Combined-view branch should not call TMDB at all on page load — background pre-fetch (`_prefetch_credits_background`) at session creation populates credits; individual actor selection lazy-fetches via actor_id branch; combined-view only needs what is already in DB
- If credits for some actors are not yet cached, those actors return no movies in combined-view — acceptable tradeoff vs 30-60s load time

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — tests confirmed identical before/after (pre-existing failures only: plex_sync/plex_webhook errors are unrelated to this change).

## User Setup Required

None - no external service configuration required. Docker rebuild + NAS deploy will activate the performance improvement.

## Next Phase Readiness

- Combined-view load time for Eligible Movies tab is now bounded by DB query latency only (sub-second on NAS)
- Gap 3 (eligible movies without actor) is fully resolved: 03.2-11 wired the frontend query, 03.2-16 removes the backend bottleneck
- Phase 4 (Query Mode) can proceed

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-16*
