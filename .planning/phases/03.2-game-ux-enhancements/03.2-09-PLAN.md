---
phase: 03.2
plan: 09
type: execute
wave: 2
depends_on:
  - "03.2-07"
  - "03.2-08"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "Runtime filter excludes movies outside the selected range"
    - "Genre filter shows genre options and filters the movie list"
    - "Chain history appears at the bottom of the session page"
    - "Ineligible actors are always visible below eligible actors"
    - "watched_runtime_minutes shows the correct sum of watched movie runtimes"
    - "Movies tab without actor shows a 'pick an actor' message instead of being blank"
  artifacts:
    - path: "backend/app/routers/game.py"
      provides: "_ensure_movie_details_in_db called in eligible-movies endpoint"
    - path: "frontend/src/pages/GameSession.tsx"
      provides: "ChainHistory at bottom, always-visible ineligible section, movies tab empty state"
  key_links:
    - from: "eligible-movies API response"
      to: "client-side runtime filter"
      via: "movie.runtime is non-null after TMDB detail fetch"
    - from: "eligible-movies API response"
      to: "genre filter options in MovieFilterSidebar"
      via: "movie.genres is non-null JSON string after TMDB detail fetch"
---

<objective>
Docker rebuild, push to Docker Hub, NAS deploy, and human verification of all 5 gap closures.

Purpose: The gap closure code from plans 07 and 08 must be built into Docker images and deployed to NAS before verification is possible.

Output: All 5 gaps confirmed closed in live NAS environment.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.2-game-ux-enhancements/03.2-07-SUMMARY.md
@.planning/phases/03.2-game-ux-enhancements/03.2-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend tests and rebuild Docker images</name>
  <files></files>
  <read_first>
    - .planning/phases/03.2-game-ux-enhancements/03.2-07-SUMMARY.md (confirm task 1 complete)
    - .planning/phases/03.2-game-ux-enhancements/03.2-08-SUMMARY.md (confirm task 1 complete)
  </read_first>
  <action>
Run backend test suite first to catch any regressions from plan 07:
```bash
cd /Users/Oreo/Projects && python -m pytest backend/tests/ -x -q
```
Expected: 19 passed, 46 skipped (app-level tests skip without stack — same as plan 06 baseline).

If tests pass, rebuild both Docker images with --no-cache and push to Docker Hub:
```bash
cd /Users/Oreo/Projects && make rebuild
```
The Makefile `rebuild` target builds `sambo7262/cinemachain-backend:latest` and `sambo7262/cinemachain-frontend:latest` with `--no-cache` and pushes both to Docker Hub.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects && python -m pytest backend/tests/ -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest backend/tests/ -x -q` exits 0
    - No test failures (skips OK — expected for app-level tests without running stack)
    - Docker build succeeds for both backend and frontend images
    - Both images pushed to Docker Hub (docker push exits 0 for both tags)
  </acceptance_criteria>
  <done>Backend tests pass. Both Docker images rebuilt --no-cache and pushed to Docker Hub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: NAS deploy and verify all 5 gaps closed</name>
  <what-built>
    Plan 07: Backend `_ensure_movie_details_in_db` — fetches full TMDB movie details (genres + runtime) on-demand for movie stubs with genres=NULL during eligible-movies requests.
    Plan 08: Frontend fixes — ChainHistory moved to bottom of page; ineligible actors always visible (no toggle); movies tab empty state says "Pick an actor from the Eligible Actors tab to see movies."
  </what-built>
  <how-to-verify>
On the NAS, pull and restart containers:
```bash
docker compose pull && docker compose up -d
```

Then verify each gap:

**Gap 1 — Runtime filter works:**
1. Start or open an active session, mark current movie as watched, click Continue the chain
2. Pick any actor, go to Eligible Movies tab
3. Open the Runtime filter in the sidebar, set a narrow range (e.g. 90m – 120m)
4. EXPECTED: Movie list updates — only movies within the range appear. Movies outside the range are hidden.
5. Reset to "Any runtime" — all movies reappear.

**Gap 2 — Genre filter shows options:**
1. In the same Eligible Movies tab with an actor selected
2. Open the Genre filter in the sidebar
3. EXPECTED: Genre checkboxes appear (e.g. "Action", "Drama", "Thriller"). On first load after deploy, TMDB detail fetch runs — wait a few seconds then reload if needed.
4. Check a genre — movie list filters to only that genre.

**Gap 3 — Movies tab empty state without actor:**
1. Click Continue the chain, then click "Eligible Movies" tab WITHOUT picking an actor
2. EXPECTED: Message "Pick an actor from the Eligible Actors tab to see movies." appears instead of a blank tab.

**Gap 4 — Chain History at bottom:**
1. Open any active session and scroll down the page
2. EXPECTED: Chain History table appears below the tabs panel, not above the session state panel. Verify by scrolling — chain history should be the last section before page bottom.

**Gap 5 — Runtime counter works:**
1. Open a session where at least one movie has been marked as watched
2. Look at the "Runtime" counter in the session header
3. EXPECTED: Shows a non-zero value like "1h 55m" (will work after the first eligible-movies request fetches runtime data for the watched movies). If it shows "—" for a session with no watched movies yet, that is correct behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 gaps are closed, or describe which gaps remain</resume-signal>
</task>

</tasks>

<verification>
All 5 gaps from plan 03.2-06 must be confirmed closed:
- Runtime filter excludes out-of-range movies
- Genre filter shows options and filters
- Movies tab without actor shows helpful empty state
- ChainHistory renders below tabs panel
- watched_runtime_minutes shows non-zero value for sessions with watched movies
</verification>

<success_criteria>
- Backend tests pass (0 failures)
- Docker images rebuilt and pushed successfully
- Human verification confirms all 5 gaps closed on live NAS
- Phase 03.2 declared complete
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-game-ux-enhancements/03.2-09-SUMMARY.md`
</output>
