---
phase: 03.2-game-ux-enhancements
plan: "25"
subsystem: api
tags: [fastapi, sqlalchemy, tmdb, background-tasks, csv-import]

# Dependency graph
requires:
  - phase: 03.2-game-ux-enhancements
    provides: WatchEvent batch-insert for CSV import (03.2-23), CSV import session creation (03.2-14)
provides:
  - _ensure_movie_cast_in_db with Movie stub upsert before cast loop and single PK lookup outside loop
  - import_csv_session with BackgroundTasks dependency and _prefetch_credits_background call after WatchEvent commit
affects:
  - game-session CSV import flow
  - eligible actors/movies after CSV import

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Movie stub upsert (on_conflict_do_nothing) in cast-population helper ensures FK target exists for CSV-imported sessions
    - Movie PK resolved once outside loop instead of per-iteration to eliminate N redundant SELECTs

key-files:
  created: []
  modified:
    - backend/app/routers/game.py

key-decisions:
  - "_ensure_movie_cast_in_db upserts Movie stub with title='' before cast loop — CSV sessions never insert into movies table so the FK target was missing; empty title is intentional (details filled by _ensure_movie_details_in_db later)"
  - "Movie PK resolved once before the for loop (down from per-iteration SELECT) — same movie_tmdb_id for all cast members; eliminates up to 20 redundant DB round-trips per call"
  - "background_tasks.add_task placed unconditionally after if prior_movie_ids: block in import_csv_session — mirrors create_session; ensures cast is pre-fetched even for single-movie imports where prior_movie_ids is empty"

patterns-established:
  - "Pattern: Always upsert Movie stub before inserting Credits in cast-population helpers — prevents FK violation for sessions that create steps without inserting into movies table"
  - "Pattern: Resolve FK parent PK once outside any loop that inserts child rows — avoids N redundant SELECTs when parent is constant across all iterations"

requirements-completed: [UX-01, UX-02, UX-03, UX-04, UX-05]

# Metrics
duration: 6min
completed: 2026-03-17
---

# Phase 03.2 Plan 25: CSV Import Actor Eligibility Fix Summary

**Movie stub upsert + single PK lookup in _ensure_movie_cast_in_db; BackgroundTasks pre-fetch in import_csv_session restores eligible actors after CSV import**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-17T15:17:51Z
- **Completed:** 2026-03-17T15:23:51Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Fixed FK violation in `_ensure_movie_cast_in_db`: CSV-imported sessions now have a Movie stub in the `movies` table before cast iteration, so Credit inserts succeed
- Eliminated N redundant Movie SELECTs per cast-population call (was 1 per cast member, now 1 total before the loop)
- Added `BackgroundTasks` to `import_csv_session` and fire `_prefetch_credits_background(last_movie_id, tmdb)` unconditionally after WatchEvent commit — mirrors `create_session` pattern

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix _ensure_movie_cast_in_db — upsert Movie stub and move PK lookup outside loop** - `d27841d` (fix)
2. **Task 2: Add BackgroundTasks to import_csv_session and fire _prefetch_credits_background** - `947862b` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `backend/app/routers/game.py` - Movie stub upsert + single PK lookup in `_ensure_movie_cast_in_db`; `BackgroundTasks` parameter + `background_tasks.add_task` in `import_csv_session`

## Decisions Made
- Movie stub upsert uses `title=""` (empty string, not null) since Movie.title is non-nullable. Title will be filled by `_ensure_movie_details_in_db` on subsequent eligible-movies calls.
- `background_tasks.add_task` placed after `if prior_movie_ids:` block (outside it) to ensure unconditional execution — even single-movie CSV imports (no prior movies) still need the pre-fetch.
- `tmdb` variable already assigned at line ~565 in `import_csv_session` — no redeclaration needed in the background task call.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. Docker rebuild + NAS deploy required to activate fix.

## Next Phase Readiness
- After Docker rebuild and NAS deploy, CSV-imported sessions will correctly populate eligible actors on first request
- `_ensure_movie_cast_in_db` is now safe for CSV-import sessions and normal sessions alike

---
*Phase: 03.2-game-ux-enhancements*
*Completed: 2026-03-17*
