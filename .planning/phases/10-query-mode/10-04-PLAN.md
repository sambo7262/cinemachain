---
phase: 10-query-mode
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - frontend/src/pages/SearchPage.tsx
autonomous: false
requirements: [QMODE-04, QMODE-05, QMODE-06]

must_haves:
  truths:
    - "Clicking 'Download via Radarr' in the splash dialog makes a POST /movies/{id}/request call"
    - "After a successful Radarr request, button shows 'Added to Radarr' for 2 seconds then resets"
    - "After 'already_in_radarr' response, button shows 'Already in Radarr' (stays, no auto-reset)"
    - "Clicking 'Watch Online' makes a PATCH /movies/{id}/watched?source=online call"
    - "After 'Watch Online' success, button shows 'Marked as Watched' for 2 seconds then resets"
    - "If Radarr returns 'error', inline error message appears below buttons"
    - "Dialog stays open after either action — user closes manually via Close or backdrop"
    - "Human verifies all action states in browser"
  artifacts:
    - path: "frontend/src/pages/SearchPage.tsx"
      provides: "Confirmed action button states with correct timing and error handling"
      contains: "requestMovieStandalone, markWatchedOnline, radarrStatus"
  key_links:
    - from: "frontend/src/pages/SearchPage.tsx"
      to: "/api/movies/{tmdb_id}/request"
      via: "api.requestMovieStandalone(splashMovie.tmdb_id) call in onClick handler"
      pattern: "requestMovieStandalone"
    - from: "frontend/src/pages/SearchPage.tsx"
      to: "/api/movies/{tmdb_id}/watched"
      via: "api.markWatchedOnline(splashMovie.tmdb_id) call in onClick handler"
      pattern: "markWatchedOnline"
---

<objective>
Verify and harden the splash dialog action buttons (Download via Radarr + Watch Online) — the interactive confirmation states, error handling, and timing. Then checkpoint for human sign-off on the complete Query Mode flow.

Purpose: Plan 10-03 created the full SearchPage including splash buttons. This plan ensures the action button state machine matches the UI-SPEC exactly (timing, labels, error display), then gets human confirmation that the page works end-to-end in a browser.

Output: SearchPage.tsx with verified action button state machine + human checkpoint approval of the complete Query Mode.
</objective>

<execution_context>
@/Users/Oreo/Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Oreo/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-query-mode/10-CONTEXT.md
@.planning/phases/10-query-mode/10-UI-SPEC.md
@.planning/phases/10-query-mode/10-03-SUMMARY.md

<interfaces>
<!-- UI-SPEC action button states (exact copy from 10-UI-SPEC.md) -->

| Button | Variant | Default label | Loading label | Success label |
|--------|---------|---------------|---------------|---------------|
| Primary (Radarr) | default | "Download via Radarr" | "Requesting..." | "Added to Radarr" |
| Secondary (Watch) | outline | "Watch Online" | "Saving..." | "Marked as Watched" |
| Cancel | outline | "Close" | — | — |

Special Radarr states:
- `already_in_radarr` → show "Already in Radarr" (informational, no auto-reset)
- `error` → show inline `<p className="text-sm text-destructive">` error message below buttons

Timing:
- Success state duration: 2 seconds, then reset to default labels
- Dialog remains open after action — user must click Close or backdrop to dismiss

<!-- Current SearchPage.tsx radarrStatus state values used in Plan 10-03 -->
radarrStatus: null | "queued" | "already_in_radarr" | "error" | "watched"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix action button state machine in SearchPage.tsx</name>
  <files>frontend/src/pages/SearchPage.tsx</files>
  <action>
Read the current `SearchPage.tsx` splash dialog section. Verify the action button state machine exactly matches the UI-SPEC.md contract. Fix any deviations.

**Checklist to verify and fix:**

1. **"Download via Radarr" button labels:**
   - Default (radarrStatus is null, not loading): `"Download via Radarr"`
   - Loading (`radarrLoading === true`): `"Requesting..."`
   - Success (`radarrStatus === "queued"`): `"Added to Radarr"` → 2 second timeout → reset to null
   - Already queued (`radarrStatus === "already_in_radarr"`): `"Already in Radarr"` — NO auto-reset (stays until dialog closed)
   - Error: button returns to default label; error shown inline below buttons

2. **"Watch Online" button labels:**
   - Default: `"Watch Online"`
   - Loading (`watchedLoading === true`): `"Saving..."`
   - Success (`radarrStatus === "watched"`): `"Marked as Watched"` → 2 second timeout → reset to null

3. **Error display:**
   - When `radarrStatus === "error"`: render `<p className="text-sm text-destructive">Request failed. Try again or check Radarr.</p>` inside DialogFooter above the buttons

4. **Dialog close behavior:**
   - `onOpenChange` handler: `setSplashOpen(open)` AND `if (!open) { setRadarrStatus(null); setRadarrLoading(false); setWatchedLoading(false) }` — clean state on close

5. **Timeout pattern must be exact (2 seconds per UI-SPEC, not 3):**
   ```typescript
   setTimeout(() => setRadarrStatus(null), 2000)
   ```
   Not 3000. Fix if wrong.

6. **Button disabled states:**
   - Radarr button: `disabled={radarrLoading || watchedLoading}` (disable both buttons while either is loading)
   - Watch Online button: `disabled={radarrLoading || watchedLoading}`
   - Close button: never disabled

7. **`already_in_radarr` NO auto-reset:**
   The `setTimeout(() => setRadarrStatus(null), 2000)` call MUST NOT fire when status is `"already_in_radarr"`. The informational state stays until dialog is closed.

   Correct pattern:
   ```typescript
   if (result.status === "queued") {
     setRadarrStatus("queued")
     setTimeout(() => setRadarrStatus(null), 2000)
   } else if (result.status === "already_in_radarr") {
     setRadarrStatus("already_in_radarr")
     // NO setTimeout — stays until close
   } else {
     setRadarrStatus("error")
   }
   ```

After auditing, apply all fixes. If the state machine from Plan 10-03 is already correct, make no changes but confirm with a comment.

Run TypeScript compile check to confirm no new errors introduced.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/CinemaChain/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- Splash dialog action button state machine matches UI-SPEC.md exactly
- 2-second timeout used (not 3)
- `already_in_radarr` does NOT auto-reset
- Error renders as inline `text-destructive` paragraph
- TypeScript compiles clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 10 Query Mode page at /search. Implements:
- Genre chips landing state (14 genres)
- Title search (default or m: prefix)
- Actor/director search (a: or d: prefix)
- Results table with sort by rating/year/RT/runtime, null-stable
- Unwatched Only toggle
- MovieFilterSidebar for genre/MPAA/runtime filtering
- Movie splash dialog with Download via Radarr + Watch Online action buttons
- Confirmation states and error handling

Backend endpoints:
- GET /search/movies?q=
- GET /search/actors?q=
- GET /movies/popular?genre=
- POST /movies/{tmdb_id}/request
- PATCH /movies/{tmdb_id}/watched?source=online
  </what-built>
  <how-to-verify>
Start the dev server if not already running:
```bash
cd /Users/Oreo/Projects/CinemaChain/frontend && npm run dev
```
(Backend must also be running: `cd /Users/Oreo/Projects/CinemaChain/backend && uvicorn app.main:app --reload`)

**Test 1 — Landing state:**
1. Navigate to http://localhost:5173/search
2. Verify: genre chips grid with "Browse by Genre" heading appears
3. Verify: no results table visible, no error state

**Test 2 — Genre browse (QMODE-03):**
1. Click the "Action" chip
2. Verify: chip highlights (variant="default"), loading skeleton appears briefly
3. Verify: results table appears with Action movies (poster, year, rating columns)
4. Verify: default sort is rating descending (highest rated movies first)
5. Verify: clicking "Action ×" chip clears back to landing state

**Test 3 — Title search (QMODE-01):**
1. Type "inception" in the search box
2. Verify: after ~300ms debounce, results appear with enriched data (RT score, MPAA, runtime, genres)
3. Verify: Inception (2010) appears with Christopher Nolan movie data

**Test 4 — Actor search (QMODE-02):**
1. Type "a: christopher nolan" in the search box
2. Verify: results appear including movies where Nolan is a director (Interstellar, Dark Knight, etc.)
3. Verify: results include BOTH acting credits (if any) AND directing credits

**Test 5 — Sort and filter (QMODE-04, QMODE-05):**
1. With any results showing, click "Year" column header
2. Verify: results re-sort by year; null years go to bottom
3. Click "Year" again — verify sort direction reverses
4. Click "Unwatched Only" toggle
5. Verify: watched movies (green "Watched" badge) disappear from results

**Test 6 — Radarr request (QMODE-06):**
1. Click any movie row to open the splash dialog
2. Verify splash shows: poster, badges (TMDB rating, RT, MPAA, runtime, year), overview, TMDB link
3. Click "Download via Radarr"
4. Verify: button shows "Requesting..." while pending
5. Verify: button shows "Added to Radarr" or "Already in Radarr" after response
6. If "Added to Radarr": verify button resets to "Download via Radarr" after ~2 seconds
7. If "Already in Radarr": verify it stays (no auto-reset)

**Test 7 — Watch Online:**
1. In any open splash dialog, click "Watch Online"
2. Verify: button shows "Saving..." then "Marked as Watched"
3. Verify: resets after ~2 seconds

**Test 8 — Close behavior:**
1. Perform a Radarr request to get a confirmation state
2. Click "Close" button
3. Open the same movie again
4. Verify: button is back to default "Download via Radarr" (state cleaned on close)
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 tests pass, or describe any issues found (include the test number).</resume-signal>
</task>

</tasks>

<verification>
Final automated check after human approval:

```bash
cd /Users/Oreo/Projects/CinemaChain/backend && pytest tests/test_search.py -v
```
Expected: 4 PASSED.

```bash
cd /Users/Oreo/Projects/CinemaChain/frontend && npm test -- --run SearchPage
```
Expected: 5 PASSED.

```bash
cd /Users/Oreo/Projects/CinemaChain/backend && pytest
```
Expected: all existing tests pass + 4 new search tests pass.
</verification>

<success_criteria>
- Action button state machine exactly matches UI-SPEC.md (2s timeout, already_in_radarr stays, error inline)
- Human has verified all 8 test scenarios in a running browser
- `pytest tests/test_search.py` green (4 tests)
- `npm test -- --run SearchPage` green (5 tests)
- `pytest` full suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/10-query-mode/10-04-SUMMARY.md`
</output>
